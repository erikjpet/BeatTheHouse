# env06_7 Package C second-rejection owner decision evidence

Status: **UNREVIEWED / OPTION-NEUTRAL / READ-ONLY**

Decision required: choose the disposition of Package C after two independent
rejections. This packet records three executable choices without selecting,
ranking or recommending one. No third ordinary remediation cycle is authorized.

The complete `env06_7` row remains 55 scenarios. Package C is only the Bar (7)
and Jazz Club (4) intake slice; package acceptance is not row completion.

## 1. Binding sources and exact identities

Captured from the local repository on 2026-08-27. Every continuation and
assembly intake must revalidate these identities against current accepted main
and the accepted env06_6 successor.

| Classification | Exact identity | Tree | Consequence |
| --- | --- | --- | --- |
| current local `main` used by this packet | `9ea919fe9b53ab3ae37e085ed462febaa8ad76f8` | `8521ef294311e3bdcb32d51d644ac76bd425e841` | documentation base only |
| full row prompt | `docs/todo/env06_7_all_variations_sequence_rework_prompt.md` | current accepted file at intake | all 55 identity conversions and cross-catalog gates remain binding |
| fixed package checklists | branch/head `codex/review-env06_7-package-checklists` at `849aa8c2487398622dc5785dc43a6458bc02c438` | `a8fa3857cf2ec29e6bb32074c7e6cf6a11528436` | common, Package C and assembly checklists; additive to the full prompt |
| first rejected Package C head | `ca41e6a2b5afb0fccbd6bd85d6966ba2ca44ad2d` | `4594f95b9a11174082832aada23122c2195d2be3` | preserved first candidate; parent `114348da` |
| second rejected Package C head | `cb684cc2cd5778dcabe00fcf4a21c6c1b6b5f05d` | `2ea719bfdbb3f5d748b23235304fee37b90b7792` | descendant of `ca41e6a2` by three commits; frozen after second rejection |
| second rejected handoff | `4aee8b9cbd33e525406b845c2bfb3087ecd9737d` | `f303e20b43659c679e9f98fb803df570346d60e4` | documentation child of `cb684cc2`; evidence only |

The checklist files are:

- `docs/plans/env06_7_package_acceptance_common.md`;
- `docs/plans/env06_7_package_c_bar_jazz_checklist.md`; and
- `docs/plans/env06_7_assembly_acceptance_checklist.md`.

They require per-package complete semantic snapshots, hidden-state privacy,
production composition, hostile atomicity, every input path, performance and
current captures/contact sheets. The assembly checklist does not convert those
package requirements into optional assembly-only work.

The second candidate's exact eight-path net payload relative to frozen base
`855a2961` is:

- `data/environments/scenario_sequences/env06_7_bars_road.json`;
- `docs/plans/env06_7_package_c_assembly_seam.md`;
- `docs/plans/env06_7_package_c_remediation_proof.md`;
- `docs/plans/env06_7_package_c_sequence_dossiers.json`;
- `scripts/core/scenario_handlers/bars_road.gd`;
- `scripts/tests/foundation/env06_7_package_c_contract.gd`;
- `scripts/ui/scenario_renderers/bars_road.gd`; and
- `tools/env06_7_package_c_author.gd`.

Neither rejected head may be landed wholesale or used as a new implementation
base. An authorized continuation starts from the accepted env06_6 successor on
then-current main and replays reviewed Package C net payload by three-way
semantic application. Generated `.tmp`, `.tools`, `review_artifacts` and owner
files remain excluded.

## 2. Fixed Package C inventory

All options preserve package id `env06_7_bars_road`, extension id `bars_road`
and exactly these established ids:

**Bar:** `bar_wake`, `bar_fight_night`, `bar_payday_rush`, `bar_lock_in`,
`bar_darts_league_night`, `bar_live_band`, `bar_dead_tuesday`.

**Jazz Club:** `jazz_club_guest_legend`, `jazz_club_rent_party`,
`jazz_club_recording_night`, `jazz_club_union_trouble`.

Weights, rumor anchors, compatible consequences and save identity remain stable
unless a separately logged code-reality reason and compatibility proof authorizes
a change. Package C never edits game/audio authority, env06_6 runtime/schema,
crew/world models, the board, main or shared catalog ownership paths.

## 3. First rejected head: `ca41e6a2`

The first candidate created eleven dossiers and calculated unique package-local
signatures, but the physical sequences were produced from one generic authoring
template. A scenario received renamed verbs, objects, actors, tags and outcomes
while progressing through the same arrival/task/work/terminal machinery. The
candidate did not establish that the eleven checklist identities had distinct
live branch topology, spatial targets/routes and branch-specific room aftermath.

Its recorded pairwise uniqueness and dossier/capture identifiers were claims
about generated records, not complete production-executed or raster evidence.
The head remains preserved as first-rejection evidence.

## 4. Second rejected head: `cb684cc2`

The remediation added three named decision actions per identity and much broader
row-local lifecycle checks. Its manifest reported authoring, focused contract
and validation PASS. Those results are useful evidence, not acceptance.

Independent review found that the decision actions are appended to the same
generic task interaction and mostly target another generic `work_N` phase or a
generic terminal. Package declarations still use generic base zones such as
`left`, `right`, `center`, `exit_lane` and `work_0..5`, with an empty declared
route set. Thus a renamed decision does not necessarily select a different
physical station, entrance, stage lane, service path or aftermath route.

The generated aftermath function still rotates generic marker/actor/service
records across outcomes. It does not prove the exact memorial seating, repaired
or security-controlled fight space, service/table queue, cellar exit, darts
lane/bracket, stage/cable flow, mutually exclusive Dead Tuesday zones, legend
backstage path, creditor occupation, recording silence/audience route or
union/management entrances required by the Package C checklist.

The proof also does not establish, at package level:

- byte/semantic complete snapshots before/after every phase and branch;
- hidden-state observer parity and no private-field influence/leak;
- real production catalog/handler/renderer/semantic-inventory composition;
- the full hostile command, operation, fact, cleanup, reentry and finalization
  injection matrix with atomic rollback;
- real pointer, keyboard and controller traversal of every required action;
- measured boundary/frame/liveness performance; or
- actual raster captures, obstruction overlays and title/sign-free package
  contact sheets. Capture ids are not raster evidence.

The handoff's statement that raster captures/contact sheets remain assembly-owner
work conflicts with the common package checklist. Assembly owns cross-catalog
and combined visual products; the package still owes its own current captures
and package-level identifiability proof.

## 5. Fixed A -> B -> C order and sole assembly ownership

The shared-catalog intake order remains **A -> B -> C**, regardless of arrival
time or owner option. The preserved ordered lineage is
`codex/env06_7-assembly-ordered`:

1. `128eef8a` applies Package A;
2. `23f13459` applies Package B after A;
3. `5cad5aab` applies the old Package C payload after B;
4. later assembly-owned seam commits follow, currently through `d78b9004`.

This lineage is unreviewed and dependency-held; it records ordering, not Package
C acceptance. A selected Package C successor must replace/replay the C net
payload semantically at position C after accepted A and B. It must not substitute
the separate A-only `codex/env06_7-assembly` lineage or reorder C ahead of B.

The dedicated env06_7 assembly owner is sole writer to shared catalog
indexes/loaders, monolithic catalogs, combined dossier/report, cross-seam
fixtures, pixel canvas/shared renderer adapters, global uniqueness report and
cross-catalog contact sheets. Package and successor squads submit net-payload
and adapter requests. The Integrator reviews/gates/lands; it does not implement
package or assembly behavior. The assembly owner does not rewrite rejected
package content to make a gate green.

## 6. Option A: same-scope spatial redesign and exceptional review

Owner statement: retain the full row/common/Package C/assembly requirements and
authorize one exceptional post-second-rejection Package C redesign and review.

### Implementation consequence

Create a fresh Package C branch from the accepted env06_6 successor. Preserve
stable ids and semantically useful content, but redesign every identity around
production-resolvable spatial authority:

- Wake uses memorial tables, the closed ordinary bar route and specific patron
  handoff/seating targets.
- Fight Night uses moved furniture, confrontation/cleanup stations and distinct
  repaired, broken and security-controlled route states.
- Payday Rush uses order, settlement, patron, counter and table targets whose
  priority changes service access.
- Lock-In binds closed legal exit, quiet/cellar exit, inclusion and reopening to
  different routes/interactions and occupants.
- Darts League binds throw line, lanes, bracket rounds, officiation station and
  final arrangement.
- Live Band binds stage, speakers, cables, soundcheck and crowd-flow lanes to
  performance/failure/post-show states.
- Dead Tuesday gives bartender, lone patron and booth separate physical targets;
  one selection closes the other two for the night.
- Guest Legend binds instrument source, stage/table route, reveal point and
  backstage access.
- Rent Party binds donation/revenue stations, moved furniture and creditor/music
  occupation routes.
- Recording Night binds microphones, cables, silence zones, take/reset station
  and audience relocation.
- Union Trouble binds picket and management entrances, stopped stage work and
  side/mediation setup targets.

Each decision must execute against a different authenticated semantic target or
route where the checklist calls for one; command renaming and generic work zones
do not count. Every success/failure/refuse/interruption aftermath must apply
identity-specific spatial/actor/interactable/service/route operations and retain
a safe exit.

The Package C owner must produce package-level executable evidence for complete
snapshots, observer/hidden parity, production composition, hostile injection,
all three input paths, reduced motion, native/Web parity, save/revisit/expiry,
performance/liveness, and actual raster capture/contact-sheet/overlay matrices.
The assembly owner separately performs global 55-id uniqueness, composition and
cross-catalog visual work.

### Acceptance consequence

One immutable Package C head must pass independent semantic and visual review
against the full prompt and both package checklists. The reviewer must identify
all eleven variants and their terminal branches without titles/sign text and
execute the distinct targets/routes through production composition. Only one
exceptional review is authorized; another rejection returns to the owner.

Package C remains blocked from ordered assembly until accepted. A -> B -> C is
unchanged, and `depth06_1` remains blocked until the complete 55-id env06_7
assembly lands and passes its independent gate.

## 7. Option B: explicit package reshape with named successors

Owner statement: retain Package C as partial rejected evidence, split its two
open acceptance boundaries into named successor packages, and keep ordered
assembly and depth closure blocked.

Create:

1. `env06_7c1` (`Bar/Jazz Spatial Authority`) — owns all eleven
   identity-specific production zones, anchors, routes, interactions, branch
   topology and aftermath operations listed in Option A. It submits shared
   adapter/semantic-inventory requests to the sole assembly owner and provides
   executable spatial/route/cleanup/revisit proof.
2. `env06_7c2` (`Bar/Jazz Package Evidence`) — depends on accepted
   `env06_7c1` and owns complete snapshots, hidden observer parity, production
   composition, hostile injection matrix, pointer/keyboard/controller traversal,
   native/Web, performance/liveness and actual package raster/contact-sheet/
   obstruction evidence.

Original Package C becomes `PARTIAL - SUCCESSORS REQUIRED`, never accepted
intake. Useful records from `cb684cc2` may be replayed only through an exact
reviewed path/hunk manifest; neither successor may call the rejected tests or
capture ids proof.

`env06_7c2` produces the one immutable Package C intake head after composing
accepted `env06_7c1`. Only then may the sole assembly owner replace the old C
payload at position C on `codex/env06_7-assembly-ordered`. Package A and B remain
before C, and no successor writes shared assembly files.

The global `env06_7` row and `depth06_1` remain blocked on both successors, final
ordered five-package assembly, 55/55 completeness and all assembly gates. The
split starts successor review accounting but preserves both Package C rejection
findings as binding history.

## 8. Option C: generic-target and assembly-evidence design exception

Owner statement: reduce Package C acceptance to permit the known generic target
model and defer evidence that the package checklists currently require to the
assembly owner.

Record exception `ENV06_7-C-GENERIC-ASSEMBLY-EVIDENCE-01` with both allowances:

1. Identity-specific verbs/tags/outcome labels attached to one generic phase
   task may satisfy player-choice distinctness even when choices use common
   `work_N`/left/right/center targets, declare no production routes and terminate
   in generic marker/actor/service aftermaths.
2. Package C may hand off declared capture ids and synthetic row-local checks;
   complete semantic snapshots, hidden observer parity, production composition,
   hostile injection, real input traversal, measured performance and actual
   raster/contact-sheet evidence may be supplied only at final assembly.

### Acceptance and release consequence

The owner must amend the full row prompt, common package checklist, Package C
checklist, assembly checklist, board and `depth06_1` criteria. The product and
release evidence may not claim that all eleven Package C scenarios have distinct
physical targets/routes, branch-specific spatial aftermath, package-level
production proof or independently reviewed package rasters. Pairwise signature
uniqueness cannot be cited as proof of physical distinctness under this
exception.

The sole assembly owner, not Package C or the Integrator, assumes every deferred
proof obligation. If final production/raster evidence shows two variants are
not identifiable, an unsafe route, hidden leak, input failure or performance
red, assembly rejects; it may not rewrite Package C content. If the unamended
55-id or `depth06_1` gate still requires genuine unique sequences and package
evidence, those dependencies remain blocked or the owner must name a successor
restoring the waived scope.

Even under this exception, A -> B -> C order, sole assembly-file ownership,
stable 11-id inventory, accepted env06_6 dependency, safe exits, atomic money/
state behavior, save compatibility and full final assembly gates remain.

## 9. Owner record required to resume

The decision record must state exactly `A`, `B` or `C`, owner and timestamp. It
must additionally name:

- for A, the Package C redesign owner and single exceptional-review
  authorization;
- for B, successors `env06_7c1` and `env06_7c2`, their owners and all dependency
  holds; or
- for C, exception `ENV06_7-C-GENERIC-ASSEMBLY-EVIDENCE-01`, every amended
  checklist/gate/release claim and any restoration successor.

Until that record exists, `ca41e6a2`, `cb684cc2` and handoff `4aee8b9c` remain
frozen rejected evidence. No agent may infer a choice, Package C may not enter
ordered assembly as accepted intake, and the Integrator may not implement a
resolution.
