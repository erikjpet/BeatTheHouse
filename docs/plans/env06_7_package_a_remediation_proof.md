# env06_7 Package A — First-Rejection Remediation Proof

> Historical rejection-response evidence. The `040c0603` production recovery
> replaces its synthetic target inventory with sealed production composition;
> see `env06_7_package_a_production_recovery.md` for the current candidate.

This package-local manifest binds to the exact product head named by the
immutable handoff. It records executable evidence, not an acceptance verdict.
The package remains dependency-held on the accepted env06_6 successor.

## Public facts materially alter play

Each of the eleven newly authored identities subscribes to a registered public
fact envelope with an exact payload predicate. The fact projects one public
payload field into typed scenario state, sets an exactly-once path receipt, and
enters a scenario-specific public aftermath with its own physical marker and
service state. These are executable phase-graph routes, not tags or metadata.

- Street Craps consumes the accepted public craps vocabulary through the
  `game_result` scenario bridge. A `street_craps_disperse` result projects the
  positive recovered `bankroll_delta` to `stake_recovery_delta`, reaches the
  `street_stake_recovered` aftermath, and proves duplicate fact delivery cannot
  recover the stake twice.
- Cruiser Parked consumes active `sweep_changed` pressure for the Back Alley and
  enters the `cruiser_pressure_route` aftermath.
- The remaining Corner Store, Back Alley, and Pawn Shop identities consume
  exact service, sweep/security, heat/surveillance, town/rumor, inventory,
  provenance, fence, stock, and appraisal results with unique predicates,
  projected state fields, and aftermath identities.

Travel departure and the safe exit remain independent executable connections.
Delivery Day remains the accepted env06_6 reference implementation and is
preserved byte-for-byte by the generator.

## Executable exact-head matrix

`tools/env06_7_package_a_check.gd` replaces the rejected static checker. It
loads the production catalog and executes:

- all success, failure, ignore, refuse, arrival-interruption, and
  work-interruption routes for all eleven newly authored identities (66 route
  traces);
- all eleven public-fact routes, including exact payload projection, a single
  durable receipt, queued duplicate replay, and same-id/different-payload
  conflict rejection without mutation;
- save/load normalization before and after every command/fact branch;
- partial and terminal reentry plus night-end expiry, cleanup, and expired
  revisit;
- command receipt replay and same-id conflict rejection;
- hostile wrong-phase command rejection without partial state mutation;
- hidden-state observer checks and deterministic serialized platform projection
  parity;
- task/safe-exit independence, visible label/prompt/non-color state, 44-by-44
  minimum hit bounds, normal/reduced-motion transition parity, and all 143
  package-scoped arrival/work/outcome/revisit/small-screen/hit/obstruction
  evidence identities for the eleven new rows.

The checker also binds the exact 12-id inventory, validates all eleven new
schema-v2 definitions against the package target inventory, preserves the
accepted Delivery Day graph, and rejects duplicate exact or normalized
signatures.

## Expected sentinels

- `ENV06_7_PACKAGE_A_GENERATED count=12`
- `ENV06_7_PACKAGE_A_EXECUTABLE PASS ids=12 routes=66 public_facts=11`

The payload does not edit the shared catalog/index/loader, combined dossier,
cross-seam fixture, renderer, pixel canvas, runtime/schema, board, crew/world
models, main, gate artifacts, or owner files.
