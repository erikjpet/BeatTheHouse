# 0.5 UI Full Redesign Report

## Final-state summary

The overhaul keeps the neon-pixel identity and authoritative gameplay model while replacing legacy run text with a structured HUD, data-driven room headers, a single risk dock, content-fit popups, portrait conversation presentation, and spatial inventory/sale showcases. Focus and hover are previews; committed selection and arm/confirm states remain explicit. The implementation uses the shared token/widget system and the artist-swappable raster manifest.

## Phase findings

| Phase | Finding and completed work | Verification artifact |
| --- | --- | --- |
| A — system/art | No 0.5 token system, widget contract, manifest, or swappable UI art pipeline existed. Added them, including deterministic fallbacks and scripted deletion tests. | `.tmp/ui05_captures/phase_a/gates/` |
| B — HUD | Player state was compressed into legacy text. Added the wallet/delta, Grand Casino chips, segmented heat/drunk meters, pending-drink ghost, conditional status tray, and interactive time detail. | `.tmp/ui05_captures/phase_b/` |
| C — rooms | Room identity and options were generic text. Added unique title art and authoritative data-driven blurbs/options for all 18 archetypes. | `.tmp/ui05_captures/phase_c/` |
| D — risky actions | Cheats competed with ordinary actions. Consolidated all available risky choices in one contained, stateful dock with unchanged select/confirm semantics. | `.tmp/ui05_captures/phase_d/` |
| E — popups | Selection and map panels retained fixed dead space. Migrated them to common title/content/action anatomy and content-fit sizing, including small-screen constraints. | `.tmp/ui05_captures/phase_e/gates/ui/` |
| F — conversation | Dialogue had procedural presentation but no authored portrait pipeline or readable reveal behavior. Added portrait/name plates, click/touch skip, and reduce-motion instant reveal; the exact baseline determinism hash remains unchanged. | `.tmp/ui05_captures/phase_f/gates/` |
| G — remaining screens | Existing menu and inventory renovation supplied a strong base, but sale screens hid quote arithmetic. Added an authoritative price showcase, emphasized the correct main-menu primary action, audited settings/accessibility and every remaining UI file. | `.tmp/ui05_captures/final/` |

## Major-surface cold-look and capture index

The visual harness is headless, so its “captures” are serialized screen-state captures rather than raster screenshots. Each pair records visible text, controls, geometry, accessibility state, interaction state, and authoritative models. Before is `.tmp/ui05_captures/baseline/foundation_visual_qa_report.json`; after is `.tmp/ui05_captures/final/foundation_visual_qa_report.json`. Component-specific after captures are in `.tmp/ui05_captures/final/gates/ui/`.

| Major surface | Cold-look: what a first-time player should think this is | Before/after capture state |
| --- | --- | --- |
| Main menu | “A neon casino-crime run starts or continues here.” | `start_screen` / `start_screen` |
| HUD | “This is my money, danger, intoxication, time, and current conditions.” | `environment_screen` / `environment_screen` |
| Environment header | “This is the room I entered, why it matters, and what kinds of things are here.” | every route room state / every route room state plus the 18-title engine check |
| Cheat dock | “These are the available dirty plays and their attention risk.” | `risky_game_fixture_screen` / `risky_game_fixture_screen` |
| Selection popup | “I am inspecting one choice; confirming is a separate commitment.” | `event_screen` / `event_screen` plus popup-fit engine cases |
| Conversation | “This person is speaking now; I can reveal the line or answer.” | conversation-bearing event states / Sal and Cage conversation states plus Phase F component capture |
| Sale/pawn | “This is the exact item and the arithmetic behind the offer.” | `sal_inspected_float_breakdown` / same state plus merchant component capture |
| Settings | “These are grouped video, audio, interface, comfort, and accessibility controls.” | UI scene baseline / final UI component route |
| Spatial inventory | “These are physical containers and selectable storage spaces, not a list.” | baseline inventory component / final inventory spatial route |
| Bag reveal | “This reel opens the exact saved bag and reveals its contents.” | baseline bag component / final bag component route |
| World map | “These room nodes are travel choices with an inspect-before-confirm flow.” | travel route states / final world-map component route |
| Journal | “This is the run’s chronological record and current pressure context.” | UI scene baseline / final journal component route |
| Run report | “This is the completed run, with navigable summary and timeline tabs.” | terminal summary states / terminal summary states |
| Meta home | “This is persistent storage, loadout, housing, Sal, and collection access.” | Sal/home states / final meta route and Sal states |
| Collection | “These are exact persistent items, bags, floats, and trade/sale actions.” | Sal collection states / final meta component route |
| Coach bubble | “This is optional first-time guidance, not a blocking modal.” | UI component baseline / final coach component route |

## Complete `scripts/ui` accounting

“Support” rows do not directly render player-facing pixels; they were audited for the structured data, caching, accessibility, or deterministic behavior consumed by a renderer.

| File | Role / migration result | Cold-look note |
| --- | --- | --- |
| `scripts/ui/attribute_badge_row.gd` | Shared tokenized badge renderer retained. | Compact item attributes, not prose. |
| `scripts/ui/bag_open_reel.gd` | Spatial container language, reduce-motion, and exact reveal identity retained. | A physical bag is being opened. |
| `scripts/ui/bag_open_reel_view_model.gd` | Support; authoritative reveal adapter audited. | Supplies exact bag/reveal facts. |
| `scripts/ui/cage_atm_view_model.gd` | Support; structured Cage facts audited. | Supplies ATM choices and costs. |
| `scripts/ui/cage_counter_view_model.gd` | Support; structured Cage facts audited. | Supplies tier/counter facts. |
| `scripts/ui/cheat_dock.gd` | New shared risk dock. | Dirty options are visibly risky and conditional. |
| `scripts/ui/coach_overlay.gd` | Existing compact overlay retained with touch/accessibility checks. | Optional contextual help. |
| `scripts/ui/coach_view_model.gd` | Support; one-time guidance semantics retained. | Supplies short coaching copy. |
| `scripts/ui/drunk_distortion_overlay.gd` | Information-preserving reduce-motion path retained. | Intoxication affects presentation, not controls. |
| `scripts/ui/environment_header.gd` | New title/blurb/options component. | Establishes room identity at a glance. |
| `scripts/ui/environment_interaction_controller.gd` | Support; focus/commit routing audited. | Keeps inspection separate from action. |
| `scripts/ui/environment_interaction_view_model.gd` | Support; structured world-object language audited. | Supplies physical room affordances. |
| `scripts/ui/foundation_action_view_model.gd` | Support; action/risk semantics retained. | Supplies legal and risky choices. |
| `scripts/ui/foundation_hud_bar.gd` | New structured HUD. | Reads as the player’s live dashboard. |
| `scripts/ui/foundation_hud_view_model.gd` | Support; old snapshot fields retained alongside structured fields. | Supplies authoritative live state. |
| `scripts/ui/foundation_main.gd` | Integration migrated across HUD, rooms, dock, popups, menu emphasis, and overlays. | One coherent game shell. |
| `scripts/ui/foundation_screen_builder.gd` | Main menu/run shell audited; existing neon title and spatial canvas retained. | Establishes brand and primary route. |
| `scripts/ui/foundation_travel_view_model.gd` | Support; travel summaries retained. | Supplies route risk and destination facts. |
| `scripts/ui/foundation_widgets.gd` | Expanded common kit: variants, states, tabs, tooltips, and popup sizing. | Consistent controls everywhere. |
| `scripts/ui/game_surface_canvas.gd` | Physical table/machine canvas retained; dock extracted. | The game happens on the object itself. |
| `scripts/ui/icon_sprite_renderer.gd` | Support; missing-art behavior audited. | Supplies readable pixel icons. |
| `scripts/ui/inventory_container_catalog.gd` | Authoritative 3/5/7/10 presentation mapping retained. | Supplies the correct open container. |
| `scripts/ui/inventory_container_surface.gd` | Spatial selection/focus/confirm implementation retained. | Items occupy real selectable spaces. |
| `scripts/ui/item_found_popup.gd` | Compact item-art notification retained. | A specific item was just acquired. |
| `scripts/ui/meta_collection_view_model.gd` | Support; collection structure audited. | Supplies persistent collection facts. |
| `scripts/ui/meta_item_interaction_screen.gd` | Spatial meta interaction plus visible quote breakdown. | A persistent exact item is being handled. |
| `scripts/ui/meta_item_interaction_view_model.gd` | Carries exact identity/floats and authoritative quote details. | Supplies exact saved-item facts. |
| `scripts/ui/meta_session_controller.gd` | Support; persistent screen routing audited. | Preserves meta actions and saves. |
| `scripts/ui/music_arrangement_selector.gd` | Support; nonvisual adaptive-music selector. | No player-facing surface. |
| `scripts/ui/music_float_pcm_stream.gd` | Support; allocation-conscious audio stream. | No player-facing surface. |
| `scripts/ui/music_layer_choreography.gd` | Support; deterministic audio choreography. | No player-facing surface. |
| `scripts/ui/music_outcome_director_model.gd` | Support; outcome-to-music model. | No player-facing surface. |
| `scripts/ui/perf_telemetry_overlay.gd` | Developer-only telemetry retained. | Clearly diagnostic, never gameplay chrome. |
| `scripts/ui/performance_liveness_guard.gd` | Support; mandatory idle-animation counter retained. | Proves animated surfaces remain alive. |
| `scripts/ui/pixel_scene_canvas.gd` | Physical room objects and Sal’s six shelf items retained. | The room itself is the interaction surface. |
| `scripts/ui/procedural_music_player.gd` | Support; cached audio runtime. | No player-facing surface. |
| `scripts/ui/run_inventory_screen.gd` | Spatial inventory plus item showcase and price breakdown. | Inspect, move, equip, repair, pawn, redeem, or sell one exact item. |
| `scripts/ui/run_inventory_view_model.gd` | Support; all owned/unopened/container entries audited. | Supplies every reachable run item. |
| `scripts/ui/run_journal_view_model.gd` | Support; timeline grouping retained. | Supplies the run’s readable history. |
| `scripts/ui/run_report_screen.gd` | Existing tab precedent retained and token-audited. | Summary and timeline of a finished run. |
| `scripts/ui/run_report_timeline_canvas.gd` | Timeline visualization retained. | Shows how the run unfolded. |
| `scripts/ui/run_report_view_model.gd` | Support; authoritative report facts retained. | Supplies results without recomputing outcomes. |
| `scripts/ui/segmented_meter.gd` | New banded meter with ticks and ghost segment. | Thresholds are readable without prose. |
| `scripts/ui/settings_menu.gd` | Grouped settings, touch targets, scaling, contrast, and reduce-motion audited. | Comfort and accessibility controls. |
| `scripts/ui/sfx_player.gd` | Support; audio feedback routing. | No player-facing surface. |
| `scripts/ui/small_screen_policy.gd` | Support; common touch/scale policy. | Keeps the same information usable on small screens. |
| `scripts/ui/talk_dock.gd` | Portrait/name plate and deterministic typewriter migration. | A person is talking in the current room. |
| `scripts/ui/terminal_consequence_view_model.gd` | Support; result language retained. | Supplies clear outcome and pressure facts. |
| `scripts/ui/ui_art.gd` | Single cached art loading/fallback door. | Missing art remains readable. |
| `scripts/ui/visual_style.gd` | Canonical palette, role, spacing, type, size, and motion tokens. | One consistent neon-pixel language. |
| `scripts/ui/wager_confirmation_controller.gd` | Support; arm/confirm tokens retained. | Wagers cannot be committed by focus alone. |
| `scripts/ui/web_audio_bridge.gd` | Support; web-safe audio bridge. | No player-facing surface. |
| `scripts/ui/world_map_canvas.gd` | Physical node map retained with focus/selection distinction. | Destinations are places, not rows. |
| `scripts/ui/world_map_overlay_controller.gd` | Detail popup migrated to shared content-fit sizing. | Inspect a destination, then travel. |

## Authoritative behavior audit

- Item identity, prices, collection floats, effects, container capacities, sale/pawn/redeem/repair outcomes, arm/confirm tokens, saves, and RNG continue to come from the existing services. UI-only breakdown fields sum to the already-authoritative quote.
- Bag, Backpack, Suitcase, and Trunk use open-interior art and authoritative capacities of 3, 5, 7, and 10 selectable spaces.
- Every owned item and unopened bag remains reachable through spatial containers. Focus/hover never moves the container or commits the action.
- Inspect, active item, merchant, repair, pawn, redeem, placement, home transfer, meta loadout, bag reveal, Sal sale/buyback, and trade-up retain the shared container language.
- Sal’s six shelf items remain physical room objects.
- Existing snapshot fields and legacy run entry points remain available.
- No intentional deferrals remain. Future polish should be additive, not acceptance work.

## Final gates

Final results are recorded under `.tmp/ui05_captures/final/`. The completion commit is permitted only after validation, all supported FoundationSuite spellings, visual QA, performance/liveness, deterministic replay, token adoption, popup fit, asset deletion/fallback, manifest cross-check, raw-tag grep, and this 54-file coverage check are green.

