# env06_7 Package C — First-Rejection Remediation Proof

This package-local manifest binds to the exact product head named by the
immutable handoff. It records executable evidence, not an acceptance verdict.
The package remains dependency-held on the accepted env06_6 successor.

> Historical rejection-response evidence. The ordered production recovery
> removes generic `work_N` authority and synthetic inventory; see
> `env06_7_package_c_ordered_recovery.md` for the current candidate.

## Reachable identity-specific choices

All eleven Bar and Jazz Club identities expose a named three-way choice at an
identity-specific lifecycle phase. Each option is a live interaction command
with a distinct phase-graph edge; choices are not renamed tags on one shared
linear route.

- Dead Tuesday makes bartender company, lone-patron company, and the closed
  booth task mutually exclusive terminal commitments.
- Lock-In makes inclusion, the quiet cellar exit, and reopening distinct live
  routes.
- Union Trouble makes mediation, crossing the union line, and crossing the
  management line distinct live routes.
- Wake, Fight Night, Payday Rush, Darts League, Live Band, Guest Legend, Rent
  Party, and Recording Night likewise expose their checklist-specific decisions
  at their own work boundaries.

## Executable exact-head matrix

`scripts/tests/foundation/env06_7_package_c_contract.gd` executes:

- eleven complete success traces with save/load before and after every command;
- 33 identity-choice traces with exact authored-target and save/load proof;
- failure, refusal, and travel interruption at all 47 active phase positions
  (141 traces), including save/load immediately around the terminal branch;
- every scenario's subscribed public pressure fact at all 36 work-phase
  positions, including world-boundary progression grace;
- command receipt replay and same-id/different-command conflict rejection;
- hostile wrong-phase injection rejection without partial state mutation;
- partial, terminal, and expired reentry, night-end expiry, and cleanup for
  every identity;
- public-projection privacy, deterministic serialized native/Web/platform
  parity, production target-inventory validation, and bounded liveness;
- independent task and safe-exit targets, visible label/prompt/non-color state,
  44-by-44 minimum hit bounds, normal/reduced-motion transition parity, and all
  110 package-scoped arrival/partial/outcome/small-screen/hit/obstruction
  evidence identities.

The same proof binds the exact 11-id inventory, unique calculated signatures,
55 pairwise comparisons, exact operation receipts, and production catalog
ownership.

## Expected sentinels

- `ENV06_7_PACKAGE_C_AUTHOR_OK scenarios=11 pairs=55`
- `ENV06_7_PACKAGE_C_CONTRACT_OK scenarios=11 signatures=11`

The payload does not edit the shared catalog/index/loader, combined dossier,
cross-seam fixture, renderer, pixel canvas, runtime/schema, board, crew/world
models, main, gate artifacts, or owner files.
