# postfix06_2 placement harvest

Date: 2026-09-22
Baseline: `b7c51bf4749422c434e9689b02852084d66ac192`
SNAPSHOT: `78a62257745602c40ec804740ee9f95d789606db`
Replacement: `docs/todo/rw06_1_fixed_slot_rooms_prompt.md`

This report is the custody map for the unfinished postfix06_2 general room-placement solver. The SNAPSHOT commit is an ancestor of the deactivation commit and eventual `main` merge, so every item below remains recoverable with:

```text
git show 78a62257745602c40ec804740ee9f95d789606db:<path>
```

`KEEP` means the player-facing invariant or reusable test should be carried into the fixed-slot implementation. `ADAPT` means the idea is useful but its current implementation is coupled to runtime search and must be recast around authored slots. `DROP` means the runtime-search mechanism is intentionally not part of the fixed-slot design; it remains available only from SNAPSHOT/history.

## Placement inventory and disposition

| Piece | SNAPSHOT files and symbols | What the snapshot implementation does | Verdict for fixed slots | Recovery path(s) |
| --- | --- | --- | --- | --- |
| Safe-exit corridor reservation and reachability | `scripts/core/scenario_layout_resolver.gd`: `resolve()`, `_resolve_visual_queue()`, `_placement_queue_priority()`, `_validate_interactions()`, `_record_repack_request()`; `data/environments/developer_placement_overrides.json` | Places safe exits before lower-priority visuals, protects their approach/walk-lane geometry, checks normal and expanded reachability, and asks for a deterministic repack when late validation finds a blocked exit. | **ADAPT.** Preserve the invariant, but express it as authored `exit_slots`, static walk-lane reachability, and disjoint slot geometry. Do not port repack requests. | `scripts/core/scenario_layout_resolver.gd`; `data/environments/developer_placement_overrides.json` |
| Live base-control inclusion | `scripts/core/scenario_layout_resolver.gd`: `_base_occupied_records()`, `_context_base_occupied_records()`, `_validate_interactions()` | Includes generated games, services, events, lenders, items, travel controls, and context reservations when validating scenario authority. | **KEEP.** The fixed-slot validator must include every live base slot and every generated inventory/overflow action, not merely scenario objects. | `scripts/core/scenario_layout_resolver.gd` |
| Exclusive normal and 44px authority validator | `scripts/core/scenario_layout_resolver.gd`: `_validate_interactions()`, `_exclusive_hit_authority_overlaps()`, `_owns_exclusive_hit_authority()`, `_overlap_pairs()`; `scripts/tests/foundation/env06_8_environment_readability_contract.gd`: `_has_expanded_hit_authority_error()` | Validates scenario/scenario and scenario/base hit and label rectangles in normal and expanded small-screen modes and reports the exact conflicting identities. | **KEEP.** Port the authority checks to the static slot validator, including base slots and labels. The 44px rule remains a supported accessibility guarantee. | `scripts/core/scenario_layout_resolver.gd`; `scripts/tests/foundation/env06_8_environment_readability_contract.gd` |
| Renderer/authority geometry synchronization | `scripts/ui/pixel_scene_canvas.gd`: `_object_ids_at_local_position()`, `_object_has_action_authority()`, `_activate_object_at_local_position()`; `scripts/tests/foundation/scenario_semantic_presentation_contract.gd`: `_check_collision_adjusted_renderer_authority()`, `_check_same_identity_base_geometry_authority()` | Gives actionable controls stable activation precedence over inspect-only visuals and checks that renderer geometry and interaction authority refer to the same final rectangle. | **ADAPT.** Fixed slots should make overlap impossible, but renderer, focus, pointer, controller, and semantic authority still need a single slot geometry source. Port only the common geometry/activation contract after rw06_1 defines the slot UI. | `scripts/ui/pixel_scene_canvas.gd`; `scripts/tests/foundation/scenario_semantic_presentation_contract.gd` |
| Scenario visual deterministic repack/displacement | `scripts/core/scenario_layout_resolver.gd`: `_resolve_visual_queue()`, `_append_repack_exclusions()`, `_collision_safe_rect()`, `_crowded_passive_candidate()`, `_class_candidate_failure_diagnostics()` and repack helpers | Searches authored, supported, coarse, and fine candidates; displaces movable visuals after late conflicts; retains authored obstacles and developer locks. | **DROP.** Runtime free-form search is replaced by authored compatible slots and overflow actions. Preserve only the guarantees through fixed-slot tests. | `scripts/core/scenario_layout_resolver.gd` |
| Base-object transitive repack solver | `scripts/core/environment_instance.gd`: `_try_repack_base_conflict_cohort()`, `_base_layout_repack_*()`, `_solve_base_layout_repack_cohort()`, `_solve_base_layout_repack_clique()` and bitset/graph helpers | Builds candidate domains, computes compatibility graphs, solves transitive collision cohorts, pins fixed developer objects, relocates generated routes, and falls back through bounded search tiers. | **DROP.** This is the unfinished general solver the owner replaced. Generated base inventory receives authored base slots or an overflow entry in rw06_1. | `scripts/core/environment_instance.gd` |
| Lazy fine-grid candidate materialization | `scripts/core/environment_instance.gd`: `_cached_base_layout_repack_candidates()`, `_base_layout_repack_candidates_for_tier()`, `_base_layout_repack_candidates()` | Builds supported candidates first and materializes the full fine grid only if a cohort cannot be solved at cheaper tiers. | **DROP.** Fixed-slot binding has no fine-grid search. | `scripts/core/environment_instance.gd` |
| Compatibility/clique solver diagnostics and oracle | `scripts/core/environment_instance.gd`: `_base_layout_repack_candidate_compatibility_graph()`, clique bounds/arc-consistency/matching helpers; `tools/environment_grounding_contract.gd`: `_check_clique_solver_oracle()` and `_oracle_*()` | Uses exact graph search and an independent bounded oracle to prove candidate-domain solutions and diagnose dense-room failures. | **DROP.** The mechanism is irrelevant to deterministic slot binding. Its history remains useful if a future generalized editor solver is attempted. | `scripts/core/environment_instance.gd`; `tools/environment_grounding_contract.gd` |
| Deferred one-pass layout finalization | `scripts/core/environment_instance.gd`: `from_archetype(... defer_flat_generated_layout)`, `_hydrate_from_dict()`; `scripts/core/run_generator.gd`: deferred `from_archetype()` calls and post-injector finalization; `scripts/core/character_chain_model.gd`: materialized-layout guard | Defers generated geometry until scenario and character-chain injectors have produced the complete destination membership, avoiding repeated solver passes. | **ADAPT.** Slot binding should likewise happen once after membership is known, but must use the new slot binder rather than `ensure_generated_layout()` search. | `scripts/core/environment_instance.gd`; `scripts/core/run_generator.gd`; `scripts/core/character_chain_model.gd` |
| Initial-generation finalization receipt and atomic rollback | `scripts/core/run_generator.gd`: `_last_environment_install_receipt`, `_install_environment_with_rollback()`, `next_environment_result()`, receipt checks in travel/interior entry; `scripts/tests/foundation/harness_production_fidelity.gd`: receipt validation and `_material_layout_context()` | Proves that initial generation/travel installed the exact finalized destination, rejects stale/tampered receipts, and restores the exact pre-call run on invalid or unavailable targets. | **ADAPT.** Preserve atomic destination installation and an explicit slot-binding receipt, but reintroduce it only after rw06_1 defines the authoritative slot-layout context. | `scripts/core/run_generator.gd`; `scripts/tests/foundation/harness_production_fidelity.gd` |
| Scenario-prime cache lifecycle | `scripts/core/run_generator.gd`: `_prime_town_scenarios()` cached-definition short circuit; `tools/environment_grounding_contract.gd`: `_check_scenario_prime_cache_lifecycle()` | Avoids re-priming an unchanged scenario cycle and proves lifecycle invalidation. | **ADAPT.** This is a useful deterministic optimization, but it landed with the deferred solver pipeline and should be reconsidered independently after slot binding is stable. | `scripts/core/run_generator.gd`; `tools/environment_grounding_contract.gd` |
| Physical-support closed boundaries and independent stage bands | `scripts/core/environment_placement.gd`: `support_for_rect_on_surfaces()`, `_rect_has_point_closed()`; `data/environments/placement_surfaces.json` Motel semantic override | Treats exact surface endpoints as supported, lets authored stage bands be their own vertical authority, and classifies the weekly-rates social actor correctly. | **ADAPT.** Encode support class/semantic zone directly on each authored slot and use closed containment in the static validator. Retain the Motel actor classification when rw06_1 authors that room's slots. | `scripts/core/environment_placement.gd`; `data/environments/placement_surfaces.json` |
| Developer placement overrides for reported pairs | `data/environments/developer_placement_overrides.json`; `data/environments/placement_surfaces.json` | Adds or adjusts object positions/candidate surfaces used while debugging the reported seeds and dense compositions. | **DROP** as runtime overrides. Re-author any visually approved coordinates as ordinary fixed slot data; no pair-specific exception should survive. | `data/environments/developer_placement_overrides.json`; `data/environments/placement_surfaces.json` |
| Exhaustive 55/415/177 composition enumerator | `scripts/tests/foundation/postfix06_2_environment_composition_contract.gd`: `_run()`, `_legal_event_compositions()`, `_check_composition()`, `_check_opposite_pair_orders()`, offered-destination and safe-exit checks | Enumerates all 55 scenarios, 415 legal base-event memberships, 177 order inversions, both offered branches, destination installation, support, actionability, normal/expanded authority, and safe exits through production generation. | **KEEP/ADAPT.** Reuse its scenario and legal-host enumeration as input to the fast static slot validator. Remove its expensive runtime placement/search assertions and order-inversion solver checks. | `scripts/tests/foundation/postfix06_2_environment_composition_contract.gd` |
| Historical exact-seed replay | `tools/postfix06_2_environment_exact_seed_contract_test.ps1`; `tools/environment_generation_audit.gd`; `tools/environment_generation_audit.ps1` | Replays all 22 retained failing seeds for six visits and records per-run evidence; supports fresh seeded audits with explicit route/survival custody. | **KEEP/ADAPT.** Preserve all seed IDs as fixed-slot regression fixtures. Run them through the new slot binding and normal room audit, not through solver-specific receipt/repack expectations. | `tools/postfix06_2_environment_exact_seed_contract_test.ps1`; `tools/environment_generation_audit.gd`; `tools/environment_generation_audit.ps1` |
| Solver-derived fixture goldens and recapture provenance | `scripts/tests/ui_scene/compile_components_and_main_flow.gd`; `scripts/tests/fixtures/crew06_5_ignored_run_baseline.json`; `tools/fixsweep06_1_update_crew_golden.gd` | Pins `layout-v13` and environment/world-map/run hashes captured from the superseded solver and records the solver-era recapture path. | **ADAPT.** Keep the golden mechanism, discard solver-derived hashes and provenance now, and rebaseline only after rw06_1 fixed slots stabilize. | `scripts/tests/ui_scene/compile_components_and_main_flow.gd`; `scripts/tests/fixtures/crew06_5_ignored_run_baseline.json`; `tools/fixsweep06_1_update_crew_golden.gd` |
| Grounding/readability/semantic focused contracts | `tools/environment_grounding_contract.gd`; `tools/scenario_room_multiseed_finalization.gd`; `scripts/tests/foundation/env06_8_environment_readability_contract.gd`; `scripts/tests/foundation/scenario_semantic_presentation_contract.gd`; `scripts/tests/foundation/harness_production_fidelity.gd` | Adds focused tests for base/scenario authority, support edges, developer locks, route blockers, lazy candidates, deferred finalization, initial receipts, same-identity geometry, and multiseed support/actionability. | **ADAPT.** Retain player-facing guarantees and the multiseed census; remove solver-mechanism assertions. rw06_1 should replace them with slot-schema, slot-binding, overflow, focus, reachability, save/revisit, and hidden-state tests. | `tools/environment_grounding_contract.gd`; `tools/scenario_room_multiseed_finalization.gd`; `scripts/tests/foundation/env06_8_environment_readability_contract.gd`; `scripts/tests/foundation/scenario_semantic_presentation_contract.gd`; `scripts/tests/foundation/harness_production_fidelity.gd` |
| Serialized multiseed execution | `tools/scenario_room_multiseed_finalization.gd`: no-argument `_run()` iterates `SEED_FAMILIES` in-process; snapshot deletes `_run_parallel_seed_families()`, `_run_seed_family_process()` and `_parse_worker_summary()` | Prevents a single audit from spawning eight concurrent project Godot processes. | **KEEP.** This is process safety, not a solver mechanic. The deactivation keeps serial dispatch while dropping physical-support/generated-layout assertions tied to the old solver. | `tools/scenario_room_multiseed_finalization.gd` |
| Static content pin used to stabilize a placement fixture | `scripts/tests/foundation/check_core_content.gd`: `_check_connected_town_foundation()` / `_town_force_condition()` custom Bar Fight Night pin | Pins a scenario so the connected-town fixture exercises a known placement path. | **DROP** from this fixture when deactivating the solver. Historical failing seeds move to dedicated rw06_1 fixtures instead of changing an unrelated foundation census. | `scripts/tests/foundation/check_core_content.gd` |
| Standalone manifest registration for the exhaustive contract | `tools/health06_1_standalone_contract_manifest.json`: `postfix06_2_environment_composition_contract.gd` | Makes the new exhaustive runtime composition script an expected standalone contract. | **DROP** now. rw06_1 should register its replacement static slot contract in the reviewed manifest. | `tools/health06_1_standalone_contract_manifest.json` |

## Deactivation file set

The handoff commit returns the following production placement paths exactly to their `b7c51bf4` content: `scripts/core/scenario_layout_resolver.gd`, `scripts/core/environment_instance.gd`, `scripts/ui/pixel_scene_canvas.gd`, `data/environments/placement_surfaces.json`, `data/environments/developer_placement_overrides.json`, `scripts/core/environment_placement.gd`, `scripts/core/character_chain_model.gd`, and the placement-only changes in `scripts/core/run_generator.gd`.

The placement-only test/tool changes are likewise returned to baseline, the three solver-derived fixture/golden files are restored to their baseline hashes, and the snapshot-only exhaustive/exact-seed files are removed from the active tree. This avoids adding a new `Contract` failure while preserving every artifact at the recovery paths above.

## Mixed-file hunk-to-row map

Line anchors below use stable symbols because line numbers drift. A row omitted from a mixed file does not own any hunk in that file.

| Mixed file | Hunk/symbol ownership |
| --- | --- |
| `scripts/core/run_state.gd` | `service_hook_status(... reserved_bankroll_credit)` = GP-PF-001; `pit_boss_watch_status()` and `_begin_rourke_escort()` count formatting = GP-PF-005. |
| `scripts/core/game_module.gd` | detached candidate/terminal settlement guards = GP-PF-002; structured settlement currency and venue identity = GP-PF-004; singular/duplicate-safe chip rendering seam = AIF-004. |
| `scripts/ui/player_text.gd` | sealed-provider/neutral strings = GP-PF-003; structured cash/chip settlement rewriting = GP-PF-004; count-aware chip grammar and de-duplication = AIF-004. |
| `scripts/ui/foundation_main.gd` | sealed-action wording/recovery = GP-PF-003; courier/status count formatting = GP-PF-005; World Map scope = UIENV-PF-002; shared modal scope and overlay hooks = AIF-001; Run Menu responsive/scroll/focus layout = AIF-002; Journal responsive body/cards/focus = AIF-003; autosave generation/backoff/status functions = RP-007. No placement hunk survives or needs removal here. |
| `scripts/tests/fixsweep06_1_accessibility_contract.gd` | World Map dynamic focus ring = UIENV-PF-002; reusable modal containment/restoration = AIF-001; 24-configuration Run Menu matrix = AIF-002; 24-configuration Journal matrix = AIF-003. |
| `scripts/ui/modal_focus_scope.gd`; `scripts/ui/world_map_overlay_controller.gd` | World Map dynamic containment/restoration = UIENV-PF-002; shared reusable topmost-modal focus scope = AIF-001. |
| `scripts/tests/postfix06_2_gameplay_presentation_content_contract.gd` | provider-neutral sealed actions = GP-PF-003; seven-game structured currency/live+restore surfaces = GP-PF-004; timer count consumers = GP-PF-005; chip grammar = AIF-004; nested production item references = CR-PF-001. |
| `scripts/tests/foundation/check_items_events_world.gd` | real-environment pawn helper and atomic Cage/pawn/ticket/cash settlement assertions = GP-PF-002; talk/dialogue authority changes are retained non-placement contract hardening. CR-PF-001's nested catalog validation lives in `scripts/core/content_library.gd` and the dedicated gameplay/presentation/content contract. |
| `scripts/core/run_action_service.gd` | reserved quote/service-hook handling = GP-PF-001; detached service/lender/Cage/pawn/ticket/free-action publication and rollback = GP-PF-002. |
| `scripts/tests/postfix06_2_transaction_atomicity_contract.gd`; `scripts/tests/postfix06_2_fact_run_state.gd` | Shared exhaustive transaction descriptors and the fact-state seam jointly cover GP-PF-001 and GP-PF-002. |
| `scripts/tests/health06_1_durable_store_contract.gd` | transactional primary/backup/temp profile installation = RP-005; settings schema/default/migration preservation = RP-008. |
| `scripts/tests/postfix06_2_reliability_contract.gd` | shared feature-PCM accounting/lifecycle coverage = RP-006; bounded autosave retry and surfaced recovery = RP-007. |
| `scripts/core/run_generator.gd` | every SNAPSHOT delta (`CharacterChainModelScript`, receipt state, deferred `from_archetype`, `next_environment_result()`, post-injector reconciliation, scenario-prime cache) belongs to UIENV-PF-003 through UIENV-PF-009 and is deactivated. RP-PF rows do not depend on these hunks. |
| `scripts/core/character_chain_model.gd` | materialized-layout membership guard belongs to UIENV-PF-003 through UIENV-PF-009; deactivated. |
| `scripts/core/environment_placement.gd` | closed support endpoints/stage-band authority belongs to UIENV-PF-003 through UIENV-PF-009; deactivated and harvested for rw06_1 static slot validation. |
| `scripts/tests/foundation/harness_production_fidelity.gd` | finalized-install receipt reuse/material-context validation belongs to UIENV-PF-003 through UIENV-PF-009; deactivated. |
| `scripts/tests/foundation/env06_8_environment_readability_contract.gd` | expanded/base authority and read-only composition additions belong to UIENV-PF-009; deactivated, guarantee retained for rw06_1. |
| `scripts/tests/foundation/scenario_semantic_presentation_contract.gd` | renderer/authority sync, strict-fine ordering, label repack, same-identity geometry additions belong to UIENV-PF-003 through UIENV-PF-009 (principally UIENV-PF-009); deactivated. |
| `scripts/tests/foundation/check_core_content.gd` | custom Bar Fight Night pin belongs to UIENV-PF-003; deactivated. |
| `scripts/tests/ui_scene/compile_components_and_main_flow.gd`; `scripts/tests/fixtures/crew06_5_ignored_run_baseline.json`; `tools/fixsweep06_1_update_crew_golden.gd` | solver-derived `layout-v13`, environment/world-map/run hashes, and recapture provenance belong to UIENV-PF-003 through UIENV-PF-009; restored to `b7c51bf4` pending rw06_1 rebaseline. |
| `tools/health06_1_standalone_contract_manifest.json` | reviewed discovery/classification is RP-003; only the `postfix06_2_environment_composition_contract.gd` member is placement and is removed. |
| `tools/health06_1_standalone_contracts_contract_test.ps1` | caller/stage-derived timeout custody and qualification routing = RP-001; reviewed manifest discovery/classification = RP-003. |
| `tools/check_godot.ps1` | repository-root/routing/stage-custody changes are RP-001; feature PCM contract registration is RP-006. These survive; none is a placement hunk. |
| `scripts/ui/perf_telemetry_overlay.gd`, `scripts/ui/procedural_music_player.gd`, `scripts/ui/web_audio_bridge.gd`, `tools/foundation_soak_probe.gd` | feature PCM accounting, eviction, telemetry, and exercise = RP-006. |
| `tools/export_itch.ps1`, `tools/export_itch_helpers.ps1`, `tools/fixsweep06_1_packaging_contract_test.ps1` | native tuple/transactional stage work = RP-010; PCK custom-container checks where wired = RP-011; typed Web child exit, audit/custody/registration order, retry = RP-012; mobile distribution identity assertions in the packaging contract = RP-009. |

All other changed files have a single ledger family or are evidence/fixtures whose ownership is explicit in the ledger.

## True defect status at handoff

These statuses describe the retained branch after solver deactivation. `FIXED` means focused failure-first coverage passed; it does not claim the superseded full postfix campaign ran. `SUPERSEDED -> rw06_1` means the snapshot experiment is preserved but is not an accepted fix.

| ID | True status | Handoff disposition |
| --- | --- | --- |
| GP-PF-001 | FIXED | Keep paid-service quote/revalidation repair and 240-cell plus descriptor transaction coverage. |
| GP-PF-002 | FIXED | Keep atomic detached-candidate settlement for all boundary-bearing actions. |
| GP-PF-003 | FIXED | Keep provider-aware/neutral sealed-action language. |
| GP-PF-004 | FIXED | Keep structured venue-authoritative settlement currency. |
| GP-PF-005 | FIXED | Keep shared action-count formatting. |
| UIENV-PF-001 | FIXED | Keep capture cleanup/lifecycle ownership separation. |
| UIENV-PF-002 | FIXED | Keep dynamic World Map modal focus scope. |
| UIENV-PF-003 | SUPERSEDED -> rw06_1 | Safe-exit invariant ports to authored exit slots and static reachability. |
| UIENV-PF-004 | SUPERSEDED -> rw06_1 | Road Crew/base-door separation ports to compatible disjoint slots. |
| UIENV-PF-005 | SUPERSEDED -> rw06_1 | Dead Shift/staff separation ports to disjoint slot authority. |
| UIENV-PF-006 | SUPERSEDED -> rw06_1 | Dead Shift/discount separation ports to disjoint slot authority. |
| UIENV-PF-007 | SUPERSEDED -> rw06_1 | Count Cage authority ports to one slot authority or overflow action. |
| UIENV-PF-008 | SUPERSEDED -> rw06_1 | Count Cage/base-event composition ports to slot capacity/overflow. |
| UIENV-PF-009 | SUPERSEDED -> rw06_1 | Normal/44px/base/label authority becomes a static slot-schema gate. |
| AIF-001 | FIXED | Keep reusable topmost-modal focus-scope controller. |
| AIF-002 | FIXED | Keep viewport-safe Run Menu and bounded scrolling. |
| AIF-003 | FIXED | Keep authoritative responsive Journal and focus-visible body scrolling. |
| AIF-004 | FIXED | Keep singular chip grammar and compatibility de-duplication. |
| CR-PF-001 | FIXED | Keep removal of stale Craps ID and nested catalog-reference validation. |
| RP-001 | FIXED | Keep repository-root resolver and maintained qualification routing/custody. |
| RP-002 | FIXED | Keep 32 archived UID companions, exact pairing/set contract, canonical line-ending hash validation, and manifest-bound validator allowance. |
| RP-003 | FIXED | Keep reviewed standalone manifest; remove only the superseded composition member. |
| RP-004 | FIXED | Keep delegated `GameModuleRegistry` startup/cache contract. |
| RP-005 | FIXED | Keep transactional primary/backup/temp durable-store installation. |
| RP-006 | FIXED (production); packaged proof -> rw06_4 | Keep shared 64 MiB feature PCM accounting, deduplication, eviction, cleanup, telemetry, and focused contracts. The exact Windows/Web package proof is a release gate, not a reason to discard the production fix. |
| RP-007 | FIXED | Keep bounded permanent/transient autosave retry and recovery status. |
| RP-008 | FIXED | Keep settings schema validation, preservation, defaults, and migration. |
| RP-009 | FIXED | Keep distribution identity on all runnable presets; packaged mobile qualification is deferred to 0.6.1. |
| RP-010 | FIXED | Keep exact native DLL tuple selection and transactional stage behavior. |
| RP-011 | FIXED | Keep `.bthpcm` allowlist plus positive/negative custom-container fixtures. |
| RP-012 | FIXED | Keep typed Web export exit, noisy-output handling, audit/custody/registration, and retry behavior. |

## Qualification handoff

The owner replaced the full postfix06_2 campaign with the release-week gates. The active ledger records the same routing in detail. In summary:

- rw06_pre completed repository validation, all 10 Smoke stages, all six retained focused Godot contracts, all 14 focused static contracts, and the baseline/head Contract comparison. Baseline `b7c51bf4` had five failing stages (`validate_project`, `godot_import`, `standalone_contract_env06_6_full_contract`, `standalone_contract_game06_2_repeated_reprieve_contract`, and `foundation_contracts`); the deactivated branch head had only the same inherited repeated-reprieve failure (`fingerprint= heat=-1`), no head-only failure, and 18/18 passing foundation shards.
- rw06_1 owns every environment placement, historical seed, legal-composition, expanded authority, and room reachability row.
- rw06_4 owns the frozen-candidate validator, Smoke, zero-failure Contract suite, 37 v0.5.1 migrations, one feature-heavy soak, idle liveness, Windows/Web exports, PCK audits, diagnostic launches, and packaged RP-006 PCM proof.
- The three-soak aggregate, explicit mid-0.6 migration mode, mobile packaged qualification, and larger platform/performance campaign move to `docs/plans/0.6.1_backlog.md`.
