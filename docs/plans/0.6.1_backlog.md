# 0.6.1 Backlog: everything deliberately deferred from 0.6.0

Created 2026-09-22 when the owner cut 0.6.0 to the release-week plan
(`docs/todo/README_0_6_release_week.md`). Nothing here is cancelled. Each entry
names its source, so a future prompt can pick it up without rediscovering it.
Release-week rows append new entries under the matching heading.

## Quality gates moved out of 0.6.0

- **Three 180-minute feature-music soaks (A/B/C) plus aggregate liveness.**
  0.6.0 runs one soak.
  Source: `docs/todone/postfix06_2_post_fix_remediation_ledger.md`, section "Full
  qualification campaign".
- **Mid-0.6 save migration** (3 fixtures, explicit
  `--fixture-class=mid_0_6` mode). Only development saves are affected.
  Source: same ledger.
- **Binding performance and platform qualification (perf06_1 Phase 4)**:
  low-end matrix, quiescent witnessed host.
  Source: `docs/todo/deferred_0_6_1/perf06_1_*`.
- **Full playtest gate refresh (playtest06_2)**, replaced this week by the
  three-ending routes.
  Source: `docs/todone/playtest06_2_playtest_gate_refresh_prompt.md`.
- **Binding cross-economy audit**: 8 playstyles × 64 seeds × 208 actions, plus
  600k Coin Pusher EV.
  Source: `docs/todo/deferred_0_6_1/balance06_1_follow_on_*`.
- **Full post-playtest tuning (balance06_2)** beyond the 1-day route tuning.
  Source: `docs/todone/balance06_2_post_playtest_tuning_prompt.md`.
- **Mobile (Android/iOS)**: signing and team credentials, and packaged
  qualification.

## Rooms and placement

- The **solver residue**: every harvest item marked DROP. It is preserved in
  `main`'s history at the postfix06_2 SNAPSHOT commit, recorded with its
  reason in
  `docs/plans/rw06_1_placement_harvest_disposition.md`.
- **More slots per room, per-scenario custom layouts, slot animations**
  (modular schema from rw06_1).
- **Animate scenario `set_position` receipts between fixed slots.** The 0.6.0
  release-week layout snaps actors to the next phase's authored slot instead of
  reserving the prior slot and proving a body-swept walk. Actions, phase state,
  exact target placement, exits, overlap checks, reachability and hidden-state
  rules remain unchanged. Restore optional walks for these exact scenarios:
  `corner_store_delivery_day`, `corner_store_inventory_night`,
  `corner_store_lotto_fever`, `corner_store_dead_shift`,
  `corner_store_aftermath`, `back_alley_fence_night`,
  `back_alley_street_craps`, `motel_conventioneers`, `motel_stakeout`,
  `motel_wedding_overflow`, `bar_darts_league_night`, `bar_fight_night`,
  `bar_live_band`, `bar_lock_in`, `bar_payday_rush`, `bar_wake`,
  `gas_station_graveyard_shift`, `gas_station_tour_bus_stop`,
  `punchline_bringer_show`, `punchline_debt_court`,
  `punchline_headliner_night`, `punchline_open_mic_night`,
  `punchline_raid_jitters`, `punchline_greased_week`,
  `punchline_high_stakes_night`, `punchline_new_muscle`,
  `jazz_club_guest_legend`, `jazz_club_recording_night`,
  `jazz_club_rent_party`, `jazz_club_union_trouble`,
  `kitty_cat_lounge_amateur_night`, `kitty_cat_lounge_bachelorette_storm`,
  `kitty_cat_lounge_buyout`, `kitty_cat_lounge_slow_night`,
  `delta_queen_captains_invitational`, `delta_queen_engine_trouble`,
  `delta_queen_fog_delay`, `delta_queen_wedding_charter`,
  `delta_queen_whale_aboard`, `pawn_shop_estate_lot_day`,
  `pawn_shop_serial_check_day`, `pawn_shop_sals_mood`,
  `grand_casino_audit_night`, `grand_casino_convention_crowd`, and
  `grand_casino_gala_night`. Source: Q-005's owner timebox.
- **Restore optional Bar base-room density after the 0.6.0 fixed-slot gate.**
  To preserve the full active-scenario slot demand, two globally disjoint safe
  exits, and 104x76 expanded collision authority, release-week authoring keeps
  one Bar floor-fixture base slot, one surface-item base slot, one randomized
  seated-person base slot, and no randomized standing-person base slot. This is
  the smallest proven trade: five optional base slots move to authenticated
  overflow, while all active scenario occupants remain in the room.
  Corner Store and Grand Casino required no analogous base-cap reduction.
  Source: Q-005's non-fitting-object/landing timebox.
- **Restore two optional Back Alley base-room details after the 0.6.0 fixed-slot
  gate.** Release-week authoring moves only `base.ground_marker.01` and
  `base.standing_person.01` to authenticated overflow. The retained fixed plane
  has nine base slots, six stage slots, two safe exits, and the authored Bar
  route; every other class keeps its proven ship cap. The ten-base-slot search
  hit the bounded 10,000-node watchdog (`SEARCH_LIMIT`), which is not evidence
  of infeasibility or `UNSAT`. Source: Q-005's non-fitting-object/landing
  timebox.
- **Restore five optional Gas Station Casino base-room details after the 0.6.0
  fixed-slot gate.** Release-week authoring moves
  `base.behind_counter_person.01`, `base.floor_fixture.03`,
  `base.ground_marker.01`, `base.standing_person.01`, and
  `base.surface_item.03` to authenticated overflow. The retained fixed plane
  has eight base slots: both doorway slots, two floor-fixture slots, two
  surface-item slots, and both wall-mounted slots. It also keeps all 17 active
  scenario slots and both globally disjoint safe exits. Every four-slot
  reduction was proven infeasible; the five-slot candidate retaining the
  behind-counter person reached the bounded diagnostic watchdog and is not
  claimed infeasible. Source: Q-005's non-fitting-object/landing timebox.
- **Restore two optional Small Underground Casino floor details after the 0.6.0
  fixed-slot gate.** On the casino layer, release-week authoring moves only
  `base.group.01` and `base.surface_item.03` to authenticated overflow. The
  retained fixed plane has nine base slots, all ten active-scenario stage
  slots, and both globally disjoint safe exits; all active and aftermath
  scenario occupants remain in-room with zero scenario overflow. Of the exact
  one- and two-slot reductions tested, the group-plus-third-surface-item pair
  was the least destructive fit. Source: Q-005's non-fitting-object/landing
  timebox.
- **Bring four Motel aftermath details into the fixed room plane.** The 0.6.0
  geometry fits every active Motel composition with no overflow, but four
  persistent outcome-only records use authenticated overflow:
  `motel_conventioneers_aftermath_service_wing_open_secondary`,
  `motel_conventioneers_aftermath_actor_crowd_jammed`,
  `motel_stakeout_aftermath_observers_misdirected`, and
  `motel_stakeout_aftermath_stakeout_interrupted`. A complete-state authoring
  search can fit them only by removing three optional randomized base slots;
  retain the base-room detail for 0.6.0 and revisit the layout after release.
  Source: Q-005's non-fitting-object/landing timebox.
- **Restore two optional Kitty Cat Lounge base-room details after the 0.6.0
  fixed-slot gate.** Release-week authoring moves
  `base.standing_person.01` and `base.surface_item.03` to authenticated base
  overflow. The retained fixed plane keeps nine base slots, all ten scenario
  stage slots, and both globally disjoint safe exits; every active and
  aftermath scenario visual remains in-room with zero scenario overflow. An
  exhaustive complete-state search proved the full cap and every one-slot
  reduction infeasible; standing-person plus third-surface-item was the least
  destructive exact two-slot reduction because it preserves counter staff,
  seated and group capacity, the game fixture, wall detail, both doorways, and
  two surface positions. Source: Q-005's non-fitting-object/landing timebox.
- **Restore the two optional Beach service positions after the 0.6.0 fixed-slot
  gate.** Release-week authoring moves `service_spots:0` and
  `service_spots:1` (the two base surface-item records) to authenticated base
  overflow. The retained fixed plane keeps the game floor fixture and both
  globally disjoint base doorways, plus all 13 scenario stage slots and both
  safe exits; every active and aftermath scenario visual remains in-room with
  zero scenario overflow. An exhaustive complete-state search over all 18
  relevant cap vectors proved the full cap and every one-slot reduction
  infeasible, making this the unique minimal exact reduction. Source: Q-005's
  non-fitting-object/landing timebox.
- **Restore Delta Queen scenario actor movement after the 0.6.0 fixed-slot
  gate.** The hand-authored release layout keeps the captain scorer,
  eliminated-player group, deck guard, fog crowd/officer, best man, and
  entourage runner stationary. Removing the three optional route endpoint
  pairs makes all active and aftermath compositions fit the boat's tables,
  rails, gangways, and deck with zero scenario overflow and collision-safe
  labels. Source: Q-005's explicit stationary-actor timebox.
- The **fixture/kind/facing placement vision**:
  `docs/plans/environment_fixture_placement_plan.md`, if present in history.
- **env06_9**: visual consequences and object identity (127 observable-
  consequence failures across 53/55 scenarios, generic unlabeled glyphs).
  Source: `docs/todone/env06_9_visual_consequence_and_object_identity_prompt.md`.
- Agent-sweep bugs **BUG-10/34/37**, held for placement work. Recheck them
  against fixed slots.

## Endings and experience

- Parts of endings narrowed by rw06_2 (appended by that row).
- Experience-log items that were not arc breakers (appended by rw06_2).
- Owner run notes that were not blockers (appended from the scoreboard).

## Teaching, voice, polish

- **Tutorial TUT-N17**: five-person cold-player human gate.
  Source: `docs/todo/deferred_0_6_1/tutorial_first_time_player_completion_prompt.md`.
- **Voice pass (voice06_1)**.
- **Post-playtest cleanup (cleanup06_1)** and **triage (triage06_1)**.
- **Second-half polish program (polish06_0 outputs)**.
  Source: `docs/todone/polish06_0_post_playtest_program_prompt.md`, its
  execution record.
- Trailer production: `docs/todo/release06_1_trailer_production_template.md`.

## Performance risks noted, not yet proven

- Continuous allocation in the Bar Dice, Video Poker and Buffalo Slots render
  paths. The 9/21 playtest flagged it without comparative profiling.
  Source: `reports/playtest_2026-09-21/gameplay_progression_postfix.md`.

## Meta and platform features

- Collection browser sort and filter.
- Steam Inventory and community-market integration.
