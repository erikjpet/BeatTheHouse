# env06_7 Package D — First-Rejection Remediation Proof

This manifest is package-local executable evidence. It binds to the exact
product head named by `env06_7_package_d_immutable_handoff.md`; it does not
record an acceptance verdict and does not waive the dependency hold on the
accepted env06_6 successor.

## Identity-specific topology

All 12 scenarios expose a named three-way decision at an identity-specific
lifecycle phase. The executable contract follows every option and proves its
authored target. Examples include Open Mic manage/support/sabotage, Headliner
honor/escort/divert, Bringer seat/defend/hostile pitch, Debt Court
uphold/contest/adjourn, Raid Jitters hide/abort/reopen, and Lounge-specific
bracket, buyout, zone, and storm decisions. The topology is encoded in the
phase graph, interaction actions, branch ids, command authority, dossiers, and
calculated signatures—not only in labels or tags.

## Executed lifecycle matrix

`scripts/tests/foundation/env06_7_package_d_contract.gd` executes:

- 12 complete success traces, with exact idempotent command replay and a JSON
  save/normalize round trip after every physical command;
- 36 identity-decision traces (three per scenario), with save/load immediately
  before and after each decision;
- failure, refusal, and travel-interruption at every one of the 56 active phase
  positions (168 traces), with save/load immediately before and after each;
- every subscribed scenario-specific pressure fact at all 44 work-phase
  positions, including world-boundary progression grace, with save/load before
  and after the branch;
- partial, terminal, and expired revisit policies for every scenario;
- night-end expiry, complete semantic cleanup, and expired reentry for every
  scenario;
- command receipt mismatch rejection, malformed command/operation authority
  rejection, and malformed fact injection rejection without state mutation;
- public projection privacy and deterministic serialized platform projection
  parity for every scenario.

## Package-scoped presentation evidence

Every scenario binds ten deterministic capture identities: arrival, partial,
success, failure, refused, interrupted, reduced-motion, small-screen,
hit-overlay, and obstruction. The contract executes the runtime presentation
projection and asserts:

- at least one live task target and one independent safe exit;
- non-empty visible label, prompt, and non-color state for every target;
- minimum 44-by-44 hit bounds for every live interaction;
- equal transition counts and semantic state under normal and reduced motion;
- exact presence of all 120 package capture identities.

The package does not edit the shared renderer, pixel canvas, catalog, combined
dossiers, or cross-seam fixtures. Assembly remains responsible for combining
these package-bound identities into the ordered A → B → C shared catalog and
for any combined contact sheet; Package D acceptance no longer depends on an
unexecuted success-only proof.

## Exact commands

```powershell
& $godot --headless --path . --script res://tools/env06_7_package_d_author.gd
& $godot --headless --path . --script res://scripts/tests/foundation/env06_7_package_d_contract.gd
```

Expected sentinels:

- `ENV06_7_PACKAGE_D_AUTHOR_OK scenarios=12 pairs=66`
- `ENV06_7_PACKAGE_D_CONTRACT_OK scenarios=12 signatures=12`
