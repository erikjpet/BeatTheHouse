# env06_7 Package E — Completion Proof

Historical note: runtime and raster claims in this proof bind the recovered
Package E evidence. Ordered production-composition status is recorded in
`env06_7_package_e_ordered_recovery.md` and awaits Warden verification.

This package-local manifest binds the Delta Queen and Grand Casino public
scenario package to the exact product head named by its immutable handoff. It
records executable evidence, not an acceptance verdict. The package remains
dependency-held on an accepted env06_6 successor.

## Frozen identity inventory

Package E owns exactly eight scenarios:

- Delta Queen: Wedding Charter, Whale Aboard, Fog Delay, Engine Trouble, and
  Captain's Invitational.
- Grand Casino: Gala Night, Convention Crowd, and Audit Night.

Every identity has a frozen three-way live decision with distinct graph edges,
not renamed labels on a shared linear route. The executable fixture binds each
scenario's exact decision phase, commands, mechanic tags, and four aftermath
identities. Whale Aboard and Audit Night expose their required public heist
anchors without publishing hidden route state. Grand Casino scenarios contain
no game or travel operations and do not change the duel or invitation gate.

## Executable package matrix

`scripts/tests/foundation/env06_7_package_e_contract.gd` executes:

- eight complete success traces with save/load before and after every command;
- 24 exact identity-choice traces with target and save/load proof;
- failure, refusal, and travel interruption at all 34 active phase positions
  (102 terminal traces), with terminal save/load proof;
- each subscribed public pressure fact at all 26 work-phase positions;
- command replay, same-receipt conflict rejection, and hostile wrong-phase
  rollback without partial mutation;
- partial, terminal, and expired reentry, night-end expiry, and idempotent
  cleanup for every identity;
- public-projection privacy and deterministic serialized platform parity;
- bounded transition liveness and normal/reduced-motion semantic parity;
- independent task and safe-exit targets, visible label/prompt/non-color state,
  44-by-44 minimum hit bounds, and all 80 package-scoped presentation evidence
  identities.

The package-local native/Web probe now provides actual platform evidence:

- native and a fresh Web export in Chrome 151 CPU4 produced the exact semantic
  SHA-256 `0c068a6431e1486850d6087cd713c0baec3b3cf909a8d5a6126f23d184a47683`;
- native measured 27.716 ms for 200 canonical serializations and a 3.448 ms
  maximum across 30 idle frames;
- Web measured 210.065 ms for 200 canonical serializations and a 69.535 ms
  maximum across 30 CPU4 idle frames;
- 80 native GL package-local rasters, their hashes, accessibility geometry,
  obstruction states, and the 960-by-2700 contact sheet are recorded under
  `docs/plans/evidence/env06_7_package_e/`.

The authoring verifier additionally binds eight unique calculated signatures,
28 pairwise normalized comparisons, exact operation receipts, production target
inventory, and a package-local dossier per identity.

## Expected sentinels

- `ENV06_7_PACKAGE_E_AUTHOR_OK scenarios=8 pairs=28`
- `ENV06_7_PACKAGE_E_CONTRACT_OK scenarios=8 signatures=8`

The payload does not edit shared catalog/index/loader files, combined dossiers,
cross-seam fixtures, the pixel canvas, runtime/schema, board, crew/world models,
main, Gate Service artifacts, or owner files. Assembly owns the bounded
`queen_public` handler/renderer adapters, catalog registration, combined Grand
Casino fixture, and final integrated-environment captures. Package E's committed
rasters prove its package-local controls and states; they do not substitute for
assembly's final renderer/environment contact sheet.

Production ContentLibrary/environment composition is intentionally still red:
Delta Queen and Grand Casino publish no semantic zones, and three existing Grand
Casino internal room routes are not catalog-backed for authoritative base-record
production. The exact assembly-owned requirements are recorded in
`env06_7_package_e_assembly_adapter_request.md`. This proof must not be used as
an accepted-ready handoff until that shared seam is supplied and the production
composition matrix is green.
