# 0.5 prerelease playtest sweep report

Date: 2026-07-26  
Prompt: `docs/todo/p1_2_prerelease_playtest_sweep_prompt.md`  
Result: PASS after fixes

## Summary

The sweep found no product-facing blocker defects. It found two should-fix issues in the release/playtest tooling that could hide or falsely report UI regressions:

| Severity | Count | Disposition |
| --- | ---: | --- |
| Blocker | 0 | None found |
| Should-fix | 2 | Fixed in this prompt |
| Polish | 0 | No follow-up prompt authored |

## Defects found and fixed

| Severity | Route / seed | Evidence | Finding | Fix |
| --- | --- | --- | --- | --- |
| Should-fix | Mouse sweep, multi-game fixture, seed `P1-2-SWEEP-003` | `.tmp/release_queue/p1_2/mouse_batch/run_analysis/run_003_analysis.json`; raw capture `.tmp/release_queue/p1_2/mouse_batch/raw_mouse_reports/mouse_run_003.json` | The visual QA route single-clicked multiple visible game objects, leaving the camera focused on the last one, then attempted to double-click an earlier stale object coordinate. The player-facing room was not dead-ended, but the harness failed with `Could not double-click visible game object: game:bar_dice`. | `tools/foundation_visual_qa.gd` now refreshes object IDs from live canvas snapshots and resets room focus/retries when a cached object hit point is off-screen after camera focus. Exact failed seed now passes; 4-seed mouse batch now passes 4/4. |
| Should-fix | Soak probe, default `P1-2-SOAK`, 180 minutes / 504 actions | Failing report `.tmp/release_queue/p1_2/foundation_soak_probe_full_report.json`; passing report `.tmp/release_queue/p1_2/foundation_soak_probe_full_after_fix_report.json` | Retained memory stayed under the total retained-growth cap and object/resource/node counts were flat, but one late allocator/cache warmup near minute 170 made the tail regression slope exceed budget. This was a false sustained-leak classification. | `tools/foundation_soak_probe.gd` still fails total retained growth and sustained recent growth, but no longer fails a plateaued one-time retained-memory step as a leak. Default soak now passes. |

## Route coverage

- Cold start / onboarding: covered by mouse playtest runs and visual QA start/onboarding states.
- Clean arc: covered by mouse playtest victory routes, FoundationSuite `all`, visual QA, performance probe, and determinism probe.
- Dirty arc / Rourke pressure: covered by visual QA, mouse batch R100 regression coverage, FoundationSuite `audit`, and stuck-state sweep.
- Meta loop: covered by visual QA Sal/home/container states and mouse playtest run-end/home traversal.
- Eight games: covered by FoundationSuite game-specific suites, performance probe renderer/game/resolve coverage, stuck-state sweep, and mouse playtest surface entry/actions.
- Small-screen and reduce-motion: covered by visual QA accessibility states and UI suite coverage.

## Playtest results

| Tool | Result |
| --- | --- |
| `tools/foundation_mouse_playtest.ps1 -CleanSave -Seed P1-2-SWEEP-003` | PASS after fix; 66 input events; victory and recovery/debt pressure verified |
| `tools/foundation_mouse_batch_playtest.ps1 -RunCount 4 -SeedPrefix P1-2-SWEEP` | PASS after fix; 4/4 playable-loop passes, 4/4 R100 UI-regression passes, 4/4 victories, 0 true failures |
| `tools/foundation_stuck_state_sweep.ps1 -SeedCount 200 -RequireGodot` | PASS; 200 seeds, 48 slot scenarios, 9 wait scenarios, 0 stuck states |
| `tools/foundation_soak_probe.ps1 -SimMinutes 180 -ActionsPerSample 28 -SeedPrefix P1-2-SOAK -RequireGodot` | PASS after fix; 19 samples, 504 actions, retained object/resource/node growth 0, serialized max 168900 |
| `tools/foundation_visual_qa.ps1` | PASS; 0 warnings |
| `tools/foundation_performance_probe.ps1 -RequireGodot` | PASS; 62 observations, renderer/game/resolve/new-surface coverage present |
| `tools/foundation_determinism_probe.ps1` | PASS; 10 seeds, 320 checkpoints, combined hash `793128878` |

## Gate results

| Gate | Result |
| --- | --- |
| `tools/validate_project.ps1` | PASS |
| Supported `-FoundationSuite` arguments | PASS 19/19: `smoke` 29012ms; `contracts` 129828ms; `contract` 130392ms; `games` 129465ms; `systems` 27441ms; `ui` stages green; `slot` 24663ms; `slots` 24007ms; `slot_acceptance` 533050ms; `blackjack` 10612ms; `roulette` 10462ms; `baccarat` 10341ms; `video_poker` 68788ms; `bar_dice` 39884ms; `pull_tabs` 12786ms; `scratch_tickets` 10689ms; `audit` 616454ms; `all` 151487ms; `full` 140925ms |
| `tools/foundation_visual_qa.ps1` | PASS |
| `tools/foundation_performance_probe.ps1 -RequireGodot` | PASS |
| `tools/foundation_determinism_probe.ps1` | PASS |
| `tools/foundation_stuck_state_sweep.ps1` | PASS |

## Deviations / contention

- The first full soak invocation used too small a wrapper timeout and was inconclusive; the default soak was rerun with a larger timeout, found the retained-memory slope false positive, and then passed after the probe fix.
- A separate trailer-rendering task repeatedly launched Godot (`tools/trailer_capture.gd`) during the FoundationSuite rerun. Contended attempts were discarded. Final counted gate results were isolated after the trailer chain cleared.
- Existing unrelated dirty files were preserved and not staged by this prompt.
