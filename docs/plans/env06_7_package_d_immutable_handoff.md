# env06_7 Package D — Immutable Remediation Handoff

Historical note: this rejected-branch handoff is superseded by
`env06_7_package_d_ordered_recovery.md` and is retained only as recovery
provenance.

Status: remediation-ready for independent Integrator review. This document
does not record a verdict.

- Dependency base: `855a296126e8b4747b78fbe89cb5a2d02daf61f5`
- Exact remediation product head: `8c2fc88535e7639c6f2760bf2a22ba2bb70d0217`
- Branch: `codex/env06_7-pkg-d`
- Payload: three-way apply of `855a2961..8c2fc885`; never replace a tree.
- Dependency hold: rebase only after an accepted env06_6 successor is named.

## Exact payload

1. `data/environments/scenario_sequences/env06_7_underground_lounge.json`
2. `docs/plans/env06_7_package_d_crew06_10_seam_manifest.md`
3. `docs/plans/env06_7_package_d_remediation_proof.md`
4. `docs/plans/env06_7_package_d_sequence_dossiers.json`
5. `scripts/tests/foundation/env06_7_package_d_contract.gd`
6. `tools/env06_7_package_d_author.gd`

No crew/world model, poker game/data/model, shared scenario catalog/index,
scenario runtime/schema/renderer, shared pixel canvas, board, main, `.tmp/`,
`.tools/`, or `review_artifacts/` path is in the payload.

## First-rejection closure

The product graph now contains a named three-way identity decision for every
scenario at its own lifecycle point. Open Mic has executable
manage/support/sabotage paths; the other 11 identities have correspondingly
specific choices rather than renamed generic failure terminals.

The focused contract now executes all required package-local proof rather than
only success and arrival refusal. The exact matrix and evidence bindings are in
`env06_7_package_d_remediation_proof.md`: all identity options, every active
phase's failure/refusal/interruption, every work phase's subscribed pressure
fact, pre/post-branch save/load, partial/terminal/expired reentry,
expiry/cleanup, travel/revisit, receipt mismatch, malformed command and fact
atomicity, hidden-state observer checks, deterministic platform projection
parity, and package-scoped accessibility/hit/obstruction evidence.

## Exact-head evidence

Executed from a clean worktree at product head `8c2fc885`:

- author/schema/uniqueness: `ENV06_7_PACKAGE_D_AUTHOR_OK scenarios=12 pairs=66`
- lifecycle/runtime/presentation: `ENV06_7_PACKAGE_D_CONTRACT_OK scenarios=12 signatures=12`
- combined focused wall time: 122.1 seconds
- repository validation: `validate_project PASS 58993ms`

The validation wrapper reported that its separately discovered engine-level
checks were skipped; the two focused Godot scripts above were executed directly
with the canonical Godot 4.6 console executable and passed.

## Integration holds owned elsewhere

The env06_7 assembly owner remains sole writer for the shared
`punchline_clubs` handler/renderer, the 55-id catalog, combined dossiers,
cross-seam fixtures, and ordered A → B → C catalog lineage. Package D remains
dependency-held on the accepted env06_6 successor. The Integrator owns review,
gating, and landing.

Known local reds: none. This is Package D's first-rejection remediation; any
second rejection must be escalated to the owner and there is never a third
review round.
