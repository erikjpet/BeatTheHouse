# Beat the House 0.5 Pre-Release Audit

> Historical audit. Superseded for current source status by
> `docs/plans/0.5_source_completion_record.md` (2026-08-12). Measurements and
> findings below remain evidence for the exact source identified here.

Date: 2026-08-05
Audited committed source: `58519d4a08add056dd63ba3734c99addca130cdb`
Audited working tree: the same commit plus the owner's uncommitted narration,
audio-bus, content-contract, and documentation work

## Verdict

**No: the project has not completed everything promised for 0.5, and the
current source is not a release candidate.**

Most of the large 0.5 feature line is implemented: Grand Casino/Cage/Rourke,
Scratch Tickets, lottery restocking and the scalper encounter, Sal's resale
shelf, the spatial inventory foundation, the dialogue-guided tutorial,
conversation/run-end cleanup, UI redesign, Web/audio closure, and the runtime
storage/performance root fixes all have code and automated evidence. Version
identity is consistently `0.5.0`, and the collection metadata check passes.

Release closure is blocked by two promised feature prompts that were split out
of the tutorial playtest pass and never executed, committed regression failures,
a false-green script-load gate, an uncommitted integration workset, the
human-only tutorial/owner decisions, and packaging/publication.

## Commitment audit

| Area | Status | Evidence / remaining work |
| --- | --- | --- |
| Release identity | PASS | `project.godot` and Windows/Android/iOS export versions identify `0.5.0`. |
| Grand Casino, Cage, Players Card, Rourke, Sal, Scratch Tickets | IMPLEMENTED | Archived prompts, release checklist, Foundation coverage, and current production code. |
| Lottery restock/scalper | IMPLEMENTED | Three-hour deterministic catch-up; exact 50/40/10 stock distribution; seeded scalper and 50/50 knowledge branches in `scratch_tickets.gd` and its Foundation checks. |
| Tutorial implementation | PRE-HUMAN PASS | TUT-N01-N16 and N18-N25 have scripted/real-interface evidence. TUT-N17 still requires five cold humans. |
| Conversation/run-end and tutorial-time behavior | IMPLEMENTED | Terminal report tests clear pending conversations; tutorial-only Pal freeze, continued environmental animation, and Blackjack tutorial pacing/freeze contracts exist. |
| Native/Web performance, soak, storage | IMPLEMENTED, REFRESH REQUIRED | Historical gates and the slot runtime/storage root-fix gates pass. Exact final-source matrix must be rerun after the remaining changes. |
| Inventory screen/card and item-description redesign | **OPEN** | `tutorial_inventory_rework_prompt.md` was created as a binding sibling task and never archived. Shared spatial storage plumbing and voiced descriptions are partial prerequisites, not the promised card/detail acceptance. |
| Meaningful destination decisions | **PARTIAL** | The tutorial Gas Casino/Underground fork exists, but the whole-game offer/forfeit comparison and data-backed opportunity-cost system in `tutorial_meaningful_decisions_prompt.md` do not. |
| Exact-source integration gates | **FAIL** | See fresh results below. |
| Human/owner approval | **OPEN** | TUT-N17, Web/Windows hands-on play, collection `draft` decision, copy/screenshots/limitations approval. |
| Packaging/publication/tag | **NOT STARTED** | No approved exact-source artifacts or `v0.5.0` tag. Existing generic files under `builds/` are not approved release evidence. |

## Fresh gate evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Architecture validation | PASS | `tools/validate_project.ps1`, 33.7 s. |
| Collection metadata | PASS | `tools/collection_meta_check.ps1 -RequireGodot`. |
| Current dirty-tree Systems | FAIL | `.tmp/pre05_audit_systems/summary.json`: three content failures plus the tutorial dealer-reprieve warning. |
| Clean `58519d4a` Systems | FAIL | `.tmp/pre05_clean_systems_foundation.json`: Grand Casino seen/locked persistence and two `parking_lot_tip/follow_tip` route-open copy contracts. Stderr also contains two off-tree inventory `grab_focus` errors, a leaked object/resource warning, and a nonzero resource-at-exit error. |
| Current UI component suite | FAIL twice | `.tmp/pre05_audit_ui/summary.json`: selected map location snaps from the previous view window instead of beginning a focus animation. The run also emits the bag-reel opposite-anchor warning. |
| Script-load gate honesty | FAIL | `gdscript_load_check.json` says PASS while its stderr contains many `SCRIPT ERROR: Parse Error` messages from compositional test fragments. The checker treats a non-null script resource as proof even when reload failed. |
| Git/release identity | BLOCKED | `main` is five commits ahead of `origin/main`; the working tree contains uncommitted owner work; no `v0.5.0` tag exists. |

Historical reports remain valid only for their recorded source. They cannot
override these fresh exact-tree failures.

## Binding gap-closure plan

1. **Reconcile the current worktree.** Finish, verify, and intentionally commit
   or explicitly defer the narration/audio-bus work. Do not discard it. Begin
   candidate testing only from a clean, committed source identity.
2. **Restore trustworthy release gates and fix committed regressions.** Execute
   `v05_release_gate_truth_and_regression_prompt.md`: fix the travel/content
   failures, map focus animation, off-tree focus, anchor warning, leaks/errors,
   and the false-green GDScript load check at their causes.
3. **Complete the inventory promise.** Execute and archive
   `tutorial_inventory_rework_prompt.md`, coordinating its item-copy portion
   with the reconciled narration rollout.
4. **Complete meaningful destination decisions.** Execute and archive
   `tutorial_meaningful_decisions_prompt.md`; preserve the existing tutorial
   fork while implementing legible, data-backed whole-run tradeoffs.
5. **Refresh performance and fidelity on final code.** Run every binding
   performance, liveness, soak, determinism, Web, visual, strict-input, and
   storage gate without waivers; then archive the performance prompt.
6. **Perform human tutorial acceptance.** Five cold players, including two
   without Blackjack knowledge, must meet TUT-N17. Archive the tutorial prompt
   only when the measurements and observations are recorded.
7. **Build the exact release candidate.** On one clean commit, execute the full
   RC prompt, refresh all captures, record the collection `draft` decision,
   complete owner Web/Windows play, and approve or reject that exact hash.
8. **Package and publish only with explicit owner authorization.** Build and
   test Web/Windows artifacts from the approved hash, record SHA-256 hashes,
   upload, publish, create `v0.5.0`, and reconcile the public records.

No historical green report, owner decision, or package may be silently carried
across a later code change. Any fix after RC approval creates a new candidate
and reruns the affected matrix.

## 2026-08-05 gap-closure execution

### Updated verdict

The implementation/regression portion of the 0.5 plan is complete. The code
baseline through `84ae3fc6` has trustworthy gates, finished narration/audio,
the shared inventory-card redesign, data-backed route opportunity costs, and
green exact-source performance/storage evidence. It is a **technical release
baseline**, not an owner-approved or published release candidate.

### Root causes and fixes

| Area | Root cause | Fundamental fix | Commits |
| --- | --- | --- | --- |
| Gate truth | process exit/non-null resources overruled parser/runtime stderr; compositional fragments were loaded as fake standalone scripts | strict classified stderr is authoritative; standalone exhaustive loads and generated split-runner coverage are explicit | `038d8a94`, `f9fb2fd2` |
| Systems/UI regressions | travel copy/state, focus-animation origin, and lifecycle/layout ownership diverged from production contracts | reconcile route state/copy; preserve pre-focus map window; defer focus/size work to owning tree/layout boundaries | `f9fb2fd2`, `ee41ac74` |
| Inventory | spatial plumbing had no shared scannable card/detail contract | pooled shared card view model/surface for run and meta storage; badge-source risk exclusion | `a0b9290a`, `39c56a31` |
| Destinations | decision framing existed only as cost/risk inputs and later only in data, not the production map | all routes author offers/tradeoffs; live choices derive commitment/forfeit; map consumes the framed list and shows all six lines | `c64bd635`, `a44f1822`, `e8fdb0c3` |
| Late-run performance | Crew debt-profile liquidity was invisible to terminal evaluation; conversation opening ran a full checkpoint | recognize debt-profile recovery; autosave presentation mutation without unrelated terminal evaluation | `3c4f7e57` |
| Slot fidelity | Buffalo true-win payout was format-blind; one test bypassed the canonical format/classification paytable | explicit per-format true-win paytables and canonical payout-contract validation | `e4ec5549`, `37eba0bb` |
| Performance gate reliability | 40 frames produced about 17 animated draw samples, so a single scheduler spike became p95 | restore authored 120-frame distribution; budgets and liveness floors unchanged | `84ae3fc6` |

The temporary invalid standalone negative-control fixture made the load stage
fail and was removed. No expected-error allowlist, assertion weakening, sleep,
budget relaxation, or liveness waiver was introduced.

### Final evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Exact-source Full matrix | PASS | `.tmp/v05_release_candidate_green/summary.json` |
| Systems / complete UI | PASS | `.tmp/v05_systems_green/summary.json`, `.tmp/v05_ui_decision_final/summary.json` |
| Standalone native performance | PASS, 63 observations / 0 failures | `.tmp/v05_release_performance_full.json` |
| Determinism | PASS, 320 checkpoints, matching hash `2820946917` | `.tmp/foundation_determinism_probe/run_a.json`, `run_b.json` |
| Strict rendered mouse play | PASS 2/2, two victories, zero failures | `.tmp/v05_mouse_final/aggregate_summary.json` |
| Visual QA / tutorial capture | PASS, no warnings / stable placement | `.tmp/v05_visual_final.log`, `.tmp/v05_tutorial_capture_final/` |
| 180-minute soak | PASS, 504 actions, negative retained growth, zero orphans | `.tmp/v05_soak_final.json` |
| Slot storage | PASS, 12 cabinets / 21 save-loads | `.tmp/v05_slot_storage_probe.log`, `.tmp/v05_slot_storage_soak.log` |
| Web L0.2 / Grand Casino | PASS at 4x CPU throttle | `.tmp/v05_web_perf_l02.summary.json`, `.tmp/v05_web_perf_grand_casino.summary.json` |
| Scratch RTP | PASS, all seven 50,000-sample bands | `.tmp/scratch_tickets/rtp_audit.json` |

Classified stderr is clean in the passing Full report. The four completed
implementation prompts are archived with execution records. Remaining release
work is deliberately outside unattended implementation: TUT-N17's five cold
human trials; owner Web/Windows hands-on approval and release-content/platform
decisions; then artifact build, hashes, upload/publication, and annotated
`v0.5.0` creation.
