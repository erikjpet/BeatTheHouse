# 0.6 Consolidated WIP Handoff

Status: WIP — the only branch permitted alongside `main`

`codex/wip-0.6-consolidated` is the sole preservation and continuation branch
for unfinished 0.6 work. No task may retain its own branch after the 2026-09-14
custody cleanup.

The active tree is synchronized with current production `main`. It originally
absorbed the deferred performance qualification branch at `eb6973d3` and the
owner-evidence transaction tooling at `5e46b64c`. The owner subsequently
approved all completed Phase 1–3 performance machinery: the evidence
transaction; exact-candidate terminal-soak and fresh-worktree native-extension
fixes; idle/liveness, allocation/zero-copy, surface/matrix, low-end and permanent
14-check Audit/Full contracts; and Slot, scripted-run and Grand Casino telemetry.
Those changes are now inherited from `main`. No runtime, tool, fixture, or
performance report implementation remains WIP-only.

The historical branch manifest remains at
`0.6_consolidated_branch_manifest.tsv`. It is provenance only; its retired refs
are not active work and must not be recreated without an owner-approved task.

## Active unfinished work

- `perf06_1`: no implementation is retained solely on this branch. The binding
  performance/platform run remains parked until a replacement placement design
  is accepted and the exact pushed candidate host is quiescent and witnessed.
  The retained historical rehearsal on `main` remains explicitly non-binding.
- `playtest06_2`: the full post-placement playtest gate remains parked, but its
  evidence-transaction safeguard is complete on `main`; there is no longer a
  WIP-only implementation for that safeguard.
- The canonical board on `main` remains authoritative for later balance,
  polish, voice, cleanup, packaging, and release work.

The rejected reusable-slot/fixture placement experiment is not part of this
active tree. The old environment-presentation alternatives, solver extraction,
browser-entropy alternative, integration experiments, game/world closeout
probes, and diagnostic working-tree patches were assessed as rejected,
superseded, or already represented in stronger form on `main`.

## Recovery rule

Do not merge this branch wholesale into `main`. Finish one bounded task here,
verify it against the then-current `main`, land only that accepted task on
`main`, and immediately refresh this branch. Do not create a durable third
branch.
