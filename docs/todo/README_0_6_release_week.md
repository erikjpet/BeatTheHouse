# 0.6 Release Week — plan and scoreboard

Created 2026-09-22 by the owner and PM. Target: 0.6.0 upload-ready artifact handoff by **2026-09-29**.
This page replaces the "Current owner-directed sequence" on
`README_0_6_board.md`. Anything not on this page ships in 0.6.1 and is listed in
`../plans/0.6.1_backlog.md`, so no idea is dropped.

## What 0.6 means

A player can start a new run and reach the win state of **all three endings**
through the normal game:

1. **Clean**: climb Linda's Players Card ladder to the clean Grand Casino ending.
2. **Cheat**: survive Rourke's walk, pat-down and interrogation, and win the
   five-hand back-room duel.
3. **Crew/heist**: build Crew trust and finish a Grand Casino heist to its win
   state.

Each ending may be simpler than the full design, but it must be finishable. Rooms
use fixed, modular slots instead of randomly scattered objects.

## Deliverable

Two upload-ready zip files: the **itch.io Web build** and the **Windows `.exe`
build**, both at version 0.6.0, built with `tools/export_itch.ps1` without
`-Push`. Each zip must pass its PCK audit and packaged smoke checks. The owner
always uploads the zips personally; agents never run butler or any upload or
publish command.

## Owner questions file

`rw06_owner_questions.md`, read and written at its primary-checkout path
`D:\Projects\Beat-The-House\docs\todo\rw06_owner_questions.md`, is the only channel between agents and the owner. Agents ask there
and keep working. The owner answers there within a few hours. Whichever agent
next finds the answer resumes the work. Every row must follow its protocol.

## Owner decisions (2026-09-22, binding)

- **Placement.** Every room gets fixed, authored, modular slots. Scenario objects
  are placed only in those slots. Anything that doesn't fit goes to a room action
  list. Adding a slot later must be a data change only.
- **Nothing useful is lost, and no branch is left behind.** The worker merges
  its snapshot commit into `main`, so all of the solver work lives permanently
  in `main`'s history and can be recovered with `git show SNAPSHOT:<path>`.
  The unfinished solver is deactivated in production. Each piece is kept,
  adapted or dropped based on `docs/plans/postfix06_2_placement_harvest.md`.
  Nothing is deleted without a recorded reason. The branch is deleted after
  the merge.
- **Every row branch merges back.** Every worktree or branch a row creates is
  merged into `main` (or fast-forwarded) and then deleted, both locally and on
  origin. A row is not DONE while it has an unmerged branch. Abandoned
  experiments are merged as a history-preserving commit and then reverted,
  never left dangling.
- **Smaller release gate.** The gate is defined in `rw06_4`. The larger gates
  move to 0.6.1.
- **All three endings** must reach their win state.
- **Run length.** A normal run to an ending takes about 150–350 player actions
  (roughly 30–60 minutes). It may be much quicker when the player hits a
  jackpot or finds a legitimate fast route. Don't remove those shortcuts.
- **Commits.** Worker agents executing these prompts commit and push their own
  work. The PM (planning) session never commits.
- **Parallel execution (Q-009).** Isolated focused Godot runs may overlap under
  the shared four-slot lease folder. Full suites, timing/performance work, soak
  and every release gate remain exclusive. The release orchestrator owns rooms,
  the scoreboard, owner checkpoints and rw06_4; the peer prompt
  `rw06_2p_endings_peer_agent_prompt.md` owns endings and then balance. After
  the shared Q-008 rendering fixes land, rw06_1 map data splits into Grand;
  Bar plus Corner Store; and all other rooms.

## Order

| Day | Row | Prompt | Depends on | Status |
| --- | --- | --- | --- | --- |
| 0 | rw06_pre | `../todone/rw06_pre_worker_handoff_message.md`: the running worker finishes on its branch, merges to main and deletes the branch | — | DONE (`54c7d788`) |
| 1 | rw06_0 | `../todone/rw06_0_custody_commit_prompt.md` | rw06_pre reports MERGED (fixes on main, branch deleted) | DONE (`7da3e5da`) |
| 1–4 | rw06_1 | `rw06_1_fixed_slot_rooms_prompt.md` | rw06_0 | IN PROGRESS / Q-008 REJECTED / PRELIMINARY EXCLUSIVE RED (Combined pushed tip `755d2d71` includes Q-011 and the targeted `env06_6` fixture repair. Its exclusive rerun restored validator timing to 182.144 s within the unchanged 243.150 s limit, then stopped at `env06_6_full_contract`: 25 stale production-authority fixture failures; accessibility and overflow also emitted ObjectDB leak diagnostics. Scenario-authority and exact-Grand fixtures are on separate one-file lanes while phase0 owns leak cleanup/integration. No seed audit or capture starts until those roots pass. Final acceptance still requires 22×6, 100×6, both visual proofs, owner approval, then full exclusive Smoke/Contract and merged-main gates.) |
| 1–5 | rw06_2 | `rw06_2p_endings_peer_agent_prompt.md` → `rw06_2_three_endings_prompt.md` | rw06_0 (runs parallel to rw06_1; final pass after rw06_1) | HANDED OFF TO PEER / WAITING Q-013 (Clean Probe 29 is exploratory/non-qualifying and confirms a $19 deterministic Grand-entry shortfall at $126 cash versus $95 fare + $50 reserve. Cheat repair is at `5e9f2a14`; Q-011/main reconciliation is pushed at peer tip `0379e92a`. Heist seed-preflight has 276 changed lines plus an untracked launcher that must be preserved; the Audit seed/persistence choice is blocked on Q-013. Qualifying 3/3 still waits for rw06_1. Canonical state is `rw06_2p_status.md`.) |
| 1–3 | rw06_5 | `../todone/rw06_5_owner_gameplay_fixes_prompt.md`: clicking a person starts a conversation, blackjack count shown between hands, Cass needs a real count | rw06_0 (parallel with rw06_1/rw06_2) | DONE (`1038a31f`) |
| 5 | owner run | Owner plays one ending start to finish; notes go to the scoreboard | rw06_1, rw06_5 | TODO (rw06_5 DONE; waits for rw06_1) |
| 5 | rw06_3 | `rw06_2p_endings_peer_agent_prompt.md` → `rw06_3_balance_prompt.md` | rw06_2 routes exist | PEER OWNED / RECONNAISSANCE COMPLETE; tuning deferred until completed route curves |
| 6–7 | rw06_4 | `rw06_4_release_gate_ship_prompt.md` | rw06_1, rw06_2, rw06_3, rw06_5 | TODO |

The release orchestrator runs rw06_0, rw06_1, owner checkpoints and rw06_4 via
`rw06_execute_release_week_prompt.md`; rw06_5 is complete. The Q-009 peer owns
rw06_2 and rw06_3 via `rw06_2p_endings_peer_agent_prompt.md` and reports only in
`rw06_2p_status.md`. The file-ownership rule is in each prompt.

## Scoreboard (only the orchestrator edits it; rows report to the orchestrator)

This is the one place that shows whether things are improving. Update values in
place, and add one dated line to the history below.

| Metric | Target | Current | Last updated by |
| --- | --- | --- | --- |
| `check_godot.ps1 -Suite Smoke` | PASS | PASS (`1038a31f`, 10/10 composite: 9/10 full merged-main suite plus a clean isolated exact-profile retest of the sole stochastic performance stage; unchanged budgets) | release orchestrator |
| `check_godot.ps1 -Suite Contract` failing shards | 0 | 0/18 Foundation shards at `1038a31f`; all later UI/tutorial/audio stages passed. The one inherited repeated-reprieve room/scenario-composition standalone stage remains (`.tmp/rw06_5/orchestrator_main_contract/summary.json`, SHA-256 `FDD2D9D421378F6A3F063ADBD378B786F05F08782F72B6AFCD849EFF1F77092B`). | release orchestrator |
| Open P1 (High) defects | 0 | 7 (UIENV-PF-003…008; RP-006 packaged proof) | release orchestrator |
| Open P2 (Medium) defects | 0 | 1 placement (UIENV-PF-009); 0 non-placement | release orchestrator |
| Endings reaching the win state through real UI | 3/3 | unknown | — |
| Owner day-2 room sample (3 rooms) | approved | REJECTED in Q-008: prior `8a7a1ae8` sheet fails the art-alignment direction. All-room correction is active; the next candidate must be normal player view plus base state at `D:\Projects\Beat-The-House\.tmp\owner_review\q008_rooms.png` and must receive a new owner approval before rw06_1 lands. | release orchestrator |
| Owner start-to-finish run | done, no blockers | not started | — |
| Owner run notes | (owner writes blockers here; non-blockers go to the 0.6.1 backlog) | — | — |
| Release gate items green (rw06_4) | all | 0 | — |
| Unmerged row branches (`git branch -a`) | 0 | 11 active release-week branches (rw06_1 core, two hand-layout ancestors and two temporary env06_6 fixture lanes; six peer-owned rw06_2 work branches, including two local evidence lanes) | release orchestrator |

History (newest last):

- 2026-09-22 PM: baseline recorded from the 9/21 post-fix ledger and playtest.
- 2026-09-23 release orchestrator: verified postfix merge `54c7d788`, SNAPSHOT ancestry, harvest, tested-tree identity and complete branch/worktree cleanup; committed the release-week plan and started rw06_0.
- 2026-09-23 release orchestrator: completed rw06_0 custody at `7da3e5da`; validator passed, Smoke closed 10/10 after the authored native-solver wrapper corrected a fresh-worktree extension-cache miss without changing a budget, and the worker Contract census was reused because every change since `54c7d788` is docs-only. Started rw06_1, rw06_2 and rw06_5 in parallel.
- 2026-09-23 release orchestrator: completed rw06_5 at product commit `1038a31f`; person events now converse before resolution, blackjack shows the player's recorded count between hands, and Cass requires a live recorded count. Production-input evidence passed; merged-main validation passed; Smoke closed 10/10 by an unchanged-profile composite; Contract passed 18/18 Foundation shards and all later stages with only the inherited repeated-reprieve standalone red. Archived the prompt and removed the merged row branches/worktrees.
- 2026-09-23 release orchestrator: rw06_2 replay safety harness reached exact branch tip `172731ee`; its engine-free source contract, hostile public-observation contract, and two-launch bridge lifecycle contract passed. Focused evidence is `.tmp/rw06_2/public_observation_contract.json` (SHA-256 `F8F699846DB1CFD6C5659F7A9D10F42C77CAB4F8573C4C5EE19D28E683889195`) and `.tmp/rw06_2/clean/bridge-20260923-054711-435-14372/summary.json` (SHA-256 `79F6D5CB75857BA83A66A475BA4EB5FC6C18A2D9311C7954B5B56AEA2C862CCF`). Qualifying ending runs remain deferred until rw06_1 lands.
- 2026-09-23 release orchestrator: rw06_1's first focused grounding run at `78dfa8e1` failed on a real generated-base slot mismatch. Read-only audit also found unstable phase-to-phase scenario bindings, two barriers intersecting the mandatory walk lane, runtime label placement still bypassing authored anchors, and an incomplete static phase/action census. The row remains IN_PROGRESS while those root causes and their non-weakened regressions are repaired; Godot is off pending a new audited tip.
- 2026-09-23 release orchestrator: expanded rw06_1's exact-tip checklist to 17 findings before spending more engine time. It now covers production-canvas/base/label authority, reachable multi-action overflow controls, fail-closed safe exits and binder errors, transient TalkDock ownership, reconstructible actor moves, and swept-body route collisions (including the Back Alley lender crossing). Core/data/static work remains on `codex/rw06_1-phase0`; the non-overlapping overflow UI and Foundation integration contract run in `codex/rw06_1-overflow-ui`. No Godot resumes until both land on one clean pushed tip and pass read-only audit.
- 2026-09-23 release orchestrator: applied the owner's latest checkpoint order: request the start-to-finish owner playthrough as soon as rw06_1 and rw06_5 are on `main`; rw06_2 continues in parallel and no longer delays that request. rw06_3 still waits for both the owner's notes and rw06_2 route evidence.
- 2026-09-23 release orchestrator: rw06_1 remains fail-closed before engine use. Core checkpoint `f3ab259e` records the incomplete bounded search; follow-up work removes greedy route pinning, matches runtime swept paths, integrates exits, and widens deterministically to complete domains. Independent audit also requires exact reachable composition census, production modular binding, preferred-slot fallback, transient TalkDock assertions and same-room replay reset. Isolated UI tip `346c8cec` fixes ordinary action dispatch, but re-audit found record-level scenario authority was not included in the stale-action seal; its additive correction is in progress. No Godot process has run on these revisions.
- 2026-09-23 release orchestrator: owner morning steer set an end-of-2026-09-24 rw06_1 landing timebox and requested immediate engine evidence plus the interim day-2 sample. Combined tip `44758fad` includes the corrected overflow authority seal; Q-004 is OPEN while focused Godot contracts and the three-room capture run. Unsafe optional actor walks now simplify to stationary slots and non-fitting objects to overflow, with each simplification logged for 0.6.1; overlap, exit, reachability and hidden-state guarantees remain hard. rw06_2 exploratory non-placement ending runs also started on current `main`; qualifying passes remain post-rw06_1.
- 2026-09-23 release orchestrator: immediate rw06_1 engine signal at pushed tip `0948d2b8` cleared a Foundation parse blocker, then failed closed as intended: the focused overflow contract found no live production game action, and the contact-sheet run produced only six unrelated base-room cells while all requested Corner Store, Bar and Grand Casino selections were rejected for missing/invalid authored label rectangles plus real overlap/route violations. The missing Q-004 sheet is not counted as evidence; production authority is being repaired under the 2026-09-24 timebox while one non-qualifying rw06_2 Clean exploration uses the serialized Godot slot.
- 2026-09-23 release orchestrator: the first non-qualifying rw06_2 Clean exploration on current-main ancestry stopped fail-closed after action 1 because replay policy tried to dismiss a coach before following the visible Pal TalkDock `continue` choice. Public UI evidence classifies this as a harness-policy blocker rather than a placement or product-arc failure. Evidence: `.tmp/rw06_2/exploratory/clean-main-6a9201e3/20260923-092920-880-9772/run-01/summary.json` (SHA-256 `6225DC1798DEE8A4EE5772047E6256D76A2C5B099F886B8F03047E9B6E72CDC9`); the replay fix is in progress before the next serialized probe.
- 2026-09-23 release orchestrator: rw06_2 fixed that first replay-policy blocker narrowly at product commit `96ad4f9d` / pushed tip `760bdf4c`: only a visible tutorial-guide TalkDock with exactly one enabled public `continue` choice precedes coach dismissal. The strengthened source regression and full engine-free validator pass; a second non-qualifying Clean probe is using the otherwise-idle serialized Godot slot.
- 2026-09-23 release orchestrator: the second non-qualifying Clean probe confirmed the TalkDock/coach fix, then stopped fail-closed after action 4 when `Skip Lessons` sat below a visible Run Menu scroll viewport and the production-input bridge had no semantic scroll command. This is a public harness-reachability gap, not an ending arc, placement or economy failure. Evidence: `.tmp/rw06_2/exploratory/clean-main-11c584bd-probe2/20260923-094003-789-18960/run-01/summary.json` (SHA-256 `D876EF3276898B59AC33B0E9F10C8A7953184E6EA29CA352A3DFFE03E74AF344`); a narrow public-only scroll bridge and hostile regression are in progress.
- 2026-09-23 release orchestrator: rw06_2's public-only Run Menu scroll bridge and its pre-scroll `Skip Lessons` call site are corrected at product commit `00a832ba` / pushed tip `f5c56f72`. The bridge uses real wheel input, verifies visible viewport motion, remains bounded to public rendered state, and passes its hostile source regression. A third non-qualifying Clean probe is queued behind rw06_1's focused contract/contact-sheet lease.
- 2026-09-23 release orchestrator: processed Q-004 without claiming approval: the owner confirmed the reserved day-2 sheet did not exist and the partial all-room image was insufficient to judge the full layouts. rw06_1 continues repairing and capturing the exact Corner Store, Bar and Grand Casino normal+expanded sample; a fresh owner question will be appended only after that artifact exists and is visually verified.
- 2026-09-23 release orchestrator: rw06_1 pushed checkpoint `6fe7ee5c`. Its three target maps statically replay 209 active snapshots / 1,411 bindings with zero active overflow and zero errors; Bar preserves all 14 scenario slots and both exits by moving five randomized base slots to authenticated overflow. Pre-engine audit then found two real authority blockers: the semantic inventory still rejects legitimate geometry-free overflow, while the production controller can re-seal unvalidated persisted bindings. Focused Godot and the replacement Q-004 capture wait only for fail-closed fixes and regressions at those seams.
- 2026-09-23 release orchestrator: rw06_2's third non-qualifying Clean probe confirmed semantic scrolling, then stopped at action 6 because the harness treated a partially clipped `Skip Lessons` edge as clickable; the label and center remained outside the Run Menu viewport, so no confirmation appeared. This is a harness rendered-reachability gap, not placement or product-arc evidence. Evidence: `.tmp/rw06_2/exploratory/clean-main-006620e5-probe3/20260923-103641-220-18380/run-01/summary.json` (SHA-256 `AF3293A2F0499FE1FCC9A2599E6C116C6F83B4020DC5387BD1464854B35EB6A1`) and screenshot `.tmp/agent_playtest/2026-09-23/rw062-clean-18380-1-3adb39135e/0011.png` (SHA-256 `D0B1B49E5D95D1DE7056FF95DBFE928DEE0603A30DC57C5E904AD67DDE1C5591`). The engine lease returned cleanly with zero Godot processes; the narrow full-visibility harness fix is in progress while rw06_1 takes the lease.
- 2026-09-23 release orchestrator: ran rw06_1's focused Godot contract and exact day-2 capture at pushed tip `4ce68e59`. The contract failed without weakening its guarantees: the compact overflow panel violates the 44 px targets/320 px viewport and its production mouse/touch action path does not yet mutate the game exactly once. The capture reached 0/3 because authenticated geometry-free overflow is still rejected during Bar/Corner Store travel and Grand Casino still has a room-route authority seam. Evidence: `.tmp/rw06_1/focused_current_tip_20260923-1044/stderr.log` (SHA-256 `7F3ECF8E986D134DDF5BC5410ED3AB0245AA3C3405A86BE3F43F758DCC899630`) and `.tmp/rw06_1/contact_sheet/day2_4ce68e59/contact_sheet_report.json` (SHA-256 `ED766EBFFDCDFCD77BA82F13C1DA19CF80C2A0AD7AFF373B756CDC1341091470`). Back Alley now has a proven timebox-safe cap retaining nine base slots, all six stage slots, both exits and its authored route; only one ground marker and one standing person simplify to overflow/stationary treatment, with follow-up logged for 0.6.1. Godot exited and the serialized lease is free while both fixes proceed engine-free.
- 2026-09-23 release orchestrator: rw06_2 fixed the third replay-only blocker at product commit `752511ff` / pushed tip `e52cd0f8`. The public bridge now derives whether the whole rendered button rectangle is inside its clipped viewport, rechecks that state immediately before click, and the replay keeps bounded-scrolling until a unique boolean-true target exists; absent, false, non-boolean and ambiguous values fail closed. Source and full engine-free validation pass. A fourth non-qualifying Clean probe now holds the serialized Godot lease; qualifying evidence remains deferred until rw06_1 lands.
- 2026-09-23 release orchestrator: rw06_2's fourth non-qualifying Clean probe verified the visibility fix with two real wheel steps and a fully readable 52/52 px `Skip Lessons` target, then stopped at action 7 when the accepted click produced no confirmation. The harness emits wheel press without the matching release that repository-native UI helpers send, so this remains a narrow replay-input blocker rather than placement, ending, economy or goal evidence. Evidence: `.tmp/rw06_2/exploratory/clean-main-d7d09f6a-probe4/20260923-105053-444-27868/run-01/summary.json` (SHA-256 `E83AE7119BECECA09CE852F8C266DCBD2EB21B55DCD8A9463E057381019A844B`) and screenshot `.tmp/agent_playtest/2026-09-23/rw062-clean-27868-1-c4483ceed7/0013.png` (SHA-256 `AF462BFC76B7871F60881B8AE867536AC08714C6701C118F6C3EB8A9A5D05243`). Godot exited cleanly; a press→release harness fix is in progress without another engine run.
- 2026-09-23 release orchestrator: rw06_1 pushed non-authority checkpoint `1ef797f8`. The compact RoomActionList no longer lets its overlay consume descendant pointer/touch input, reasserts 44×44 launcher/close targets and resets its width after a 320 px clamp. Back Alley now deterministically validates 16 active snapshots / 64 bindings with nine base slots, six stage slots, two exits, its authored Bar route and zero overflow/errors; one optional ground marker and standing-person base slot plus optional scenario walks are logged for 0.6.1. The author now reports its 10,000-node watchdog as `SEARCH_LIMIT`, never `UNSAT`, and two separate regenerations were byte-identical. Full all-map authoring continues engine-free while the authority seam is repaired.
- 2026-09-23 release orchestrator: rw06_2's wheel press→release correction is pushed at product commit `a2e98c61` / branch tip `8d3fe210`; its strengthened source contract and full engine-free validator pass. Probe 5 then proved the fully visible `Skip Lessons` click opens the rendered confirmation dialog, including its visible OK and Cancel controls, before stopping at action 7 because those internal controls are absent from the bridge's public button catalog. This is a narrow replay-harness reachability gap, not placement, ending, economy or goal evidence. Evidence: `.tmp/rw06_2/exploratory/clean-main-01a5dc32-probe5/20260923-110216-836-21584/run-01/summary.json` (SHA-256 `E0FD3B4623BB0DC43CA5D3C7B09C7C7BD6B3EBFC7807E6A741EB0450FCA9038A`) and screenshot `.tmp/agent_playtest/2026-09-23/rw062-clean-21584-1-8686b09441/0013.png` (SHA-256 `C48696B1846D386C57306C7DC17AE5E2EED994956C7576B455F73442535CC70D`). Godot exited with empty stderr and zero remaining processes; the narrow dialog-control bridge fix proceeds engine-free.
- 2026-09-23 release orchestrator: rw06_1 pushed Motel fixed-slot checkpoint `6631a87b`. The map deterministically validates 36 active snapshots / 299 bindings with ten base slots, fourteen stage slots, two exits, zero overflow and zero target-validator errors; its optional walks are kept stationary under the Q-005 timebox, and a second regeneration was byte-identical. Gas Station Casino is next; after exact no-solution results through most four-slot reductions, the remaining bounded checks are timeboxed before taking the least destructive overflow cap instead of delaying the 2026-09-24 landing.
- 2026-09-23 release orchestrator: rw06_2 fixed the fifth replay-only blocker at product commit `2f0655c2` / pushed tip `05ffb8db`. The bridge exposes only the rendered, enabled tutorial ConfirmationDialog OK/Cancel controls under exact stable IDs, with full-visibility checks; the replay admits only the exact surface/role/id and boolean-true signals. Two valid controls plus seven hidden/disabled/clipped/ambiguous/wrong-id/missing/non-boolean hostiles pass, as do the post-merge source contract and full engine-free validator (133.8 s). A single non-qualifying Clean proof probe now holds the serialized Godot lease; qualifying evidence remains post-rw06_1.
- 2026-09-23 release orchestrator: rw06_1 pushed Gas Station Casino fixed-slot checkpoint `1a7e004d`. Its active slice validates 49 snapshots / 428 bindings / 113 actions and its complete aftermath-inclusive slice validates 101 snapshots / 494 bindings / 113 actions, all room-bound with zero overflow and zero static errors. The proven timebox cap keeps two doorway, two floor-fixture, two surface and two wall slots; five dropped base classes/slots are logged for 0.6.1, optional actors remain stationary, and two regenerations were byte-identical (SHA-256 `38706A59805690DA1212AE719D8E96F6D157FDAD2021E86226C6BB672FED28A2`).
- 2026-09-23 release orchestrator: rw06_2 follow-up commit `104e4f62` corrected the bridge's Window-vs-Control visibility check before engine use. Probe 6 then selected the exact visible `tutorial_skip_dialog:ok` control but left the confirmation rendered at action 8 because the harness injects every click into the main viewport instead of the owning ConfirmationDialog Window viewport. This is another narrow replay-input blocker, not placement or product-arc evidence. Evidence: `.tmp/rw06_2/exploratory/clean-tip-104e4f62-probe6/20260923-112542-759-25648/run-01/summary.json` (SHA-256 `8CD5D6AD84C47E7B1844994222D1722B08F8FA4300FBC99F5283491413E86D4C`) and screenshot `.tmp/agent_playtest/2026-09-23/rw062-clean-25648-1-7c24f98e36/0014.png` (SHA-256 `C0F337CE97E25673335783635EEE02D99E3CBA71AABD1404D8B6EC848FFD9771`). Godot exited with only the generic ObjectDB leak warning and zero remaining processes; a button-owned viewport input fix proceeds engine-free.
- 2026-09-23 release orchestrator: rw06_1 pushed Small Underground Casino host checkpoint `49b72d59` and comedy-club layer checkpoint `ab53109f`. The empty host map validates its seven base, three stage and two exit slots; the layer's active slice validates 96 snapshots / 662 room bindings / 433 actions with zero overflow, while its complete slice validates 170 snapshots / 744 bindings with only two authenticated aftermath overflow actions and zero static errors. Both regenerated byte-identically; authoring pauses at the clean tip so the semantic-authority repair can merge and unblock the exact day-2 capture.
- 2026-09-23 release orchestrator: rw06_1 merged authenticated persisted-slot authority commit `0d2837af` into pushed core tip `3aa1a771`, then removed the authority and already-merged overflow-UI branches/worktrees locally and remotely. The exact-tip focused Godot contract returned 20 real failures across hostile authority, categorical service refresh/reload/revisit, compact width, delivery finalization and production mouse/touch activation; no guarantee was weakened. Evidence: `.tmp/rw06_1/focused_authority_20260923-113725/stderr.log` (SHA-256 `8C9F09EF5E2DAB4426DDA6E86B9112B6295741BA4CA323209ACFE5C075CAD02C`). Godot exited with zero remaining processes and a separate short-lived repair branch is diagnosing before capture.
- 2026-09-23 release orchestrator: rw06_2 fixed its sixth replay-only blocker at product commit `48cc7dc7` / pushed tip `7af48369`. Dialog buttons are revalidated against their exact owning Window viewport and receive the unchanged real motion/press/release sequence there; ordinary controls remain rooted in the main viewport. Two valid root/dialog fixtures and three null/wrong/ambiguous viewport hostiles pass, as do the full engine-free validator and prior wheel/dialog hostile sets. Probe 7 now uses the serialized Godot lease; qualifying evidence remains post-rw06_1.
- 2026-09-23 release orchestrator: rw06_2 probe 7 accepted the exact rendered ConfirmationDialog OK identity at action 8 but did not leave lesson state because direct child-Window viewport input used dialog-local coordinates instead of the embedder's physical coordinate space. This remains a harness-only reachability blocker with no placement, ending, goal or economy conclusion. Evidence: `.tmp/rw06_2/exploratory/clean-tip-7af48369-probe7/20260923-113929-307-21240/run-01/summary.json` (SHA-256 `CC9EDA352A82F91B183D28E2EB10E827A8DF56A56AC6D71EB48BC1A14EA212F3`) and post-wait screenshot `0015.png` (SHA-256 `C31D634CEDFF30A69990CB4E8FE68C3FDAD10AEB63922F54177BB2CC52FEB2FF`). Godot exited normally with zero processes; a narrow embedder-offset input correction proceeds engine-free.
- 2026-09-23 release orchestrator: rw06_1 completed a read-only exact cap proof for the next Small Underground Casino casino layer while the integrated core stays frozen. Full and all seven one-slot reductions are exact no-solution; among 21 two-slot reductions, eight pass and GU is least destructive. The pending GU patch removes only `base.group.01` and `base.surface_item.03`, preserving both doorways, floor/game/layer fixtures, behind-counter staff, Silas and both wall records. Its in-memory census is 61 active snapshots / 417 bindings / 274 actions and 108 complete snapshots / 469 bindings / 274 actions, with zero overflow and zero errors; the two simplifications will be logged for 0.6.1 when implemented.
- 2026-09-23 release orchestrator: a fresh fixed-slot report at frozen core tip `3aa1a771` selects the requested Corner Store, Bar and Grand Casino peaks with 6/7/10 bindings and zero overflow (SHA-256 `892C70D5679ACFAAD780788ABA9DB3A6E10C86C57A744AEB85F5EA566832001E`). The immediate real capture still failed closed at 0/3 because semantic authority rejects generated `normalized_hit_rect` and `small_screen_rect` on ordinary room records, not because any target selection overflows. Evidence: `.tmp/rw06_1/contact_sheet/day2_3aa1a771_20260923-114903/contact_sheet_report.json` (SHA-256 `CDC218EBA2D9C62D9E1C2FDE8BE9E9D4A2B1376FFA0936EF32D315EB5F361DED`). Godot exited with zero processes; the exact seam joined the focused-contract repair before another capture.
- 2026-09-23 release orchestrator: rw06_2 pushed the narrow embedded-Window coordinate correction at product commit `54080a15` / tip `f643859d`: only the exact visible tutorial dialog controls translate by the embedded Window offset and inject through the root embedder, with two valid and six hostile source fixtures plus the full engine-free validator green. Probe 8 then stopped before `ready.json` on a GDScript inference parse error for the embedder viewport, so it produced no gameplay or product conclusion. Stderr SHA-256 is `AF295127E0E014B1D6E36E00FBF2A3B3A50F8278D481AF6D6B0014A1A42B20E0`; Godot exited normally with zero processes and an explicit type annotation proceeds engine-free.
- 2026-09-23 release orchestrator: rw06_1 bootstrapped the new contract-fix worktree's canonical Godot class cache, then reran the focused contract. The repair clears wrong-class authority, the production alias regression, mouse activation and duplicate environment-only resurrection reports, reducing 20 failures to 10 without weakening a guarantee. Six persisted terminal-envelope checks, compact width, delivery geometry/overlap and touch activation remain red. Evidence: `.tmp/rw06_1/contract_fix_20260923-1200/focused_stderr.log` (SHA-256 `85E1C8229C595F686F5041ED245105E3EF6BDD0F204B1AEA9FACB8B3E0033621`); bootstrap passed, focused exited 1, and Godot returned to zero while diagnosis continues engine-free.
- 2026-09-23 release orchestrator: rw06_2 fixed probe 8's compile blocker at `e4059ee6`, then probe 9 reached a valid ready session but the shell launcher host was cut off by its five-second tool timeout before route actions. The owned session accepted a graceful quit and produced no game-behavior conclusion. Partial ready SHA-256 is `80A742862239F59C560A6F9F983DCF4ED2C4B00849DFBF526DB8242D3059C078`; corrected single-call, attached long-timeout orchestration stayed alive beyond seven seconds and is pushed at `7e522069`. No second probe was started; Godot returned to zero.
- 2026-09-23 release orchestrator: rw06_2 probe 10 used the corrected attached launcher and reached action 7, then the exact dialog route raised a runtime harness error because `ConfirmationDialog` has no `get_parent_viewport()` method. The session auto-issued an accepted graceful quit, the owned process exited, and no placement, ending, goal or economy conclusion was drawn. Evidence: `.tmp/rw06_2/exploratory/clean-tip-7e522069-probe10/20260923-120631-649-9868/run-01/summary.json` (SHA-256 `205B6BE06FF7BB85FD276D0BB39866114C47031394023F963B04877228751A03`) and screenshot `0014.png` (SHA-256 `3F786AEC9BFC04C9A8019048D25B2469E827C8513DE0BCC8FDBA04F76C888EA8`). The narrow fix resolves the dialog's real parent node, rejects null, and requires that parent's viewport to be the exact app embedder; it proceeds engine-free with Godot at zero.
- 2026-09-23 release orchestrator: rw06_1's second focused repair run kept 10 emitted failures but materially narrowed the seam: geometry authority now passes, while six terminal-envelope checks share one ticket-redeemer placement-class mismatch, compact sizing observes a stretched 1280 px viewport despite a 320 px test root, delivery has exactly three genuine label/target overlaps, and production touch activation remains red. No assertion or guarantee was weakened. Evidence: `.tmp/rw06_1/contract_fix_20260923-121712/focused_stderr.log` (SHA-256 `F84BD3EE9B6159DE71FEEFE96CFCD9129D617FB689E5C72CC930F3FBDCC473B7`); the focused run exited 1 and Godot returned to zero while a bounded third repair proceeds engine-free.
- 2026-09-23 release orchestrator: rw06_1's third focused repair cleared every layout and authority defect: terminal refresh/reload/revisit, 320 px compact width with 44 px targets, the three measured delivery overlaps, geometry, mouse, hostile, exit, reachability and hidden-state checks all pass. Four production touch-path messages remain across the delivery canvas, scenario-sequence action and live overflow game entry; the early failed cleanup also emits a leak warning. Evidence: `.tmp/rw06_1/contract_fix_20260923-122555/focused_stderr.log` (SHA-256 `FAC1829B6061CDD218A6420DB34598F62845949275C913C7B1505D8527D21FBF`); the run exited 1 and Godot returned to zero while touch-only diagnosis continues engine-free.
- 2026-09-23 release orchestrator: rw06_2 probe 11 at `9cb85e3b` finally advanced through tutorial skip, a fresh seeded run and real travel to Gas Station Casino, changing cash 100→93 and heat 0→1. It then exposed two distinct non-placement seams: long normal Dealer's Advice copy clipped the unchanged 40 px `Skip tip` CTA inside a fixed 144 px panel, and replay policy selected that control despite public `fully_visible:false`; execution correctly rejected it before input. Evidence: `.tmp/rw06_2/exploratory/clean-tip-9cb85e3b-probe11/20260923-122239-628-20992/run-01/summary.json` (SHA-256 `98058AD4D2021A7612D58BF2F5789891BC3C137022544E8AB38ED22DE1949EFA`) and clipped screenshot `0025.png` (SHA-256 `2B7EDC1447AF57316FDA22DED48F2C087030F61756EB329A481A9B396513F52C`). Pushed fix `ef082651` uses the existing 172 px safe coach height (+14 small-screen), adds a rendered long-copy containment regression, and requires a unique boolean-true fully-visible coach control; focused source, semantic and full static validation pass, and one confirmation probe is serialized.
- 2026-09-23 release orchestrator: rw06_1's focused contract is green after reducing its original 20 failures to zero without weakening any terminal, overlap, geometry, compact-width, input, exit, reachability or hidden-state guarantee. The final touch fix keeps real `Input.parse_input_event(ScreenTouch)` and maps logical canvas-global hit points through the Window's canonical screen transform; all mouse, touch, keyboard and controller mutations pass exactly once. Evidence: `.tmp/rw06_1/contract_fix_20260923-123743/focused_stdout.log` (SHA-256 `EFFCE6D9D4DEF873A7838A55F471478A68217247710DF0D1A6BFB6D4B75751A0`) and stderr (SHA-256 `327BD3ADE82CB2647797432C7ADAF5103D2CE63D5FF847F04C08E8D216B3FF85`); exit 0, with only non-fatal shutdown diagnostics, and Godot returned to zero. The repair is being committed for integration into core before the exact day-2 capture.
- 2026-09-23 release orchestrator: merged focused repair `d143edb2` history-preservingly into pushed rw06_1 core tip `4fd4c350`, then copied its ignored green evidence into the core worktree and deleted the merged repair branch/worktree locally and remotely. The core tip differs from the green repair product tree only by release scoreboard history; its exact static report is regenerating before the Corner Store/Bar/Grand Casino capture. Two release branches remain.
- 2026-09-23 release orchestrator: the exact day-2 capture at integrated core tip `4fd4c350` failed closed at 0/3, so no owner sample was posted. Bar and Grand Casino peak selections reached no state accepted by the capture resolver, while Corner Store rejected `event:scenario_delivery_day_stock` for a placement-class mismatch. Evidence: `.tmp/rw06_1/contact_sheet/day2_4fd4c350_20260923-124339/contact_sheet_report.json` (SHA-256 `FC7A14AE76145A071F7B221E526AC5BEA088A169D2C3D662B1F9F2AD9760805A`), stdout SHA-256 `E1A716FEE8803BCD524DA0EC1624001491E2DD4E75B19F6E2985F6E4289B7614`, stderr SHA-256 `24628FB0E8BC951759C6AA774DE75C139EDFD0E1F702A99A0377CFBB1E4F0F27`; exit 1 and Godot returned to zero. A bounded engine-free repair is aligning capture reachability with the exact static census and correcting only the production classification seam before one rerun.
- 2026-09-23 release orchestrator: rw06_2 probe 12 at `ef082651` traversed Gas Station Casino → Motel → Bar → Kitty Cat Lounge and exposed the enabled Grand Casino invitation after 29 actions; cash changed 100→93→87→79→63 and heat 0→1→0→0→2. The generic event opener correctly refused the two visible choices (`Take the invite`, `Not yet`), so this is a replay-policy defect rather than a product dead end or unclear goal. Pushed tip `c7a6c937` now invokes the exact accept choice; source, semantic and full static validation pass. Evidence summary SHA-256 `6F4D1316A9F495A34482AF7D7B96026125F1CADCB0FD26660F8E9A7F6C773FD7`, trace `7426DB80D6390B9F722A1A39EC5535A98FEEB0FD22DF4B6165C756079E8B326F`, money `B0BC21CEF1AFCC823237881BAD87C8618EC03E691AE7F66DE2C96C1B12D56867`; one confirmation probe holds the serialized engine lease.
- 2026-09-23 release orchestrator: rw06_2 probe 13 at `c7a6c937` proved the exact `Take the invite` action with visible feedback, then exposed a product arc-clarity blocker: the invitation says “The Grand Casino opens,” but with $63 against its $70 route cost the capped world map omits Grand Casino entirely instead of preserving the invited destination as a visible disabled goal. This is not yet an economy-wall conclusion—the deficit is $7 and legitimate games remain visible. A separate replay-policy defect then wandered to Delta Queen and stopped at its two-action boat lock; it will instead fail closed on the visible affordability state. Evidence summary SHA-256 `632273E70085DAD0C03D51F5D4C2E0999C5533D951B040CD426B808EC5AF99EF`, trace `32BDF25C6D958AA3497068E95FE943D86B7A3AC9D67DECDA39114CAC1DBF6C19`, money `7E552A23EE8A2BD5C49E42B832896FEE864C1E8276517E0DE4267B23EA1FFF1D`, post-invite map result `B50D31500D4C48B33579AD8C213722CF2FE4FA1086E9F921579759989829B5DE`. One run only; graceful cleanup, global Godot count zero. The generic shutdown leak warning is preserved but is not classified without retained-object evidence.
- 2026-09-23 release orchestrator: rw06_1's diagnostic day-2 rerun proved the Corner Store class repair reaches its host room and proved exactly one runtime-reachable target state for each requested peak (Bar `work_1`, Corner Store `verification`, Grand Casino `work_1`), with 11/18/32 states explored and no traversal truncation. All three then failed before layout because the capture helper asked `ScenarioEngine.sequence_projection` to revalidate the already-applied next state against the arrival-phase environment; the production commit path uses the Runtime's prevalidated public projection before sealing the new layout. This is a capture-tool seam, not a room/route/overflow failure. Evidence: `.tmp/rw06_1/contact_sheet/day2_4fd4c350_repair_20260923-1305/contact_sheet_report.json` (SHA-256 `7F5591AB3F907C94B0F44FF867DA0DCDFF7614FE8FED72B55187049CD8D6FC98`), stdout `704BE2B97F6815FFC7376434C2235BBED959CD4E34A715142434082574455B16`, empty stderr `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`; 0/3 captures and global Godot count zero. The helper is being aligned to the exact production post-operation seam without changing product validation.
- 2026-09-23 release orchestrator: rw06_1's final exact day-2 capture is green at pushed tip `d651e424`: Bar `work_1`, Corner Store `verification` and Grand Casino `work_1` each rendered in normal and expanded mode, for 3/3 rooms and six source captures with zero report failures. The helper now mirrors the engine's prevalidated post-operation projection seam; Corner Store's catalog-hinted delivery event and closed semantic replay share one authored behind-counter class while hostile wrong-class authority remains rejected. Evidence report SHA-256 `5C1D07ED3F3F903FBF24701D598E130D81FC867701F16ECACBCD83FBB5503F36`, sheet `8F37493B765573E4BDFE0C5F03A196B82F6E537A9600405EB0E6AC8E127D29C1`, stdout `6762FD4E259011A2A5CD4158204E56FCBE13F2B8E157D99D9783EFC2161CD070`; exit 0, only the generic ObjectDB warning, and global Godot count zero. Q-006 now asks the owner to review the real sheet while authoring continues.
- 2026-09-23 release orchestrator: rw06_2 pushed `91fad55c` for probe 13's product blocker. `WorldMap.travel_target_ids` now admits a disabled candidate only when an explicit event unlocked it, preserves that promised destination inside the unchanged card cap, and leaves enabled Tier-2 priority unchanged. The low-bankroll regression requires the exact affordability reason at $63 against the $70 Grand route; the replay stops and reports public cash/cost/reason instead of spending on unrelated travel. Route costs, economy math and RTP are unchanged. Source/semantic/full static checks pass; focused engine evidence waits behind rw06_1's serialized run.
- 2026-09-23 release orchestrator: rw06_1 pushed Small Underground Casino casino-layer checkpoint `67b61350`. The exact GU cap moves only `base.group.01` and `base.surface_item.03` to authenticated overflow, preserving both doorways, the floor/game/layer fixtures, behind-counter staff, Silas and both wall records; both simplifications are logged for 0.6.1. The map now has zero static errors, with 61 active snapshots / 417 bindings / zero overflow and 108 complete snapshots / 469 bindings / zero overflow; two regenerations were byte-identical (SHA-256 `E9666F504B7F3491360938643D36C5CDD85D728F4ACF6C40F78A64906DB33C9D`). Full known red fell from 5,332 to 3,933 without changing the global active overflow count of 38; report SHA-256 `52578A62D13A664E37E7FEEED29C0707DEBFACE701417D5E14063726F0E5173B`.
- 2026-09-23 release orchestrator: rw06_2's first focused attempt at `91fad55c` invoked the inherited Foundation source directly and was correctly rejected during parse (`JsonCoerceScript` duplicates the parent member), before a report or product assertion existed. This is an invocation-only failure and does not change the product classification. Evidence: `.tmp/rw06_2/focused-91fad55c-content/20260923-131509-394`, stdout SHA-256 `8A9286B40E2BC60D875F8F46649840C334AC7E914F816E9126B597E1E66AC52E`, Godot log `475A808B814B9EF0DC1730F6E69F37CDFDF72B173508F792BDC706C5434C7101`, empty stderr. Exactly one process ran and global Godot count returned to zero; the canonical generated split-runner path is being prepared engine-free without altering the test.
- 2026-09-23 release orchestrator: rw06_1 pushed Motel and Motel Room checkpoint `180c9805`. Both maps now have zero static errors; active mode proves 36 snapshots / 299 bindings / zero overflow / 84 actions, while complete mode proves 76 snapshots / 351 bindings / four intentional aftermath-only overflow records / 84 actions. The exact aftermath label anchor is deterministic, two regenerations were byte-identical (placement SHA-256 `5E14368281E310B758897A181379A8D3304545CAB9C000B2141B749614840216`), known full red fell from 3,933 to 3,928, and active global overflow remains 38. No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: rw06_1 pushed Pawn Shop checkpoint `c878c5da`. Pawn Shop now has zero static errors, with 12 active snapshots / 48 bindings / zero overflow / 42 actions and 48 complete snapshots / 84 bindings / zero overflow / 42 actions. Deterministic regeneration produced placement SHA-256 `721C1C17DDE51750177701758D17578A1DF58F66DDCAFA996B777CCC73AA6A2E`; known full red fell from 3,928 to 3,842 (report SHA-256 `E1680F4FC1FB04433845607CEFE6F7F8969052C8A56F9E2A197A9CF7FFA70A92`). No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: rw06_2's supported generated split-runner check at `91fad55c` passes the focused Foundation `content` assertion with zero failures (`executed_check_ids=[content]`, 211,645 ms). Evidence directory `.tmp/rw06_2/focused-91fad55c-split-content/20260923-132017-889`; report SHA-256 `954BCD9CD63FA2E5AFC5F1E92F8A0712163222A06960448C1A95FF661B7B2E32`, stdout `17027FE5B3468D682168484D9D4DAE4C5915825031A770B8DCD42BD114B617D1`, Godot log `19519FC4486063486D04CCBBA43D0FB4BFF0AB9327CF4CB38B8F9EF744947752`, generated runner `6E2F6FBC27D938AD88E0009FB19E71FA10ABAB387559FD72DB6BE9B6C0016A4D`, empty stderr. The Windows console wrapper did not expose a numeric native exit field, so the authoritative JSON (`passed=true`) and completion banner establish the result. One launch ran; its wrapper and child both exited and global Godot count returned to zero.
- 2026-09-23 release orchestrator: rw06_1 pushed Jazz Club checkpoint `d0a56a65`. Jazz Club now has zero static errors across 10 base records, 10 stage records, two exits and 69 preferences, with 73 active snapshots / 502 bindings / zero overflow and 129 complete snapshots / 563 bindings / zero overflow. Deterministic regeneration produced placement SHA-256 `502FB5714365FFF2719B06B69C204AD399E7FFD72E73577718B133C330FDE6B0`; known full red fell from 3,842 to 2,776, active global overflow remains 38, and the report SHA-256 is `5EF7EBA6C15E4792CD4CCDBDDBB445E067AF59825221DF48263736E0A89F7204`. No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: rw06_1 pushed Grand Casino checkpoint `d194897a`. The main Grand map now has zero static errors across 13 base records, 13 stage records, two exits and 67 preferences, with 54 active snapshots / 426 bindings / zero overflow and 96 complete snapshots / 486 bindings / zero overflow; its high-limit, back-room and cage maps remain separate work. Deterministic regeneration produced placement SHA-256 `0C510C3CEE23EE23CEABDB11A361C638F95235E6DCBA462A4443BDEB6AB64B32`; known full red fell from 2,776 to 2,748 and the report SHA-256 is `39CB407F95C2D5E2E8367D2164BE5F1CF7372D0BE650D9D1BF94D074990AEC00`. The four Motel aftermath-only authenticated overflow identities are now logged in the 0.6.1 backlog. No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: rw06_1 pushed Kitty Cat Lounge checkpoint `cabb5b1f` after exhaustive cap search proved that every zero- and one-cut plan is unsatisfiable. The minimal cap defers `base.standing_person.01` and `base.surface_item.03` to authenticated overflow, with both simplifications logged for 0.6.1; the map has zero static errors, 84 active snapshots / 569 bindings / zero overflow and 148 complete snapshots / 637 bindings / zero overflow. Deterministic regeneration produced placement SHA-256 `E13E24FEACD098CDD3AE777F75D40D4D8029F4388F8EA7FCA30DC3E255AC941B`; known full red fell from 2,748 to 2,289, global active overflow fell from 38 to zero, complete overflow is 37, and report SHA-256 is `1A6EA8C426BDB2C42EAFED4F214F5DE32F9F2CEAD00FEDD3E25846279A70B1B1`. No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: rw06_1 pushed Beach checkpoint `e8d2d2b9` after exhaustive search over 18 complete-state cap vectors proved the baseline and every one-cut plan unsatisfiable. The unique minimal cap defers `service_spots:0` and `service_spots:1` to authenticated base overflow, with both simplifications logged for 0.6.1, while preserving game-floor capacity and both doorways. Beach has zero static errors, 33 active snapshots / 304 bindings / zero overflow and 66 complete snapshots / 345 bindings / zero overflow. Deterministic regeneration produced placement SHA-256 `D3FD6D8001808E0888CF88D49279D80FFA747D78646E92B349E1F80233136D4D`; known full red fell from 2,289 to 1,490, active overflow remains zero, complete overflow fell from 37 to 27, and report SHA-256 is `E096D88700D918522B2C641E4217A746B8883729B2CFFFA3F2EA9B8948DCC939`. No Godot process ran for this checkpoint.
- 2026-09-23 release orchestrator: owner answers Q-006 and Q-007 supersede every computed/search-authored room coordinate as final layout evidence, while retaining the hard overlap, exit, reachability and hidden-state guarantees. rw06_1 stopped the Delta search before applying it and is reworking Bar, Corner Store and Grand Casino first, then every room, using hand-chosen coordinates aligned to the background art and named typed slot groups whose occupants alone randomize. People and props must be grounded on the matching surface, scenario slots must be fixture-local and disjoint from base objects, and Grand receives a complete table/machine-area redesign. Before landing, a new player-view three-room sheet without debug overlays must be asked through the owner questions file; Q-006 and Q-007 remain ANSWERED until the remediation is implemented.
- 2026-09-23 release orchestrator: rw06_2 pushed exploratory clean-route checkpoint `b0aee96f`. Its replay now handles the publicly visible Grand fare shortfall by entering Kitty's rendered slot surface, using at most 24 ordinary spins plus deterministic visible bonus/handpay controls, and retrying the exact Grand card once the public bankroll reaches the displayed fare; Nudge, autoplay, unrelated travel and private state are excluded. Engine-free replay-source and full static checks pass; one non-qualifying fixed-seed probe is authorized while final qualifying routes remain blocked on rw06_1.
- 2026-09-23 release orchestrator: rw06_2 probe 14 at `b0aee96f` stopped at action 36 with the same player-visible blocker, `No visible unvisited route can advance the Grand Casino invitation.`, before the new slot-fare recovery path could begin. No retry ran. Evidence root `.tmp/rw06_2/exploratory/clean-tip-b0aee96f-probe14/20260923-134033-285-24908`; run summary SHA-256 `8F02C84599143F741DE28BE7B4AF0C64E6174BB4D7AC3A1E272927E483DE0233`, public trace `FCA532C78B5D89EDCA4016B9A4FC38719C6F0107C32BF990D13976861ED88905`, money trace `7E552A23EE8A2BD5C49E42B832896FEE864C1E8276517E0DE4267B23EA1FFF1D`. The expected-failure rethrow left no parent summary; the per-run summary records the exact failure. Global Godot count returned to zero, and diagnosis continues engine-free against the immediate post-invite observation and map policy.
- 2026-09-23 release orchestrator: Q-007 remediation is split across two conflict-bounded rw06_1 worktrees from `e8d2d2b9`. `codex/rw06_1-phase0` owns Bar, Corner Store, Grand Casino and its auxiliary maps plus integration/contact-sheet work; new pushed branch `codex/rw06_1-hand-other` at `D:\Projects\Beat-The-House-worktrees\rw06_1-hand-other` owns the remaining physical maps, keeping every Small Underground variant together. `placement_surfaces.json` remains the sole authoritative schema; no parallel loader/schema was committed. Both workers must hand-author literal art-aligned named slots, and both branches/worktrees will be merged and deleted before rw06_1 is DONE.
- 2026-09-23 release orchestrator: rw06_2 pushed root fix `7571c942` for probe 14's repeated Grand-route omission. Public trace inspection showed this was product logic, not observation timing or replay policy: after the exact invitation and settled waits, Grand was still absent while Delta and four protected revisits were visible. `_ensure_priority_targets` had derived effective priority from score-sorted candidates; it now honors the caller's declared progression order, replaces non-priority new cards first and then only lower-priority unvisited priority cards, and never replaces a visited route. An exact crowded-Kitty regression requires all four revisits plus Grand and excludes Delta. Replay-source and full static validation pass; no economy, game math, RTP or placement changed, and one focused `systems/content` proof is authorized before another exploratory retry.
- 2026-09-23 release orchestrator: rw06_2's focused canonical generated split-runner `systems/content` proof at `7571c942` passes (`passed=true`, zero failures, native exit 0, 239,806 ms), with exactly one launch and global Godot count zero before and after. Evidence directory `.tmp/rw06_2/focused-7571c942-split-content/20260923-140024-688`; report SHA-256 `0F73C768991284C4CB68245A66B6CA964F0D765D4CF353660627ECA5654B9456`, stdout `F6DC258919F5A9E142208482FC48D4107F9C0BB29979D7B540F2250548333309`, stderr `7E8F5DEB3BF520C8ABB23951DEAFB0669C320CEBB4CEB6668C755C617C5FD7D3`, Godot log `36B3CE82552788D1AB2371FB39E265EC01B4BF8F4CDECA4E581BC53789795F5A`, metadata `9FD58799B10CEFE50393B77069E1C13E6DB26351A7ABE3D60DE5A8CE8AD90C67`, generated runner `3E28512E22E8481AB233A00A99F58D88DBD5F286A6586C92D324F63E85CA9662`. The three `misc2` controller-mapping lines are standard; verbose leak output identifies exactly two RefCounted instances, both at reference count zero, matching the canonical gate's known generic ObjectDB allowlist, with no script error or unclassified leak.
- 2026-09-23 release orchestrator: rw06_2 probe 15 at `7571c942` still stopped with `No visible unvisited route can advance the Grand Casino invitation.` at action 58, before fare recovery, in exactly one launch with no retry and global Godot count zero afterward. Evidence root `.tmp/rw06_2/exploratory/clean-tip-7571c942-probe15/20260923-140637-564-28124`, session `.tmp/agent_playtest/2026-09-23/rw062-clean-28124-1-844e25c919`. The focused synthetic regression was narrower than production: the real map has two event-unlocked candidates, and the current first-event selection likely allows disabled Delta while excluding unaffordable Grand despite Grand leading the declared progression priority. Diagnosis and the exact two-event regression proceed engine-free before any further run.
- 2026-09-23 release orchestrator: rw06_1 hand authoring adds pushed branch `codex/rw06_1-hand-meta` at `D:\Projects\Beat-The-House-worktrees\rw06_1-hand-meta`, based on `e8d2d2b9`, to keep the Sep-24 timebox moving without overlapping core edits. The orchestrator owns only the `apartment`, `house` and `motel_room` map objects there; the non-core worker excludes those three, while the core worker retains Bar, Corner Store, Grand Casino/auxiliaries and integration. The branch uses the same sole-authority `placement_surfaces.json` schema, Q-007 hand-authored/art-aligned rules, serialized Godot lane and cleanup requirement.
- 2026-09-23 release orchestrator: rw06_1 pushed its first Q-007-compliant non-core checkpoint `95b7e1ef`, changing only `small_underground_casino:back_room`. The hand-authored named slots align the game and matching event to the planning table, the numbers event to its desk, the job-board and Mags events to their fixtures, the practice rig and Silas to the floor, Rook to the doorway, plus disjoint stage and exit positions. Per-object class overrides preserve those supports; the exact map validator reports zero errors and JSON/diff-scope checks pass. No Godot process ran.
- 2026-09-23 release orchestrator: rw06_2 pushed `8ddda237` after probe 15 exposed competing event promises. The narrow product fix keeps Grand in the disabled-priority allowlist only when the normalized Grand node is explicitly event-unlocked; an exact regression has both Delta and Grand event-unlocked with Grand disabled, Delta enabled and four revisits, and requires Grand plus every revisit while Delta is displaced. The replay-source contract and full static validation pass. Its one canonical generated `systems/content` launch also passes at exact tip (`passed=true`, zero failures, native exit 0, 240,503 ms), with global Godot count zero before and after and no retry. Evidence directory `.tmp/rw06_2/focused-8ddda237-split-content/20260923-141916-658`; report SHA-256 `8C2908F5E67E656EC192337463400B178CC33B3CD3A9BF34540FE0274D0AB1BD`, stdout `3D2A4ED9AE0E25E3C73C853A0BFB895853B431021D001DB309538D19DD1A0236`, empty stderr `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`, Godot log `EBA467FF408AEF8373B25DE2AC23292B265FA5C4DF50AFC4C2890F3D4D960533`, metadata `17B355C3CD17696EE277A71C707249D17DBD2019E3A5FF88071456A4B90A61F7`, runner `DFB1CBABC56ACC0CE9D960C2B9505E657853B1C813E517A491F90E9B8582B8DD`. Only the three standard `misc2` controller-mapping lines appeared; there was no ObjectDB warning, leak, script error or unclassified error.
- 2026-09-23 release orchestrator: the first meta-room art-audit capture invocation in the fresh `rw06_1-hand-meta` worktree stopped during parse because its ignored `.godot` cache lacked the registered global script classes (`UserSettings`, `ProfileInventory`, `MetaCollectionService` and related types). This is an invocation/cache setup seam and yields no product or placement conclusion; no retry ran and global Godot returned to zero. Evidence directory `.tmp/rw06_1/hand_meta_art_audit/20260923-1418`; Godot log SHA-256 `1BB1CA87988E85A55763D4B2A0A48FF749D6AC922525F0A07612E3B17A77E8D5`, stdout `8880351ABC1BE7E00A0268DF918BC88930C642709C1455D560E9D557AE15FDEB`, empty stderr `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`. The supported shard-cache contract identifies `global_script_class_cache.cfg`, `uid_cache.bin` and imported artifacts as the stable cache set for a fresh worktree.

- 2026-09-23 release orchestrator: Q-007 hand authoring reached three clean pushed checkpoints. Core `90eb9a54` replaces Bar and Corner Store with literal named fixture-aligned slots and exact serializer replay; both maps have zero focused static errors. Non-core `044198cb` completes every assigned map with zero owned-map errors and zero active overflow; Delta Queen proves 748 active and 856 complete bindings with zero errors/overflow, while its timebox-safe stationary actor route is recorded in the 0.6.1 backlog. No Godot process ran for the non-core work.
- 2026-09-23 release orchestrator: meta-home checkpoint `9e3865c2` changes only Motel Room, Apartment and House to hand-authored semantic home/travel/event slots. Each focused map validator reports zero errors, with zero generic ids and zero missing binding references. A cache-supported art-audit invocation completed six captures but exposed a capture-store seam: the three requested home selections all rendered House, so those images are not placement evidence and no retry ran. Evidence root `.tmp/rw06_1/hand_meta_art_audit/20260923-1429-cache-supported`; report SHA-256 `98D0633842DFE4DBCE1904D8C8BB1B11ABBF67C36EBA839896A32C162DD17BD5`, Godot log `026D18085E9F51FCC30BBD1D85A4CA60D9DF64C7193185230A78A7C976668FCD`; global Godot count returned to zero. Integration must also preserve the generated `home_container:<id>` binding when emitting its late `meta_container:<id>` record.
- 2026-09-23 release orchestrator: the first isolated Q-007 focused smoke at exact core tip `90eb9a54` stopped fail-closed on its first seed, Bar 009, when the six-visit route entered Corner Store. Normal and expanded validation found two real conflicts: `late_shift_discount` overlaps `scenario_delivery_day_stock`, and `travel:leave` overlaps `delivery_exit`, including labels. No later seed or retry ran. Evidence root `.tmp/rw06_1/bar_store_smoke_90eb9a54_lease1`; metadata SHA-256 `1D5E81F3B87B7D57A6FC4D2EDCE489E2494596842C0A6928E2421588FEC73360`, failed seed JSON `74AF301E4A5E422F282D7C162F7ACD2FBB912D6027F553B3B72A406FC80CAE1D`, isolated Godot log `FD71316EB32BF3419FD33CBBCB4754B7AA303D2B3E150043F0E799A197683B2F`; pre/post Godot counts were zero. Repair proceeds engine-free without weakening targets, overlap, exit or reachability guarantees.
- 2026-09-23 release orchestrator: rw06_2 probe 16 proved the invited Grand survives product target selection but disappears from the public map snapshot because `FoundationTravelViewModel.enriched_world_map_snapshot` rendered only `enabled` targets. Product commit `c3869acd` now renders a selected non-hidden Grand as disabled at bankroll $63 with the exact fare reason, keeps unrevealed routes absent and retains crowded-card/revisit assertions; its engine-free source contract passed (SHA-256 `80D0FBB01FC8AC836682ECEEBF10873B395DD5CB5ED301226F90C3CF6E877C05`). The first focused launch at that code tree is invalid evidence, not a product failure: it omitted isolated `APPDATA` and inherited global `developer_environment_placements.json`, exactly explaining all 13 Bar/Gas placement failures (invalid-evidence audit SHA-256 `0771D7EE42BE2878367888DFA3054546C1150BEE6EB56DB1DCBD9F11B1E233B5`). The single isolated rerun at pushed docs-only descendant `fbcefad6` passes authoritative Foundation `content` with zero failures in one launch: `.tmp/rw06_2/focused-fbcefad6-isolated-content/20260923-150543-183`, report SHA-256 `C44C4311556872659F61053022534C4A366F5BB33198E06E2EA5FACFCBC910E0`, stdout `2EF633B159E93C186C87D1FA2D8F0CF6900CC46C7B4D21BD1956CFF60936E7B3`, Godot log `F58140D0B03633D5D30265D38CFAD7E054779B66BB1DF510A745A3353E580DDF`, metadata `B8F2CDD9E02F42C54491B8F821C3D754963A0E2E0AC59238F069EFA11CB964E1`; pre/post Godot counts were zero. Windows did not retain a numeric native exit field, so the wrapper shell guard returned 1 despite the passing report; no retry ran.

- 2026-09-23 release orchestrator: rw06_2 Clean probe 17 at exact `fbcefad6`, seed `RW06-CLEAN-ROUTE-01`, advanced beyond the previously invisible invited-Grand blocker, then stopped fail-closed at action 36 with `A modal interrupted Grand fare recovery; the replay will not guess through it.` One isolated-`APPDATA` replay invocation ran with no retry and graceful cleanup; global Godot count returned to zero. Evidence root `.tmp/rw06_2/exploratory/clean-tip-fbcefad6-probe17/20260923-151816-929-11848/run-01`; summary SHA-256 `D5F68A22DF266CB58C2A0D0937BDE5BCBCE205B873702297DF19EB3D37943BCE`, public trace `321627303036D31F88830E5377893024CF587AA8499D99FBF209214022E25FD8`, money trace `64C52B5449F7CFC83F0658BCBB9442707AC9A946862BB518DACB0C1CF8A2D1F0`, action-36 observation `742AAC441208B933E5559CF7F2E6719FF3F0045A5AB6F537AE0608975BCE7FB7` and screenshot `7505B272FCCE97BF1BA3DD1B34E31C266A962732885FBFCDBE6DB3E6CCBDB344`. The modal is the normal authored public `machine_jam`, with enabled confirmation-gated `wait` and `push` choices; this is a replay-policy blocker, not a product arc break. The narrow fix handles only this exact visible event during fare recovery, chooses `push` at public heat ≤24 and `wait` above it, and fails closed on any unknown or malformed modal.

- 2026-09-23 release orchestrator: rw06_1 core checkpoint `6ac6c86e` repairs the two Store conflicts found by its first focused smoke and completes the literal hand-authored Grand main/high-limit/back-room/cage redesign. Exact changed maps since the prior core tip are Corner Store plus those four Grand maps. Core-owned focused static errors are zero; active Bar/Store/Grand scenarios prove 1,411 bindings with zero overflow, and complete Grand simulations prove 486 bindings with zero overflow. Store now asserts three distinct behind-register occupants, pairwise normal and 104×76 target separation, label separation, and base-left-travel label separation from every Delivery exit mapping. The completed non-core and home branches are being history-merged before the full engine-free gate.
- 2026-09-23 release orchestrator: rw06_2 pushed exact tip `b08f788c` for the normal public `machine_jam` encountered by Clean probe 17 during Grand fare recovery. The replay handles only that exact visible event, validates its public schema and unique enabled choices, chooses `push` at public heat ≤24 and `wait` above it with confirmation-aware dispatch, and fails closed on unknown, talk, ambiguous, malformed or unresolved modals. Its source contract passed two valid plus nine hostile fixtures (report SHA-256 `3CDD3002EB48DE8E0716B14AF4BB232749A2E0EDC7DB4B90F211C9232459AD80`) and full engine-free validation passed. At 15:47 CT the owner-questions file still had no item after Q-007, global Godot count was zero, and exactly one isolated non-qualifying Probe 18 was leased at this pushed tip; no retry or further launch is authorized.
- 2026-09-23 release orchestrator: rw06_1 exact pushed integration tip `022044d0` is clean and contains core `6ac6c86e`, non-core `044198cb`, home `9e3865c2` and the meta/capture fix `00f9ae3c` as ancestors. Repository validation and the literal 21-map serializer replay pass. Static evidence `.tmp/rw06_1/static/integration_all21_clean_capture.json` (SHA-256 `5001CCA83ECD730DDD6AC2A973A8718A66B830DCCBBBD27257857419BABE1C10`) reports zero errors across 21 maps, 18 archetypes and all 55 legal scenario hosts: 767 active snapshots / 5,452 bindings / zero overflow and 1,504 complete snapshots / 6,314 bindings / 27 authenticated allowed overflow records. The serializer SHA-256 is `FC904B5EA876D885F45022703F5AA08E42F2E468CAF53E9FD1576EA489B00830`. With global Godot count zero, the next lease is a fail-fast repaired five-seed smoke and, only if all five pass, one isolated clean player-view capture of Bar, Corner Store and Grand Casino in normal and expanded views; no Q-008 is posted until all six raw sources and the composed sheet pass both machine and visual inspection.
- 2026-09-23 release orchestrator: rw06_1's lease at `022044d0` stopped on the first Stage-A seed with no retry and no contact-sheet capture. `POSTFIX-UIENV-009-4151570434` reached Corner Store and found `event::event:town_rumor_staff` label overlapping `scenario::delivery_event_gate` in normal and expanded layouts, including the hard unrelated-control check. Evidence JSON `.tmp/rw06_1/lease2_022044d0/seeds/POSTFIX-UIENV-009-4151570434.json` is SHA-256 `240E0728BE1C6B6297ED80E937BDFA40026D72963161E19DD7AC243BD7A72F36`; log SHA-256 `FCE607610A807F0A8E8C0913A3713E17A3A924B1B90B55758E415723AC86D841`; pre/post Godot census was zero. A secondary ignored harness `AppendAllLines` overload error occurred only after the raw child evidence was written. Repair must add this exact legal composition to deterministic static coverage and move the literal label/slot without weakening any overlap, exit, reachability or hidden-state guarantee.
- 2026-09-23 release orchestrator: rw06_2 Probe 18 at `b08f788c` live-proved the normal `machine_jam` policy: at Heat 1 the replay selected public `push`, the popup closed, Heat became 7, and three further visible spins succeeded. The first later boundary was transport-only: a byte-identical fifth `click_action slot_spin 0` command was transiently unavailable to `FileAccess.open`, but the bridge converted null-open to an empty command, rejected it and consumed its ordinal. Pushed exact tip `f8c37e10` now leaves a null-open command pending without execution/result/ordinal advance while preserving rejection of a successfully opened truly empty file. Its final source contract (one valid plus four hostile command-open fixtures and existing suites) passed at SHA-256 `845610D4F64C7B3C243C36E9CFD647B1899C6F5BF07368C5133FE3C1D8380559`, full static validation passed, and one isolated no-retry Probe 19 was leased from a zero Godot census.
- 2026-09-23 release orchestrator: rw06_1 pushed clean repair tip `48a3e959`. Deterministic red evidence reproduced exactly one seed-009 legal Corner Store Delivery Day label conflict (SHA-256 `DCFDEE033A7B0E819F4130579A8F1172847AAEA0C16CC7CDE26107D25118E8C6`); the new fixture checks label-label and both directions of normal/expanded label-target clearance. Moving only `stage.event_wall_1`'s authored label anchor from y=112 to y=82, with its wall slot/target/support unchanged, made the same gate green with zero errors (SHA-256 `5AEA58D7F969D2F81A61458A176300330D88FE4C691422314BE9FC19C4241C19`). All-21 literal replay and full repository validation pass, ancestry of both parallel layout tips remains proven, and at 16:11 CT the owner-questions file still had no item after Q-007. A fresh isolated five-seed fail-fast lease is authorized from a zero Godot census; the clean capture remains conditional on all five passing.
- 2026-09-23 release orchestrator: independent read-only audit rejected `48a3e959` as the final static-gate tip before its authorized engine rerun launched. The checker replayed only `scenario::` visuals and modeled base slots as targets, so its 55-scenario/22-seed green result did not generically test active base-label rectangles against legal scenario labels/targets; the single seed-009 fixture proved the observed case but not the hard all-composition overlap guarantee. Lease 3 was cancelled during preflight, no Godot process launched and the census remained zero. rw06_1 is adding a fail-closed finite census of every legal active base-label + scenario binding composition in normal and expanded modes, both label-target directions, while retaining the exact seed regression and excluding impossible host/phase/state Cartesian products.
- 2026-09-23 release orchestrator: rw06_2 Probe 19 at exact `f8c37e10` live-proved the pending-command transport correction by passing Probe 18's command-0068 boundary and completing all 24 bounded spins without an empty-command or ordinal failure. It then stopped with bankroll $15 versus Grand fare $109 after starting at $63 and losing $48. Public trace inspection classifies this as an omitted recoverable route plus an overly narrow replay strategy, not yet a product economy wall: enabled public alternatives included positive event choices, multiple venues/games and disclosed family/Crew lender offers, while source audit shows outside debt does not block Players Card eligibility. Evidence hashes: launch `103FBAB00D6227B74A9237A5489883A8DE0785517F6E11D2C2E2F7B81669C1D8`, summary `AF647469BC70A4CEB63F3F3287D32B96A8E796077CAC35E66DF774BE030DD824`, public trace `F72B12BA13A315E5FDD6D3F4B857B1FF2869ADC85FF8F2BD3756D810DC2A1D81`, money trace `1BB9E5CC5033717D1BB9E1AB3DB8855BF540A1D672C560967B748BD09DFEB6EA`. Pre/post census was zero and there was no retry. Its generic ObjectDB warning had no verbose reference-count lines and is not canonically allowlisted, so the probe is non-qualifying. A regression-first funding helper may use only uniquely visible enabled public actions and disclosed positive-principal/obligation choices, verify public bankroll/debt changes, prevent repeat farming, and retain a capped loss-stopped slot fallback; no hidden state, game math, RTP, balance data or gate changes are authorized.
- 2026-09-23 release orchestrator: rw06_1 pushed red acceptance-gate checkpoint `320831d0`. The binding prompt requires every slot label/target pair to be disjoint in both layouts, including base slots, so an exact co-occupancy model cannot replace the conservative modular-slot envelope. The new finite gate covers 188 base slots, 4,205 five-direction base/base checks, 1,504 catalog-authorized reachable scenario snapshots and 315,445 base/scenario checks; it currently reports 113 base/base conflict kinds and 86 base/scenario conflict kinds. No placement coordinates were auto-generated. Bar and Corner Store are being hand-authored first while an independent read-only audit proposes literal Delta Queen and Grand-family anchors; remaining conflicts follow per map. Slots with no nearby art-associated label domain must use authenticated overflow with an exact 0.6.1 backlog entry. At 16:40 CT Q-006/Q-007 remained ANSWERED with no Q-008 or newer item, and global Godot count remained zero.
- 2026-09-23 release orchestrator: rw06_1 pushed literal Bar and Corner Store label-anchor checkpoint `e0e20ed4`. Both maps are green under the new conservative all-slot gate, all 21 serializer replays pass, and the global red set fell from 199 to 171: base/base conflicts 113→95 and base/scenario conflicts 86→76. Coordinates were hand-authored near their fixtures; no search-generated placement was accepted. Delta Queen/Grand and the remaining maps continue engine-free from this exact pushed tip.
- 2026-09-23 release orchestrator: rw06_1 pushed literal Back Alley, Beach, Motel and House checkpoint `13d5236b`; those maps join Bar and Corner Store as conservative-green. The global red set fell 171→151 (base/base 95→91, base/scenario 76→60), with the all-21 serializer still green. Delta Queen's `base.event_wall_1` has no possible nearby maximum-label anchor while retaining `base.event_wall_2` and `base.event_table_1` expanded targets; the worker is comparing occupant preferences and will defer the least-required capacity to authenticated overflow rather than move a target off its art. At 17:08 CT there was still no question after Q-007 and global Godot count was zero.
- 2026-09-23 release orchestrator: rw06_1 closed the conservative hand-authored placement gate at clean pushed tip `fc186f48`. All 21 maps replay; 185 base slots, 4,065 base/base checks, all 1,504 complete legal snapshots and 305,880 base/scenario checks are green with zero conflicts and zero active overflow. Gas Station, Delta Queen and Jazz Club each remove only one locally impossible low-priority capacity, preserve their independent exits and fixture authorities, remap the closed occupant/index set, retain every visible displaced action through authenticated overflow, omit hidden-only actions, and record exact follow-up in the 0.6.1 backlog. The sole Godot lane is leased to the five historical seeds at this exact tip, fail-fast with no retry; the six raw player-view captures and Q-008 remain conditional on all five passing.
- 2026-09-23 release orchestrator: rw06_1's exact-tip lease passed historical seeds 009, 061, 090 and 040 in order, then stopped fail-closed on seed 063 with no retry and no capture. Apartment→Gas Station finalization rejected `scenario::gas_station_tour_bus_stop_restroom_queue` for blocking the mandatory player access lane in normal or expanded layout; the run stopped after one environment and zero completed travels. Evidence root `.tmp/rw06_1/lease4_fc186f48`; metadata SHA-256 `E88D9C2752F0398C8B4933B3714C403ECB8638826E3E833AD9B21ED806A8F588`, failed JSON `E1C9A92319D55996F3422A444A6EEF18813A26E140817500B70D102BF688A7E1`, failed log `412948978F2674230206B25117BDEA19A03ED549B18A735BCBC39662C3E6D7AD`. Pre/post Godot census was zero. The exact composition is being added to deterministic normal/expanded lane coverage before an authored repair; no guarantee or gate is weakened.
- 2026-09-23 release orchestrator: independent read-only audit rejected rw06_2's uncommitted Grand-fare funding draft before another engine probe. The draft consumed the full debt model although the consequence panel is hidden and the HUD renders only a debt icon tooltip/count; its lender confirmation admitted changed terms/controls, its cash-event fixture fabricated `text` where the live canvas publishes `detail`, the live mouse path could accept disabled inline actions, any stale positive bankroll result could satisfy the cash postcondition, and TalkDock exposed full lender terms before its visible typewriter completed. The branch remains at pushed evidence tip `f8c37e10`; no new Godot ran. Repair must use genuinely rendered feedback, bankroll and HUD-icon projections, bind results to the selected event, make mouse/keyboard disabled behavior agree, wait for visibly complete terms, and fail closed on every merge or unverifiable state with production-shaped hostile fixtures.
- 2026-09-23 release orchestrator: rw06_1 repaired the exact seed-063 Gas Station lane failure without weakening a guarantee and reached clean pushed tip `8a7a1ae8`. Independent engine-free validation passed: 21 maps, 55 legal hosts, 767 active and 1,504 complete snapshots, exact active/complete overflow 36/63, 303,450 base/scenario plus 4,065 base/base conservative pair checks with zero conflicts, and 750 mandatory-lane checks. The serialized isolated no-retry Godot lease then passed historical seeds 009, 061, 090, 040 and 063 in order with zero pre/post processes (`.tmp/rw06_1/lease5_8a7a1ae8/smoke_metadata.json`, SHA-256 `119F914FC3EB9C007EF45448815D51DC29D97434B3C588B69ADABD2CF37A96AD`). A fresh six-image player-view capture passed its report/hash assertions and was visually inspected; Q-008 now links its sheet (SHA-256 `40CF5E5B771C9A8BEF01562E486CAC5A6429434BA2DF2D3FF1C24C0C5500E6BF`). The capture log records the tool's generic ObjectDB exit warning; it is review evidence, not a qualifying substitute for the remaining acceptance gates.
- 2026-09-23 release orchestrator: owner Q-008 rejected the `8a7a1ae8` player-view sheet and superseded zero-visual-overflow as a goal: abstract tasks, zones, routes, barriers, ledgers, seals and similar nonphysical concepts must use the authenticated room action list, while only people, games, shop items and real props with art occupy room slots. The blocking rework applies to every room, includes real counter occlusion and a complete art-aligned Grand Casino regroup, and requires a new primary-checkout review image plus owner approval before landing. The rw06_1 writer is working engine-free while rw06_2 uses one serialized Godot lease; the Sep-24 timebox and overlap, exit, reachability and hidden-state guarantees remain unchanged.
- 2026-09-23 release orchestrator: rw06_2 pushed hardening checkpoint `1c1642db` after all engine-free source/semantic contracts and full repository validation passed. One explicitly leased Godot 4.6 public-observation contract then passed in a single launch with no warning, error or leak; report `.tmp/rw06_2/public_observation_contract.json`, SHA-256 `F8F699846DB1CFD6C5659F7A9D10F42C77CAB4F8573C4C5EE19D28E683889195`, pre/post process census zero. The commit binds physical clicks to an unchanged fully rendered live object, uses only public rendered feedback and exact save acknowledgements, and adds production-shaped multi-lender fixtures. The rw06_1-owned local Pixel canvas dependency was deliberately excluded and must be reconciled after rw06_1 lands before qualifying routes.
- 2026-09-23 release orchestrator: applied owner Q-009. The existing rw06_2 worker stopped without launching Probe 20 and handed pushed tip `1c1642db` to the separate rw06_2/rw06_3 peer through `rw06_2p_status.md`; the release orchestrator will no longer edit those rows. Q-009 also replaces machine-wide serialization with up to four properly isolated focused runs coordinated by live-PID lease files, while full suites, performance/timing, soak and rw06_4 remain exclusive. The shared lease directory is `D:\Projects\Beat-The-House-worktrees\.godot_leases\`.
- 2026-09-23 release orchestrator: rw06_1 is WAITING Q-010 only for safe engine access after an unleased Godot 4.6 process appeared against the primary checkout at 20:27 CT with no surviving launcher. No unknown process was terminated and no overlapping run was started. Engine-free Q-008 corrections continue; the shared patch now uses exact stable-ID plus closed renderer authority for physical scenario props and sends unmapped abstractions to More room actions.
- 2026-09-23 release orchestrator: accepted binding Q-011. Beach must always be a visible and enabled destination from the Delta Queen whenever normal travel is available, independent of seed, scenario, discovery, revisit and save/Continue. A separate `codex/rw06_1-beach-route` worker owns the root-cause production travel/UI regression; rw06_1 layout work must preserve a reachable boat-to-Beach control, and the peer owns matching ending-route assumptions.
- 2026-09-23 release orchestrator: rw06_1 pushed semantic-room checkpoint `6a3f2cc1` and integrated Grand correction `da812b76`. Exact closed authority retains 45 physical art-backed scene IDs and 2,865 physical in-room bindings while routing 2,587 abstract records to More room actions; 767 active snapshots / 5,452 bindings, 150,545 base/scenario pairs, 4,065 base/base pairs and 68 mandatory lanes are conflict-free. Grand's host correction reduced the deliberately fail-closed static remainder from 29 to 28; Bar/Corner and the other rooms own those bounded contact/no-slot findings. Focused Godot remains blocked only by Q-010, not by paper audit.
- 2026-09-23 release orchestrator: the rw06_2 peer reported Q-011 route-harness coverage for fresh Delta Queen arrival, revisit and Continue: two valid policy cases pass and 11 hostile states fail closed without manual unlocks or injected destinations. This is replay-policy evidence only; the independent rw06_1 Beach worker still owns the production visibility/save migration, capped post-priority selection and real UI regression.
- 2026-09-23 release orchestrator: shared rw06_1 tip `f4ce1dc6` seals renderer overflow authority and completes the Grand, Bar and Corner lanes. The real controller, More room actions, composed canvas and renderer-fallback regression retain abstract public actions without room geometry; Bar/Corner assigned findings are zero and the global static remainder is exactly 18 unchanged all-other findings. Grand and Bar/Corner branches/worktrees were verified as ancestors, then deleted locally and remotely. A dedicated all-other lane is applying the independently proven support/stationary recipe without physical overflow or route invention.
- 2026-09-23 release orchestrator: owner chose Q-010 B and closed the unleased primary Godot session; Q-010 is RESOLVED and isolated engine work resumed. Q-011 checkpoint `d3113877` is pushed with source/current-Delta Beach migration, post-priority capped protection and production UI/save regressions; engine-free validation and diff checks pass. Its branch remains unmerged until the same new tests fail on the pre-fix parent and pass on the fix under isolated Godot.
- 2026-09-23 release orchestrator: peer status reports Clean Probe 27 cleared the prior funding-order wall and stopped only because a Result panel covered the next lender action; the harness now restores the room surface through a real map round trip. Cheat hardening is harness-ready/live pending at reported integration `50c55788`, heist audit is active, and the Q-011 replay matrix now passes 2 valid cases and rejects 13 hostile states including four-card eviction. Production Q-011 evidence remains orchestrator-owned.
- 2026-09-23 release orchestrator: rw06_1 integrated the all-other-room lane at clean pushed tip `492e96ff`, then deleted that lane's local and remote branch and worktree after ancestry verification. The exact engine-free census covers 21 maps, 18 archetypes, 55 legal scenario hosts, 767 active snapshots / 5,452 bindings, 150,775 base/scenario plus 4,065 base/base conservative pair checks, and all 68 mandatory lanes with zero conflicts or active nonphysical room records. Twelve fixture-supported counter contacts and two stable-ID standing actors close the final 18 findings without route invention, physical overflow or weakened overlap, exit, reachability or hidden-state guarantees.
- 2026-09-23 release orchestrator: at 21:18 CT an unleased Godot console/child pair reopened against the primary checkout after the Q-011 fail-before run's zero-process preflight. The isolated Q-011 pair plus that primary pair reached, but did not exceed, Q-009's four-process ceiling; the already-isolated run may finish, the rw06_1 focused lane remains in preflight without launching another engine, and Q-012 asks the owner how to clear the renewed primary session. Engine-free work continues.
- 2026-09-23 release orchestrator: peer Clean Probe 28 exposed an rw06_1 production projection defect at the Motel wedding scenario: the live resolver omits unavailable `lender:brother_in_law`, but sealed layout records resurrect it as rendered/enabled while the valid `event:call_brother_in_law` Counter Phone remains hidden; the physical click returns “That lender is not available.” A dedicated engine-free lane from `492e96ff` is adding production-shaped regressions and repairing `restore_live_presentation_fields` so absent sealed services/lenders stay absent and present records use live availability/action fields. The peer separately integrated its Heist production fix as `cb3886d6`; both focused Heist contracts pass, while natural Bishop/Audit Night reachability remains live-route work.
- 2026-09-23 release orchestrator: owner answered Q-012 option A and authorized closing the renewed exact primary Godot pair. Revalidation found PIDs 19308/22260 had already exited, so nothing was terminated; Q-012 is RESOLVED and the machine contained only the properly leased Q-011 pair. The Q-011 red→green sequence and the isolated rw06_1 focused contract may continue within Q-009's four-process ceiling, while final visual evidence still waits for the Motel projection repair to join the shared tip.
- 2026-09-23 release orchestrator: peer tip `5f5955b2` repairs both authored Count exits to the canonical Delta Queen, preserves their distinct pressure/deadline values, removes arbitrary first-node fallback and applies Q-011 after either exit; its exact-target, real `clean_sweep` arrival and missing-Delta no-mutation regressions plus full engine-free validation pass. rw06_3 reconnaissance at `96877199` parsed all 53 data files and found no evidence-backed tuning change yet, so balance remains data-only and deferred until complete 3/3 route curves exist.
- 2026-09-23 release orchestrator: Q-011's broad pre-fix systems launch timed out in an unrelated shard before `world_map_foundation` and is explicitly invalid/uncited. Its focused replacement at pre-fix `b8f0a073` plus test-only `fd25c26d` is a meaningful red: only `world_map_foundation` ran and failed the new hidden/locked plus capped-competition assertion because Delta Queen offered Grand Casino, Kitty Cat Lounge and House but no Beach (report SHA-256 `4162F084D958D8568548F00BBEA0F8CBFB8BA435798A9C7D042B7B27896EDFFF`). Fixed-tip Foundation and full production UI/save greens remain required.
- 2026-09-23 release orchestrator: rw06_1's focused suite at `492e96ff` passed validation/import/load checks and 20 standalone contracts, then failed one unchanged semantic-authority assertion for the Delta Queen navigation lamp. Policy validation accepted its reviewed `stable_id|...` slot binding while `scenario_visual_art_key` did not. Narrow pushed fix `d5968792` changes only that lookup to recognize the same exact stable-ID composite authority; writer/static/diff checks pass and the identical focused suite is rerunning before integration.
- 2026-09-23 release orchestrator: Q-011's identical focused `world_map_foundation` check passes at exact fix `d3113877` with zero failures and empty stderr (report SHA-256 `67DAA20B0786D4F170F2BC41E423EFEF4EB771E7C4FE5C8F80B4B9BED2FF87C2`). The first full production UI/save run then stopped only at its new setup precondition: a preceding zero-bankroll proof left just Grand Casino enabled, so the fixture did not actually create more than three competing non-Beach routes. No product assertion failed. The amended fixture must carry sufficient real bankroll through Continue, reassert more than three production-enabled competitors after reload, and rerun both focused Foundation and full UI green before merge.
- 2026-09-23 release orchestrator: shared rw06_1 tip `0320d3f6` now contains exact-authority fix `d5968792` and live-projection fix `240707ce`; combined writer/static/diff/full-validator gates pass and both completed lane branches/worktrees were deleted after ancestry proof. The unchanged targeted overflow/UI contract passes, but the broader focused wrapper next exposed `env06_6_cluster_contract`'s stale hand-built fixture: it omits production slot bindings and normal/small label rectangles, so initial finalization fails and later missing-state errors cascade. The fixture is being repaired through real fixed-slot authority without suppressing the gate; final captures remain blocked.
- 2026-09-23 release orchestrator: peer engine-free reconstruction at `81dbf503` measures Clean's deterministic route at $126 after the verified Alley, Motel and Crew gains versus $109 Grand fare plus a $50 chip reserve, leaving a $33 deficit. The replay now consumes only deterministic current-stop liquidity, reports exact public cash/fare/reserve/shortfall, and no longer treats Slot luck as qualifying recovery; source/hostile/parser checks pass. Probe 29 is one no-retry live confirmation after Q-011 releases the lane, and no balance value changes before that measurement.
- 2026-09-23 release orchestrator: resolved Q-011 at merge `b84f6ee4` from exact tested tip `8fcd5bd2`. The focused pre-fix check failed only the hidden/locked Beach-under-cap assertion (report SHA-256 `4162F084D958D8568548F00BBEA0F8CBFB8BA435798A9C7D042B7B27896EDFFF`); the exact-tip focused check passed with zero failures (report SHA-256 `177D65FE024E887AF3245011A1B231718DF68DFD68EDED7CC9F09B489D032545`) and the full production UI/save/Continue runner passed with empty stderr (launcher SHA-256 `B0176C94D6C2063A0F7F20ECB45B7EB9E7F4B063AEF0CA6615CC4DA9C2410B97`). Coverage proves fresh/revisit/legacy Continue access, exactly one Beach inside the normal three-card cap after all priorities, free Walk after reload, visible-disabled global locking and one generic `travel:leave` control. Post-merge validation passed; `8fcd5bd2` is an ancestor of `origin/main`; both Q-011 worktrees/branches were deleted after evidence was preserved under `.tmp/rw06_1/q011_evidence`.
- 2026-09-23 release orchestrator: rw06_1 repaired the stale lifecycle fixture without weakening its contract at shared pushed tip `69648844`; writer/static/diff/full-validator pass and the unchanged targeted `env06_6` finalization contract passes in 55.5 s with `ENV06_6_CLUSTER_FAILURES_JSON=[]`, empty stderr and no leak warning. The full focused wrapper is running on the same commit. The rw06_2 peer also integrated the Cheat barred-Peek exit and exact Rourke choice at `5e9f2a14` / pushed tip `d4846acb`; its strengthened source/hostile checks and full engine-free validator pass, while live Cheat reachability remains pending.
- 2026-09-23 release orchestrator: the first broader rw06_1 gate at `69648844` is RED before Godot and is not qualifying. `validate_project` was functionally clean but took 276.936 s against the unchanged S0.1 limit of 243.150 s, producing exit 126; no later stages ran (summary SHA-256 `3D235797B0099D7549D9EE2286B0ED91BBF9623764682372A4B52E9B47F2BC36`). Its launcher also called `check_godot.ps1 -Suite Smoke -AllowConcurrentGodot` under a normal lease, contrary to Q-009's mandatory EXCLUSIVE policy for every Smoke run. The timing budget remains unchanged. The next attempt will merge current `main`, acquire `EXCLUSIVE.lease`, remove concurrent mode and run only after the active peer probe and all heavy validators clear.
- 2026-09-23 release orchestrator: acceptance audit rejects the existing 22-seed artifact as evidence: `.tmp/rw06_1/exact_seed/summary.json` is `passed:false` (SHA-256 `4F2AEB141810E4C3BE947F9D95FDD60CC06ED987B04C400959B63F479D9D5878`) and all 22 result JSON files are missing because its logs report Godot unavailable/audit skipped. The older Lease5 proved only seeds 009/061/090/040/063 at pre-Q-008 tip `8a7a1ae8`; static enumeration is not a six-visit production run. After the preliminary fixed-slot gate is root-fixed and green, all 22 historical UIENV seeds must run through the real six-visit runner, followed by the binding `environment_generation_audit.ps1 -Runs 100 -Visits 6 -RequireGodot` gate.
- 2026-09-23 release orchestrator: peer Clean Probe 29 ran once with no retry and stopped at action 63 with public cash $126, current Grand fare $95, required chip reserve $50 and a measured $19 shortfall (initial requirement $159). Its ObjectDB leak warning makes it exploratory/non-qualifying, but it replaces the engine-free $33 estimate with live public evidence; the evidence root is `.tmp/rw06_2/exploratory/clean-probe29-20260923-221142-103-27900`. Both Godot processes and its lease exited, releasing the machine for rw06_1's corrected EXCLUSIVE Smoke rerun.
- 2026-09-23 release orchestrator: the corrected exclusive preliminary gate at combined pushed tip `755d2d71` restored `validate_project` to 182.144 s within the unchanged 243.150 s limit, but is RED after 28 stages: `env06_6_full_contract` exited 1 after 228.194 s with 25 failures rooted in stale label rectangles, binding/canonical identity, declared target/tombstone/context/aftermath, actor anchor and exact Grand route fixtures. Summary SHA-256 is `00DCBCAFC2163080E035912D9BE0E65C4A88662B3F2BF138F7C433C8DEE4A1B5`; stage stdout/stderr hashes are `D10752D23DA4F1D8770C2D95DC64EDCBFF9D83B63EF9995702F97B78FBB1E3CC` / `D58906115BE55817D1021C1565B2FFC2E755586E6267A839DF0152A254EB31DB`. The accessibility and overflow contracts passed their assertions but each leaked two `RefCounted` instances; those diagnostics are blockers even though the wrapper did not count them. No downstream seed/audit/capture run started.
- 2026-09-23 release orchestrator: final rw06_1 acceptance remains: final-tip static validator; real 22-seed × six-visit runner; 100×6 generation audit; primary Q-008 Bar/Corner/Grand base+busiest normal-player 3×2 sheet; separate all-18 busiest normal+expanded sheet plus every-other-room visual pass; owner approval; full EXCLUSIVE Smoke and Contract on the final candidate with unchanged idle/performance; merge and independent merged-main gates; close seven placement P1 plus one P2; archive the prompt; then delete phase0/hand-meta/hand-other branches/worktrees. No final all-room sheet exists. The harvest disposition must replace stale overflow 36/63 claims with the Q-008 authority census (2,587 active / 3,191 complete nonphysical rows routed to More actions) and document hand-authored coordinates plus Q-007 writer-only tooling.
- 2026-09-23 release orchestrator: committed owner question Q-013 alone at `b8917b24` and marked rw06_2 `WAITING Q-013` for the real Heist Audit seed/persistence choice. Peer tip is `0379e92a`; Probe 29 launcher/summary SHA-256 values are `9D6E605D10D75D326427F205D84A21FDC889DB06AF6A15E05E37D8D35C5B67BB` / `E7DE8F1FD0E0B557007C43674315BADCFF865F04ED49CB6FBE99FD5D9C831542`, but the run is non-qualifying both for its ObjectDB warning and its acknowledged unstaged rw06_1 Pixel canvas patch. The 276-line plus untracked-launcher Heist seed-preflight worktree remains preserved and uncommitted/unpushed pending the answer.

## Rules every row inherits

- Owner decisions only go through `rw06_owner_questions.md`. Ask, then keep working.
- Never weaken a test, budget, idle-liveness floor or deterministic assertion to
  get to green. An idle draw cost of 0.000 without its liveness counter is a
  failure.
- Everything is seeded and happens at action boundaries, never on wall-clock
  time. Every consequence fires exactly once across save, reload, travel and
  revisit.
- No hidden-state leaks: Turn, traitor, rigged-draw and unrevealed-ticket
  information never appears in scene data, saves, captures or audio.
- A run that ignores the Crew must be a true no-op for Crew systems.
- Scope discipline: if something doesn't block this week's goal, it goes into
  `../plans/0.6.1_backlog.md` with enough detail to act on later. It is never
  silently dropped.
- Q-009 Godot policy: at most four isolated focused runs may overlap, each with
  its own worktree, `APPDATA`/`LOCALAPPDATA`, log, live-PID lease and owned
  cleanup. Full Smoke/Contract/Full, performance/timing, soak and rw06_4 are
  exclusive and run only after every other lease and Godot process clears.
