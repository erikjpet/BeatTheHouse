# Agent Prompt — fix06_21: Restore Main Smoke Health and Fail-Closed Post-Land Verification

Board row: `fix06_21` in `docs/todo/README_0_6_board.md`.

## Authority and scope

Work from exact red main `3f5e990750a8e2809f556c4e01a3d60972f49f21`
on an isolated named branch/worktree. This is an infrastructure/evidence row.
It authorizes no product, runtime, economy, physics, geometry, budget, assertion,
or unrelated fixture change.

The owner authorizes updating exactly the two stale SHA-256 expectations in the
inactive-delivery ordinary-travel UI compile fixture, retaining every full-state
and scalar assertion. Before editing, recursively compare the serialized content
covered by the old and new hashes and prove that the sole leaf delta is the
fix06_7 Quarter Falls copy change introduced by source `737a8fe9` and first-parent
landing `040f9fe2`. If any other content differs, stop without changing hashes and
report the exact extra delta.

The correction commit message must record both full old hashes, both full new
hashes, the exact content diff, source and landing commits, and the fact that no
assertion was removed or weakened.

## Post-land process correction

Determine and report exactly what the current post-land verification actually
runs. Correct the authoritative path so post-land verification fails whenever
the exact current main is red, including this retained Smoke UI failure, and so
no row can be marked DONE against a red main. Preserve stage output, exact tree
identity, native-plugin identity, functional failures, timing failures, and
stderr rather than summarizing a red stage as accepted. Do not change suite
budgets or omit expensive stages to obtain green.

Keep the two-hash fixture correction and post-land fail-closed process change in
separate logical commits. Run the focused reproduction and the authoritative
post-land gate on the exact candidate. Return an immutable head and complete
evidence manifest to the dedicated Integrator for independent review, gating,
and landing. Never push, alter remote/release state, delete files, or stage owner
artifacts under `.tmp/`, `.tools/`, or `review_artifacts/`.
