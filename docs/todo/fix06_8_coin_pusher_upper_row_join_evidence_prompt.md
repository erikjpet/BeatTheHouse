Status: AUTHORIZED — TOOLING-ONLY PROOF
Board row: `fix06_8` in `docs/todo/README_0_6_board.md`

# Agent Prompt — fix06_8: Deterministic Upper-Row Join Evidence

Resume from preserved provenance branch `codex/land06-fix06_8` at `c821d5a6`.
The owner selected Option A: prove the required adjacent-stock motion through a
deterministic production control/nozzle trace against unchanged production
physics and geometry.

Use the exact production-entry settled snapshot, production action/resolve/drop
queue, and live-session advancement paths. Supply a one-period no-input control
that proves the opening remains parked. The committed trace must prove an
accepted emitted drop, platform-rooted first support beside qualified
pre-existing platform-rooted stock, and movement of those exact adjacent coins
over a phase-matched full stroke. Do not seed-search, weaken adjacency, accept a
support-only neighbor, or infer motion from platform phase.

This row authorizes evidence tooling only. Do not change the nozzle, geometry,
transport, contact, neighboring-stock behavior, solver, renderer, machine data,
tuning, RTP, EV, payouts, RNG, schema, migration, goldens, or budgets. If a
fixed deterministic production trace satisfies all preconditions but cannot
demonstrate adjacent-stock movement, preserve the exact red evidence, classify
it as a product-physics defect in its own new row, and escalate to the owner.
Classification alone authorizes no physics correction.

Run static validation, focused and full foundation gates, determinism, native/Web
parity where the existing row requires it, actual-GL capture with inspection,
and the standard performance probe. Build and hash the native addon before any
engine timing. Return an immutable head and evidence manifest for independent
Integrator review. Never push, alter remote/release state, delete files, or
stage `.tmp/`, `.tools/`, `review_artifacts/`, or other owner artifacts.
