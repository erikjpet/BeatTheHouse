Status: TODO
Board row: `fix06_8` in `docs/todo/README_0_6_board.md`

# Agent Prompt — fix06_8: repair Plan 9.4 upper-row join production fixture

Work in `D:\Projects\Beat-The-House` on a clean branch from the exact current
`main`. Read the current board/landing contract, the pusherv3_11 closure report,
Plan 9.4, pusherv3_9 and pusherv3_10 records, the production live-session/drop
path, and the full capture tool before editing.

The retained post-pusherv3_10 `upper_row_join` capture is red for all three
machines. Every paid drop is accepted and platform-rooted and every PNG saves,
but `advanced_existing_body_ids=[]`. Earlier pusherv3_9 evidence advanced 68
bodies per machine. pusherv3_10 changed opening stock to settled, full-width and
idle while `_capture_upper_row()` did not change, so the leading diagnosis is a
stale fixture, not permission to waive the physical-neighbor requirement.

Change capture/test tooling only unless corrected proof establishes a product
physics defect. Deterministically choose or prepare a legal production nozzle
and motor phase beside platform-rooted upper stock; use the actual committed
production drop and live session (the first accepted drop starts the motor).
Record before, landing and after states. Assert:

- the dropped body is platform-rooted through its real support graph;
- a specifically named neighboring pre-existing body has a real neighbor
  relationship and advances forward within a declared horizon of at most three
  complete cycles;
- the same no-drop idle control does not advance that neighbor;
- normal and reduced-motion captures both exist, and every machine publishes
  `upper_row_join=true` in a fail-closed manifest.

Do not remove or weaken advancement, flip a displacement sign blindly, choose a
lucky run, refresh a golden, inject a synthetic solver event, or replace the
production path with a direct solver shortcut. If no legal deterministic
production setup can prove the contract, stop and route a product-physics defect
with the complete failed evidence instead of changing gameplay here.

Hard boundaries: no tuning, RTP, EV, payout, odds, wager, RNG, schema, migration
or performance-budget change. Run exact validation, focused Coin Pusher, the
targeted normal/reduced capture and the unchanged broad Plan 9.4 manifest using
the qualified ignored native addon. Record its source/hashes and every red and
timing result. Commit logically, obtain independent code and visual review, and
land only the reviewed net payload.
