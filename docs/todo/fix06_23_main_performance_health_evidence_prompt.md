# Agent Prompt — fix06_23: Current-Main Performance Health Evidence

Board row: `fix06_23` in `docs/todo/README_0_6_board.md`.

Exact current main `a62f48840686c6b5d09a6e5670bb537fd86e15bc`
is content-identical to hash-correction source `e3f6e94a` but retained post-land
Smoke is red in `foundation_performance_probe`. Preserve the full original red
summary and every targeted control. This row is evidence-only unless a separate
owner-approved defect row is later opened.

Using an idle qualified host, the exact same main tree, exact Godot identity,
and a newly built/hash-recorded native plugin, reproduce the complete
performance probe and the targeted Coin Pusher sequence. Retain every attempt,
including environment focus, idle surfaces, game resolve timings, carriage,
drop, skill-stop, collect, frame, draw, and liveness results. Compare exact
source/merge tree identity and explain why the targeted `3f5e9907` control was
green at 47.980s, `e3f6e94a` was green at 47.039s, and `a62f4884` produced both
wrapper and targeted reds.

Do not change code, product behavior, tests, budgets, multipliers, baselines,
physics, geometry, or tuning. Never discard or relabel a red. If exact main is
reproducibly red on an eligible idle host, open the smallest evidenced product
or infrastructure defect row and escalate before implementation. If an exact
eligible full Smoke is green, retain it alongside all reds and return the
health classification to the Integrator; only the Integrator may decide landing
eligibility under the binding post-land policy.
