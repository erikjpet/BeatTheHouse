# rw06 final queue

Rules: `rw06_final_push_prompt.md`. Claim by putting your lane in Owner and setting State to IN PROGRESS. Edit only your own rows. Never claim a task whose Needs are not DONE.

| ID | Task | Needs | Owner | State | Notes |
| --- | --- | --- | --- | --- | --- |
| T1 | **Heist ending.** Reach the Crew heist win screen through normal play and save `.tmp\owner_review\ending_heist.png`. Narrow to one plan or fewer recruits if needed; log the cuts. Includes merging the Switch overflow handoff. | — | C | DONE | continue from `codex/reset-c-heist` (worktree `reset-c-heist-part3`); Lane B aligned Punchline Crew Back Room access with the release Count's Associate architect gate, removing the hidden five-job grind. Lane B pinned Bishop's authored placement to `standing_person` across all three Grand rooms, preventing sealed-layout validation from misclassifying the live contact as a fixture and suppressing the pending Crew handoff. Lane B split Cass and the Host Desk across accessible right/left targets on the same authored desk so their normal and expanded small-screen authority stays disjoint, authored the delivery sequence's `center` zone in High-Limit and Cage, and authenticated Cage shelf records against their exact saved stock row so normal interior travel cannot fail closed before a pending handoff appears. Lane B pinned every declared Punchline layer transition to an authored doorway slot and doorway class, replacing the casino layer's erroneous shared floor-fixture binding so the Club and Crew Back Room controls remain hittable after saved-layout regeneration; Lane B then consolidated the Back Room's duplicate class override objects so its casino exit actually retains that doorway authority after JSON parsing. Lane B made the Planning/Live Table leave rows true UI dismissals and preserved that dismissal authority through the projected inline room-action choices, so every table path clears the event modal, selection, focus, and event screen instead of advancing a no-op Crew event. Lane B routed the visible inline event rows through that same dismissal-aware response handler, so the native Planning/Live Table Leave row no longer bypasses the dismissal contract and leaves a stale EVENT selection. Lane B kept every live delivery contact's authenticated handoff action in More room actions, giving deadline-bound package jobs a reachable fallback when person arrival or another room sequence temporarily owns the canvas hit plane. Lane B centered the Delta Queen staff label on its authored table slot and moved the Crew lender label below the Rowdy Regular's expanded target, separating both pairs in normal and expanded layouts so sealed presentation can attach pending delivery contacts. Lane B made the Count's exact planning-to-Grand, return, live-play, and marked-getaway legs crew-covered while that score is active, removing repeated $70 fare walls without changing any global route price, wager, deadline, odds, or payout; the Planning Table now also discloses the Grand Heat 40 hold limit. Lane B made modeled delivery expiry/capture a committed gameplay result instead of a failed travel transaction, preventing the last-action Count schedule from rolling back into the same permanently enabled dead-end route and surfacing the closed-window result to the player. Lane B kept active-delivery actions visible on the normal post-travel Result view, restoring the Count schedule's Hold Sightline control immediately after Cage arrival. Lane B made the authenticated Planning Table Fold a paid teardown rather than a terminal abort: it now clears only that plan's setup delivery while retaining the already-authenticated lock and earned setup facts, with the same narrow migration rescuing saves made under the old behavior even if later table choices lowered the architect below the original rank gate. Lane B aligned the Count's schedule hold with the existing Heat 55 Crew lookout band and made its authored nine-action table window inclusive, so the observed normal return at Heat 53 and the third minimum blackjack hand no longer impose unavoidable failure/late penalties; all global heat, route, wager, odds, and payout rules remain unchanged. Lane B reconciled immutable scenario baseline rectangle membership against the current authenticated room bindings before finalization, preventing a refreshed overflow event from regaining stale geometry and blocking normal Punchline layer travel while preserving the strict sealed-layout validator. Lane B pinned Rook's live leads encounter to its authored `standing_person` class in every Punchline layer, so sealed Back Room presentation can validate and commit the Count schedule hook instead of misclassifying the visible crew contact as a fixture. Lane B moved the Back Room practice rig within its authored support so its full-size label and hit target remain disjoint from Rook's ride doorway in both normal and expanded layouts, restoring Planning Table access. Lane B decoded the preserved Count save and retained its valid active schedule route: the Planning Table now marks that exact run as already live and directs the player back to Grand Cage instead of offering a duplicate service that must fail. Lane B also renamed the quiet-table sequence's forbidden machine identifier to a public-safe form and drops only its legacy invalid registration on load, restoring sealed room presentation without weakening identifier validation. Lane B authored the missing `center` semantic zone in the Grand Back Room, which is accepted as a Count live-table venue, preventing a later live-play sequence from failing sealed target authority there. Lane B gave the authenticated Count play a one-time 24-chip Crew table float—the exact three minimum $8 hands—so an otherwise valid score cannot dead-end after setup expenses or ordinary losses; the global blackjack stake, odds, and payout remain unchanged, and the table no longer offers Begin the Play before setup is complete. Lane C confirmed the hot-schedule recovery remains normally playable through the Cage ATM and cheap Pull Tabs, so Lane B removed the proposed automatic cooldown and retained the existing heat economy. Lane B made the dynamic delivery action strip invalidate the clipped HUD height whenever its contents change, so Cage `Hold Sightline` and other authenticated route controls occupy a real visible/hittable second row instead of remaining below the cached one-row boundary. Lane B routed the exact direct-delivery action ahead of generic character dialogue in More room actions, so the handoff fallback completes the package instead of reopening an unrelated NPC sequence. Lane C completed the normal-play Count route at bridge action 998 with Bishop as the sole required specialist; `Hold`, `Sit`, and `Dock` resolved the three live-table decisions before the authenticated exit produced the `Somebody Got Pinched` victory. The opened review image is `D:\Projects\Beat-The-House\.tmp\owner_review\ending_heist.png`. Final public ledger: $366 put to work, $149 Grand Casino ATM debt outstanding, and $45 Crew settlement. The 0.6.1 backlog records the deferred broader heist plan, trust grind, and longer setup. |
| T2 | **Balance: clean and cheat.** Replay both finished routes and record the action count and money curve. Tune data (prices, rewards, lender terms, gates) so each takes about 150–350 actions for a normal player; jackpots and fast routes may be quicker. Log changes in `docs/plans/rw06_3_balance_changes.md`. Don't touch heist or Crew files until T1 is DONE. | — | A | DONE | Clean `players_card`: 197 actions, debt-free, final $68 cash (pre-win peak $193 chips). Cheat `showdown_survived`: 117 actions, debt-free, final $79 cash / $39 chips at 89 heat. Current curves logged; no further tuning needed. |
| T3 | **Full-run bug pass: clean.** Play the clean ending start to finish on current `main` like a new player, in the Windows build or the Web build at `http://localhost:8088/p/beat-the-house/play/latest/v0.6.0/`. Fix anything that breaks the run: crashes, dead ends, unclickable objects, script errors, wrong text. After T2 merges, replay once more. | — | B | DONE | Pre-T2 and post-T2 production-input runs reached `players_card` in 198 and 197 actions with clean logs; corrected Linda's contradictory ready-state marker copy. Screenshot: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_clean.png`. |
| T3b | **Full-run bug pass: cheat.** Same as T3, for the cheat ending. | — | D | DONE | Full current-code run reached `showdown_survived` in 118 actions; screenshot: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_cheat.png`. No product defect found. |
| T4 | **Balance: heist.** Same as T2, for the heist route. | T1 | D | DONE | Owner-timeboxed final Count replay reached `heist_out_hot` at bridge action **389** (**164** gameplay actions), down from 998. The run retained Audit Roster, schedule hold, cart handoff, three ordinary blackjack hands, visible Hold -> Sit -> Dock decisions, and the marked getaway. It entered live play at **$107 cash / 24 chips / Heat 17 / no debt**, reached the third hand at **$102 cash / 0 chips / Heat 11**, and finished at **$1,002 bankroll / no debt**. Global prices, blackjack rules, odds, RTP, and the Count payout ladder are unchanged. Evidence: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_heist.png`. Further pacing ideas are deferred to `docs/plans/0.6.1_backlog.md`. |
| T5 | **Full-run bug pass: heist.** Same as T3, for the heist. | T1 | C | DONE | Fresh new-player Bishop + Count run completed through normal visible controls on main. Count now gives an exact next-step prompt for `Hold → blackjack hand → Sit → blackjack hand → Dock → blackjack hand → Take the exit`, including why pre-decision hands do not count. Concrete route fixes kept the Audit Roster reachable, restored authenticated delivery/contact actions, and opened only the active Count getaway leg through closed venue hours. Crew action count: 40. Public economy checkpoints: $80 start; $157 after the $70 Crew advance; $175 with the favor settled and debt cleared; $77 / 16 chips before Count play; one-time table float to 40 chips; $77 / 35 chips / Heat 49 at getaway. Final report: $146 money put to work, Crew debt $70 settled, 2/2 deliveries complete, 1/1 job complete, Bishop only, `heist_out_hot` victory. Evidence: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_heist.png`. |
| T8 | **Web audio memory leak.** Symptom: after a web session left open for hours, the console floods with `Uncaught DataCloneError: Failed to execute 'postMessage' on 'MessagePort': Data cannot be cloned, out of memory` from the audio worklet (`RingBuffer.consumed` / `node.port.onmessage` in `index.js`), and memory grows until audio breaks. Find what keeps growing: likely the procedural music / feature PCM retained bytes, `scripts/ui/web_audio_bridge.gd`, `scripts/ui/procedural_music_player.gd`, or stream playback that is never freed. Fix it so memory stays flat over a long session. Confirm by leaving the Web build (`http://localhost:8088/p/beat-the-house/play/latest/v0.6.0/`) running with music for 30+ minutes, travelling and playing games, with no errors and flat memory (browser task manager). | — | A | DONE | Web audio sources now release handlers/nodes and prune ended one-shots; Web SFX/music fail closed instead of falling back to Godot's leaking AudioWorklet path. Fresh candidate `0.6.0-dev+24f67151817c_66d8b6fa6899_84b99da8d810` ran 30m31s with continuous music, Apartment → Corner Store → Gas Casino travel, repeated coin-pusher drops and Pull Tabs play. Chrome private memory stayed flat/down (1,587.6 → 1,569.0 MB after warmup); zero browser warnings/errors or audio-worklet failures. |
| T9 | **Coin pusher: unplayably slow, and its UI shows in every game environment.** (a) Make all three coin pusher machines smooth to play in both the Web build and the Windows build. Find the real cost with the in-game perf overlay or a profiler (native solver calls, per-frame redraws, physics step, deep copies), fix it, and keep the machines' rules and EV unchanged. (b) The coin pusher's UI appears in other game environments. Find why it leaks outside the machine (for example, an overlay or HUD node that isn't hidden or freed on exit, or a shared surface that isn't reset) and make it appear only while a coin pusher is open. Confirm by playing each machine for a few minutes on Web and Windows, then entering two other games and rooms to check that no coin pusher UI remains. | — | D | DONE | Web presentation now publishes at a bounded 30 FPS instead of the old 1.1-second cadence, and surface replacement explicitly releases the coin-pusher hardware/static caches so they cannot survive into another game. Rules and EV are unchanged. A fresh Windows candidate from `dfa295b3` exported cleanly; Quarter Falls, Jackpot Ridge, and The Vault Drop stayed responsive through normal drop/stop/nudge play. Scratch Tickets, Pull Tabs, and the returned room showed their own renderer/no renderer with zero coin-pusher actions. Evidence: `D:\Projects\Beat-The-House\.tmp\owner_review\t9_windows_quarter_falls.png`, `t9_windows_jackpot_ridge.png`, `t9_windows_vault_drop.png`, `t9_windows_scratch_tickets_no_leak.png`, `t9_windows_pull_tabs_no_leak.png`, `t9_windows_room_no_leak.png`. |
| T6 | **Test repair.** Run `tools/check_godot.ps1 -Suite Smoke` and then `-Suite Contract` once each, alone on the machine. Fix every red by fixing the product, or by updating a test that encodes superseded behavior (rooms, tutorial, Cass and similar), noting why. Never weaken a real guarantee. | T2, T3, T3b, T4, T5, T8, T9 | B | IN PROGRESS | Lane C verified the previously reported `gdscript_load_check` / `env06_6_cluster_contract.gd` failure is already green on current main after `fb916703`. Lane C completed the non-overlapping `contracts_runtime_economy` release-scope repair at `8e92d74e`: the strict Crew content validator now accepts Bishop by omitting his meaningless empty Inner Circle perk key, while lender tests assert the authored `$70` / one-favor release terms, a separate refusal lifecycle, and Count-only release eligibility instead of the superseded `$45` / two-favor / Whale behavior. Focused `lender_debt_foundation` rerun removed all six of those failures; its 13 remaining failures are the room-layout/delivery cascade Lane B already owns. `economy_pressure`, `service_hook`, `suspicion_security`, and `run_report` passed; `travel_route` is likewise blocked only by Lane B-owned room geometry. Lane C releases this shard. Final split: Lane B owns authored room geometry, slot validation, `travel_route`, and the final full-suite Smoke and Contract runs; Lane C owns the Smoke content/runtime/UI shards; Lane A owns the performance shards, including `foundation_perf_smoke`, and every remaining Contract shard. Lane C Smoke content is green at `f2f46d51`: the prior layout assertions were already repaired; the remaining Audit Roster failure was fixed in product copy so the visible result explicitly says a new travel route opened and names Small Underground Casino/The Punchline. Focused `content` rerun passed with zero failures. Lane C Smoke runtime is green at `c50d5895`: all six runtime checks pass. The only current failure was a real Buffalo feature-audio cold-load hitch (117.5 ms against the retained 100 ms limit); the delivered stem loader now streams the header and five PCM payloads directly instead of reading the full 3.8 MB pack and copying five slices. Audio data/behavior and the strict budget are unchanged. Lane B's full authored-slot static validation passed all 21 maps, 18 archetypes, and 55 scenarios with zero base/scenario or base/base conflicts. The corrected focused Contract `travel_route_foundation` run on `c50d5895` executed once and passed with zero failures. |
T6 Notes (Lane A performance): the qualifying machine-alone run is green at `23d7ce74` with every existing budget retained. Product fixes compact Scratch purchase receipts, avoid cloning unrelated room state and a second complete Blackjack table during compatibility settlement, and resolve the themed font once for each shadowed animated label. Exact repaired observations: Scratch resolve **1.777 ms avg / 4.014 ms p95 / 5.467 ms max**, Blackjack resolve **2.913 / 3.096 / 3.119 ms**, Blackjack idle draw **3.190 ms avg / 3.399 ms p95**, and Coin Pusher idle draw **3.367 / 4.785 ms**; animation liveness remained active with 50 redraws against the unchanged floor of 8. The complete 46-observation performance probe passed.

T6 Notes (Lane A final performance): `93ed547d` opts native Blackjack into the existing stepped low-detail idle cast at 15 FPS while finite active channels retain 60 FPS; two ordinary viewport captures showed the idle table visibly animating between frames. After an isolated run exposed Coin Pusher idle draw at **6.203 ms p95** against the unchanged **5.0 ms** budget, `1f1fdf33` enabled the renderer's existing pixel-equivalent retained shell, backglass, playfield, and hardware layers on native while leaving the moving platform/body batch live. The machine-alone `foundation_perf_smoke` rerun on exact `9fd15ec4` passed all **46 observations** with zero failures: Coin Pusher idle **2.255 ms avg / 2.598 ms p95 / 2.843 ms max** with **49 redraws**, Blackjack idle **2.166 / 2.888 / 2.888 ms** with **13 redraws**, both above the unchanged liveness floor of **8**. Coin Pusher active draw p95 remained green from **2.301 to 2.945 ms** with physical motion present. Evidence: `.tmp/t6_a/foundation_perf_smoke_9fd15ec4.json` in Lane A's worktree.

T6 Notes (Lane C Smoke UI): UI fixes are merged at `c78a0d7a`. The release identity assertion now follows the authoritative `0.6.0` development identity; Path A retains the real Underground/return-route guarantee without demanding the superseded uncapped Apartment card; and the visible `Restart Tutorial` product action once again starts First Night while preserving the one-time profile-offer flag. The focused UI rerun passed those boundaries, then stopped only in Lane B's authored-room scope: Pawn Shop `meta_pawn_counter:sell` projected as geometry-free overflow instead of receiving a distinct fixed behind-counter slot beside Sal. Exact evidence is `.tmp/test_reports/t6_c_smoke_ui_fix3/{stdout.txt,stderr.txt,godot.log}` in Lane C's worktree. After Lane B's slot repair, `3b4eb18c` refreshed only the ordinary-travel layout hashes whose underlying fare/clock/heat/RNG/route/story/action fields remained byte-identical, and `4e5d84d9` kept the abstract-marker rejection while accepting an authenticated delivery contact that owns the legacy handoff object ID. The next focused run reached the terminal Crew favor handoff; its captured save proved the UI fixture had queued the favor without its required Crew debt, so production correctly failed closed and rolled back. `3506800f` restores that authentic due-marker prerequisite while retaining exact +22 cash/+4 heat assertions and additionally requiring atomic marker clearance; `8a5dda35` matches the marker fixture to the current authored `$70` / one-favor conversion authority. The next run exposed a real presentation-state write: changing only the action-card category rebuilt and recommitted the room-object catalog because `current_screen` was unnecessarily part of its cache key even though no catalog projection consumes it. Product fix `d8a042f4` (merged as `4884b851`) removes that screen-only invalidator while retaining every gameplay/selection invalidator and the strict serialized-state assertion. That repaired boundary exposed the same write when merely highlighting a destination: `selected_travel_target_id` was also unused by room-object projection but forced the same authority rebuild. Product fix `2240e0d3` (merged as `ecc46914`) removes only that travel/map-only invalidator; event/item selection invalidation remains because it changes inline catalog presentation. The next run reached the Crew lender popup regression, whose synthetic Motel room demanded a canvas actor despite having no authored Crew slot under the current strict placement contract. Test repair `26564e6c` (merged as `fc9cab97`) injects the same unrelated `motel_knock` event into Corner Store, where The Crew owns authenticated three-person placement, preserving the popup-isolation, identity, rollback, and lender-result guarantees without inventing geometry. Final isolated direct rerun on `ba39d99a` is GREEN across all six UI programs: `ui_scene_compile`, `game_library_launchers`, `dave_bus_encounter`, `inventory_spatial_ui`, `inventory_spatial_main_integration`, and `ui05_design_system`. Evidence is `.tmp/test_reports/t6_c_smoke_ui_final_direct3/`; wrapper `15184` and pairs `18008/11260`, `16172/8340`, `25804/24348`, `20128/30496`, `17592/25612`, and `28468/30540` all exited with zero Godot residual. Lane C Smoke content/runtime/UI ownership is complete and released; Lane D may resume Contract.

T6 Notes (Lane D Contract): Lane D owns every remaining Contract shard except performance (Lane A), authored room geometry / slots / `travel_route` and the final full-suite runs (Lane B), and Smoke content / runtime / UI (Lane C). Lane D will run its Contract shards one at a time and land narrowly scoped product or superseded-expectation repairs without weakening any guarantee.

| T7 | **Final release.** Update the release copy in `docs/plans/release_0_6_0_copy.md` with the final facts (read the Q-022 answer in the questions file first). Build the final Windows and Web zips from `main` with `tools/export_itch.ps1` (no `-Push`, never upload), then launch each once. Refresh the site's web channels at `D:\Projects\site\projects\beat-the-house\public\play\{latest,dev}\v0.6.0\`. Ask the owner the artifact-handoff question with zip paths and SHA-256 hashes. | T6 | | OPEN | |
