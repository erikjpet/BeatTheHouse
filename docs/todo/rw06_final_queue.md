# rw06 final queue

Rules: `rw06_final_push_prompt.md`. Claim by putting your lane in Owner and setting State to IN PROGRESS. Edit only your own rows. Never claim a task whose Needs are not DONE.

| ID | Task | Needs | Owner | State | Notes |
| --- | --- | --- | --- | --- | --- |
| T1 | **Heist ending.** Reach the Crew heist win screen through normal play and save `.tmp\owner_review\ending_heist.png`. Narrow to one plan or fewer recruits if needed; log the cuts. Includes merging the Switch overflow handoff. | — | C | IN PROGRESS | continue from `codex/reset-c-heist` (worktree `reset-c-heist-part3`); Lane B aligned Punchline Crew Back Room access with the release Count's Associate architect gate, removing the hidden five-job grind. Lane B pinned Bishop's authored placement to `standing_person` across all three Grand rooms, preventing sealed-layout validation from misclassifying the live contact as a fixture and suppressing the pending Crew handoff. Lane B split Cass and the Host Desk across accessible right/left targets on the same authored desk so their normal and expanded small-screen authority stays disjoint, authored the delivery sequence's `center` zone in High-Limit and Cage, and authenticated Cage shelf records against their exact saved stock row so normal interior travel cannot fail closed before a pending handoff appears. Lane B pinned every declared Punchline layer transition to an authored doorway slot and doorway class, replacing the casino layer's erroneous shared floor-fixture binding so the Club and Crew Back Room controls remain hittable after saved-layout regeneration; Lane B then consolidated the Back Room's duplicate class override objects so its casino exit actually retains that doorway authority after JSON parsing. Lane B made the Planning/Live Table leave rows true UI dismissals and preserved that dismissal authority through the projected inline room-action choices, so every table path clears the event modal, selection, focus, and event screen instead of advancing a no-op Crew event. Lane B routed the visible inline event rows through that same dismissal-aware response handler, so the native Planning/Live Table Leave row no longer bypasses the dismissal contract and leaves a stale EVENT selection. Lane B kept every live delivery contact's authenticated handoff action in More room actions, giving deadline-bound package jobs a reachable fallback when person arrival or another room sequence temporarily owns the canvas hit plane. Lane B centered the Delta Queen staff label on its authored table slot and moved the Crew lender label below the Rowdy Regular's expanded target, separating both pairs in normal and expanded layouts so sealed presentation can attach pending delivery contacts. Lane B made the Count's exact planning-to-Grand, return, live-play, and marked-getaway legs crew-covered while that score is active, removing repeated $70 fare walls without changing any global route price, wager, deadline, odds, or payout; the Planning Table now also discloses the Grand Heat 40 hold limit. Lane B made modeled delivery expiry/capture a committed gameplay result instead of a failed travel transaction, preventing the last-action Count schedule from rolling back into the same permanently enabled dead-end route and surfacing the closed-window result to the player. Lane B kept active-delivery actions visible on the normal post-travel Result view, restoring the Count schedule's Hold Sightline control immediately after Cage arrival. Lane B made the authenticated Planning Table Fold a paid teardown rather than a terminal abort: it now clears only that plan's setup delivery while retaining the already-authenticated lock and earned setup facts, with the same narrow migration rescuing saves made under the old behavior even if later table choices lowered the architect below the original rank gate. Lane B aligned the Count's schedule hold with the existing Heat 55 Crew lookout band and made its authored nine-action table window inclusive, so the observed normal return at Heat 53 and the third minimum blackjack hand no longer impose unavoidable failure/late penalties; all global heat, route, wager, odds, and payout rules remain unchanged. Lane B reconciled immutable scenario baseline rectangle membership against the current authenticated room bindings before finalization, preventing a refreshed overflow event from regaining stale geometry and blocking normal Punchline layer travel while preserving the strict sealed-layout validator. Lane B pinned Rook's live leads encounter to its authored `standing_person` class in every Punchline layer, so sealed Back Room presentation can validate and commit the Count schedule hook instead of misclassifying the visible crew contact as a fixture. Lane B moved the Back Room practice rig within its authored support so its full-size label and hit target remain disjoint from Rook's ride doorway in both normal and expanded layouts, restoring Planning Table access. Lane B decoded the preserved Count save and retained its valid active schedule route: the Planning Table now marks that exact run as already live and directs the player back to Grand Cage instead of offering a duplicate service that must fail. Lane B also renamed the quiet-table sequence's forbidden machine identifier to a public-safe form and drops only its legacy invalid registration on load, restoring sealed room presentation without weakening identifier validation. |
| T2 | **Balance: clean and cheat.** Replay both finished routes and record the action count and money curve. Tune data (prices, rewards, lender terms, gates) so each takes about 150–350 actions for a normal player; jackpots and fast routes may be quicker. Log changes in `docs/plans/rw06_3_balance_changes.md`. Don't touch heist or Crew files until T1 is DONE. | — | A | DONE | Clean `players_card`: 197 actions, debt-free, final $68 cash (pre-win peak $193 chips). Cheat `showdown_survived`: 117 actions, debt-free, final $79 cash / $39 chips at 89 heat. Current curves logged; no further tuning needed. |
| T3 | **Full-run bug pass: clean.** Play the clean ending start to finish on current `main` like a new player, in the Windows build or the Web build at `http://localhost:8088/p/beat-the-house/play/latest/v0.6.0/`. Fix anything that breaks the run: crashes, dead ends, unclickable objects, script errors, wrong text. After T2 merges, replay once more. | — | B | DONE | Pre-T2 and post-T2 production-input runs reached `players_card` in 198 and 197 actions with clean logs; corrected Linda's contradictory ready-state marker copy. Screenshot: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_clean.png`. |
| T3b | **Full-run bug pass: cheat.** Same as T3, for the cheat ending. | — | D | DONE | Full current-code run reached `showdown_survived` in 118 actions; screenshot: `D:\Projects\Beat-The-House\.tmp\owner_review\ending_cheat.png`. No product defect found. |
| T4 | **Balance: heist.** Same as T2, for the heist route. | T1 | | OPEN | |
| T5 | **Full-run bug pass: heist.** Same as T3, for the heist. | T1 | | OPEN | |
| T8 | **Web audio memory leak.** Symptom: after a web session left open for hours, the console floods with `Uncaught DataCloneError: Failed to execute 'postMessage' on 'MessagePort': Data cannot be cloned, out of memory` from the audio worklet (`RingBuffer.consumed` / `node.port.onmessage` in `index.js`), and memory grows until audio breaks. Find what keeps growing: likely the procedural music / feature PCM retained bytes, `scripts/ui/web_audio_bridge.gd`, `scripts/ui/procedural_music_player.gd`, or stream playback that is never freed. Fix it so memory stays flat over a long session. Confirm by leaving the Web build (`http://localhost:8088/p/beat-the-house/play/latest/v0.6.0/`) running with music for 30+ minutes, travelling and playing games, with no errors and flat memory (browser task manager). | — | A | DONE | Web audio sources now release handlers/nodes and prune ended one-shots; Web SFX/music fail closed instead of falling back to Godot's leaking AudioWorklet path. Fresh candidate `0.6.0-dev+24f67151817c_66d8b6fa6899_84b99da8d810` ran 30m31s with continuous music, Apartment → Corner Store → Gas Casino travel, repeated coin-pusher drops and Pull Tabs play. Chrome private memory stayed flat/down (1,587.6 → 1,569.0 MB after warmup); zero browser warnings/errors or audio-worklet failures. |
| T9 | **Coin pusher: unplayably slow, and its UI shows in every game environment.** (a) Make all three coin pusher machines smooth to play in both the Web build and the Windows build. Find the real cost with the in-game perf overlay or a profiler (native solver calls, per-frame redraws, physics step, deep copies), fix it, and keep the machines' rules and EV unchanged. (b) The coin pusher's UI appears in other game environments. Find why it leaks outside the machine (for example, an overlay or HUD node that isn't hidden or freed on exit, or a shared surface that isn't reset) and make it appear only while a coin pusher is open. Confirm by playing each machine for a few minutes on Web and Windows, then entering two other games and rooms to check that no coin pusher UI remains. | — | D | DONE | Web presentation now publishes at a bounded 30 FPS instead of the old 1.1-second cadence, and surface replacement explicitly releases the coin-pusher hardware/static caches so they cannot survive into another game. Rules and EV are unchanged. A fresh Windows candidate from `dfa295b3` exported cleanly; Quarter Falls, Jackpot Ridge, and The Vault Drop stayed responsive through normal drop/stop/nudge play. Scratch Tickets, Pull Tabs, and the returned room showed their own renderer/no renderer with zero coin-pusher actions. Evidence: `D:\Projects\Beat-The-House\.tmp\owner_review\t9_windows_quarter_falls.png`, `t9_windows_jackpot_ridge.png`, `t9_windows_vault_drop.png`, `t9_windows_scratch_tickets_no_leak.png`, `t9_windows_pull_tabs_no_leak.png`, `t9_windows_room_no_leak.png`. |
| T6 | **Test repair.** Run `tools/check_godot.ps1 -Suite Smoke` and then `-Suite Contract` once each, alone on the machine. Fix every red by fixing the product, or by updating a test that encodes superseded behavior (rooms, tutorial, Cass and similar), noting why. Never weaken a real guarantee. | T2, T3, T3b, T4, T5, T8, T9 | | OPEN | |
| T7 | **Final release.** Update the release copy in `docs/plans/release_0_6_0_copy.md` with the final facts (read the Q-022 answer in the questions file first). Build the final Windows and Web zips from `main` with `tools/export_itch.ps1` (no `-Push`, never upload), then launch each once. Refresh the site's web channels at `D:\Projects\site\projects\beat-the-house\public\play\{latest,dev}\v0.6.0\`. Ask the owner the artifact-handoff question with zip paths and SHA-256 hashes. | T6 | | OPEN | |
